<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>10K&#39;s</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2020-10-09T01:46:44.266Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>10K</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>30 Min to Docker</title>
    <link href="http://yoursite.com/2020/10/08/30-Min-to-Docker/"/>
    <id>http://yoursite.com/2020/10/08/30-Min-to-Docker/</id>
    <published>2020-10-09T01:19:06.000Z</published>
    <updated>2020-10-09T01:46:44.266Z</updated>
    
    <content type="html"><![CDATA[<p>Docker 是一个开放源代码软件项目，项目主要代码在2013年开源于 GitHub。它是云服务技术上的一次创新，让应用程序布署在软件容器下的工作可以自动化进行，借此在 Linux 操作系统上，提供一个额外的软件抽象层，以及操作系统层虚拟化的自动管理机制。</p><p>Docker 利用 Linux 核心中的资源分脱机制，例如 cgroups，以及 Linux 核心名字空间（name space），来创建独立的软件容器（containers），属于操作系统层面的虚拟化技术。由于隔离的进程独立于宿主和其它的隔离的进程，因此也称其为容器。Docker 在容器的基础上进行了进一步的封装，从文件系统、网络互联到进程隔离等等，极大的简化了容器的创建和维护，使得其比虚拟机技术更为轻便、快捷。Docker 可以在单一 Linux 实体下运作，避免因为创建一个虚拟机而造成的额外负担。</p><a id="more"></a><h2 id="一、欢迎来到-Docker-世界"><a href="#一、欢迎来到-Docker-世界" class="headerlink" title="一、欢迎来到 Docker 世界"></a>一、欢迎来到 Docker 世界</h2><h3 id="1-Docker-与虚拟化"><a href="#1-Docker-与虚拟化" class="headerlink" title="1. Docker 与虚拟化"></a>1. Docker 与虚拟化</h3><p>在没有 Docker 的时代，我们会使用硬件虚拟化（虚拟机）以提供隔离。这里，虚拟机通过在操作系统上建立了一个中间虚拟软件层 Hypervisor ，并利用物理机器的资源虚拟出多个虚拟硬件环境来共享宿主机的资源，其中的应用运行在虚拟机内核上。但是，虚拟机对硬件的利用率存在瓶颈，因为虚拟机很难根据当前业务量动态调整其占用的硬件资源，因此容器化技术得以流行。其中，Docker 是一个开源的应用容器引擎，让开发者可以打包他们的应用以及依赖包到一个可移植的容器中，然后发布到任何流行的 Linux 机器上。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206876/Docker/docker1_dhiq4d.png" alt="image.png">Docker 容器不使用硬件虚拟化，它的守护进程是宿主机上的一个进程，换句话说，应用直接运行在宿主机内核上。因为容器中运行的程序和计算机的操作系统之间没有额外的中间层，没有资源被冗余软件的运行或虚拟硬件的模拟而浪费掉。</p><p>Docker 的优势不仅如此，我们来比较一番。</p><table><thead><tr><th>特性</th><th>Docker</th><th>虚拟机</th></tr></thead><tbody><tr><td>启动速度</td><td>秒级</td><td>分钟级</td></tr><tr><td>交付/部署</td><td>开发、测试、生产环境一致</td><td>无成熟体系</td></tr><tr><td>性能</td><td>近似物理机</td><td>性能损耗大</td></tr><tr><td>体量</td><td>极小（MB）</td><td>较大（GB）</td></tr><tr><td>迁移/扩展</td><td>跨平台，可复制</td><td>较为复杂</td></tr></tbody></table><h3 id="2-镜像、容器和仓库"><a href="#2-镜像、容器和仓库" class="headerlink" title="2. 镜像、容器和仓库"></a>2. 镜像、容器和仓库</h3><p>Docker 由镜像(Image)、容器(Container)、仓库(Repository) 三部分组成。</p><p>Docker 的镜像可以简单的类比为电脑装系统用的系统盘，包括操作系统，以及必要的软件。例如，一个镜像可以包含一个完整的 centos 操作系统环境，并安装了 Nginx 和 Tomcat 服务器。注意的是，镜像是只读的。这一点也很好理解，就像我们刻录的系统盘其实也是可读的。我们可以使用 <code>docker images</code> 来查看本地镜像列表。</p><p>Docker 的容器可以简单理解为提供了系统硬件环境，它是真正跑项目程序、消耗机器资源、提供服务的东西。例如，我们可以暂时把容器看作一个 Linux 的电脑，它可以直接运行。那么，容器是基于镜像启动的，并且每个容器都是相互隔离的。注意的是，容器在启动的时候基于镜像创建一层可写层作为最上层。我们可以使用 <code>docker ps -a</code> 查看本地运行过的容器。</p><p>Docker 的仓库用于存放镜像。这一点，和 Git 非常类似。我们可以从中心仓库下载镜像，也可以从自建仓库下载。同时，我们可以把制作好的镜像 commit 到本地，然后 push 到远程仓库。仓库分为公开仓库和私有仓库，最大的公开仓库是官方仓库 Dock Hub，国内的公开仓库也有很多选择，例如阿里云等。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206876/Docker/docker2_mbemm8.png" alt="docker2"></p><h3 id="3-Docker-促使开发流程变更"><a href="#3-Docker-促使开发流程变更" class="headerlink" title="3. Docker 促使开发流程变更"></a>3. Docker 促使开发流程变更</h3><p>笔者认为，Docker 对开发流程的影响在于使环境标准化。例如，原来我们存在三个环境：开发（日常）环境、测试环境、生产环境。这里，我们对于每个环境都需要部署相同的软件、脚本和运行程序，如图所示。事实上，对于启动脚本内容都是一致的，但是没有统一维护，经常会出问题。此外，对于运行程序而言，如果所依赖的底层运行环境不一致，也会造成困扰和异常。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker3_komnuc.png" alt="docker3"></p><p>现在，我们通过引入 Docker 之后，我们只需要维护一个 Docker 镜像。换句话说，多套环境，一个镜像，实现系统级别的一次构建到处运行。此时，我们把运行脚本标准化了，把底层软件镜像化了，然后对于相同的将要部署的程序实行标准化部署。因此，Docker 为我们提供了一个标准化的运维模式，并固化运维步骤和流程。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker4_qeqjk4.png" alt="docker4"></p><p>通过这个流程的改进，我们更容易实现 DevOps 的目标，因为我们的镜像生成后可以跑在任何系统，并快速部署。此外，使用 Docker 的很大动力是基于 Docker 实现弹性调度，以更充分地利用机器资源，节省成本。</p><p>哈哈，笔者在使用 Docker 过程中，还发现了一些很棒的收益点，例如我们发布回滚的时候只需要切换 TAG 并重启即可。还比如，我们对环境升级，也只需要升级基础镜像，那么新构建的应用镜像，自动会引用新的版本。（欢迎补充～～～）</p><h2 id="二、从搭建-Web-服务器开始说起"><a href="#二、从搭建-Web-服务器开始说起" class="headerlink" title="二、从搭建 Web 服务器开始说起"></a>二、从搭建 Web 服务器开始说起</h2><h3 id="1-环境先行，安装-Docker"><a href="#1-环境先行，安装-Docker" class="headerlink" title="1. 环境先行，安装 Docker"></a>1. 环境先行，安装 Docker</h3><p>现在，我们需要安装以下步骤安装 Docker。</p><ul><li>注册帐号：在 <a href="https://hub.docker.com/" target="_blank" rel="noopener">hub.docker.com/</a> 注册账号。</li><li>下载安装</li></ul><blockquote><p>官方下载地址：（Mac）：<a href="https://download.docker.com/mac/stable/Docker.dmg" target="_blank" rel="noopener">download.docker.com/mac/stable/…</a> 阿里云下载地址（Mac）：<a href="http://mirrors.aliyun.com/docker-toolbox/mac/docker-for-mac/?spm=5176.8351553.0.0.468b1991jdT95t" target="_blank" rel="noopener">mirrors.aliyun.com/docker-tool…</a> 阿里云下载地址（Windows）： <a href="http://mirrors.aliyun.com/docker-toolbox/windows/docker-for-windows/" target="_blank" rel="noopener">mirrors.aliyun.com/docker-tool…</a></p></blockquote><ul><li>安装指南 这里，双击刚刚下载的 Doker.dmg 安装包进行安装。<br><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker5_xexwy3.png" alt="dcoker5"></li></ul><p>安装完成后启动， Mac 顶部导航栏出现了一个图标，通过菜单可以进行 docker 配置和退出等操作。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker6_hc5ngs.png" alt="dcoker6"></p><blockquote><p>官方指南：<a href="https://docs.docker.com/install/" target="_blank" rel="noopener">docs.docker.com/install/</a> 阿里云指南（Linux）：<a href="https://yq.aliyun.com/articles/110806?spm=5176.8351553.0.0.468b1991jdT95t" target="_blank" rel="noopener">yq.aliyun.com/articles/11…</a></p></blockquote><ul><li>设置加速服务</li></ul><p>市面上有很多加速服务的提供商，如：DaoCloud，阿里云等。这里，笔者使用的是阿里云。（注意的是，笔者操作系统是 Mac，其他操作系列参见阿里云操作文档）</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker7_yfcmkq.png" alt="docker7"></p><p>右键点击桌面顶栏的 docker 图标，选择 Preferences ，在 Daemon 标签（Docker 17.03 之前版本为 Advanced 标签）下的 Registry mirrors 列表中将<br><code>https://xxx.mirror.aliyuncs.com</code> 加到”registry-mirrors”的数组里，点击 Apply &amp; Restart 按钮，等待 Docker 重启并应用配置的镜像加速器。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206869/Docker/docker8_bqj4mr.png" alt="dcoker8"></p><blockquote><p>阿里云操作文档：<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/mirrors" target="_blank" rel="noopener">cr.console.aliyun.com/cn-hangzhou…</a></p></blockquote><ul><li>查看版本</li></ul><p>至此，我们已经安装完成了。这里，我们来查看版本。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker version</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>查看结果，如下所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker9_jtz10z.png" alt="docker9"></p><h3 id="2-实干派，从搭建-Web-服务器开始"><a href="#2-实干派，从搭建-Web-服务器开始" class="headerlink" title="2. 实干派，从搭建 Web 服务器开始"></a>2. 实干派，从搭建 Web 服务器开始</h3><p>我们作为实干派，那么先来搭建一个 Web 服务器吧。然后，笔者带你慢慢理解这个过程中，做了什么事情。首先，我们需要拉取 centos 镜像。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker run -p 80 --name web -i -t centos &#x2F;bin&#x2F;bash</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>紧接着，我们安装 nginx 服务器，执行以下命令：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rpm -ivh http:&#x2F;&#x2F;nginx.org&#x2F;packages&#x2F;centos&#x2F;7&#x2F;noarch&#x2F;RPMS&#x2F;nginx-release-centos-7-0.el7.ngx.noarch.rpm</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>安装完 Nginx 源后，就可以正式安装 Nginx 了。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">yum install -y nginx</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>至此，我们再输入 <code>whereis nginx</code> 命令就可以看到安装的路径了。最后，我们还需要将 Nginx 跑起来。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">nginx</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>现在，我们执行 <code>ctrl + P +  Q</code> 切换到后台。然后，通过 <code>docker ps -a</code> 来查看随机分配的端口。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker10_luhnj2.png" alt="docker10"></p><p>这里，笔者分配的端口是 <code>32769</code> ，那么通过浏览器访问 <code>http://127.0.0.1:32769</code> 即可。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker11_bvxa2e.png" alt="dcoker11"></p><p>大功告成，哈哈哈～</p><h3 id="3-复盘理解全过程"><a href="#3-复盘理解全过程" class="headerlink" title="3. 复盘理解全过程"></a>3. 复盘理解全过程</h3><p>现在，我们来理解下这个流程。首先，我们输入 <code>docker run -p 80 --name web -i -t centos /bin/bash</code> 命令会运行交互式容器，其中 <code>-i</code> 选项告诉 Docker 容器保持标准输入流对容器开放，即使容器没有终端连接，另一个 <code>-t</code> 选项告诉 Docker 为容器分配一个虚拟终端，以便于我们接下来安装 Nginx 服务器。（笔者备注：Docker 还支持输入 <code>-d</code> 选项告诉 Docker 在后台运行容器的守护进程）</p><p>Docker 会为我们创建的每一个容器自动生成一个随机的名称。事实上，这种方式虽然便捷，但是可读性很差，并且对我们后期维护的理解成本会比较大。因此，我们通过 <code>--name web</code> 选项告诉 Docker 创建一个名称是 <code>web</code> 的容器。此外，我们通过 <code>-p 80</code> 告诉 Docker 开放 80 端口，那么， Nginx 才可以对外通过访问和服务。但是，我们的宿主机器会自动做端口映射，比如上面分配的端口是 <code>32769</code> ，注意的是，如果关闭或者重启，这个端口就变了，那么怎么解决固定端口的问题，笔者会在后面详细剖析和带你实战。</p><p>这里，还有一个非常重要的知识点 <code>docker run</code> 。Docker 通过 run 命令来启动一个新容器。Docker 首先在本机中寻找该镜像，如果没有安装，Docker 在 Docker Hub 上查找该镜像并下载安装到本机，最后 Docker 创建一个新的容器并启动该程序。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker12_dnhf9b.png" alt="docker12"></p><p>但是，当第二次执行 <code>docker run</code> 时，因为 Docker 在本机中已经安装该镜像，所以 Docker 会直接创建一个新的容器并启动该程序。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker13_nguuew.png" alt="docker13"></p><p>注意的是，<code>docker run</code> 每次使用都会创建一个新的容器，因此，我们以后再次启动这个容器时，只需要使用命令 <code>docker start</code> 即可。这里， <code>docker start</code> 的作用在用重新启动已存在的镜像，而<code>docker run</code> 包含将镜像放入容器中 <code>docker create</code> ，然后将容器启动 <code>docker start</code> ，如图所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker14_rvxkig.png" alt="docker14"></p><p>现在，我们可以在上面的案例的基础上，通过 <code>exit</code> 命令关闭 Docker 容器。当然，如果我们运行的是后台的守护进程，我们也可以通过 <code>docker stop web</code> 来停止。注意的是，<code>docker stop</code> 和 <code>docker kill</code> 略有不同，<code>docker stop</code> 发送 SIGTERM 信号，而 <code>docker kill</code> 发送SIGKILL 信号。然后，我们使用 <code>docker start</code> 重启它。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker start web</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>Docker 容器重启后会沿用 <code>docker run</code> 命令指定的参数来运行，但是，此时它还是后台运行的。我们必须通过 <code>docker attach</code> 命令切换到运行交互式容器。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker attach web</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h3 id="4-不止如此，还有更多命令"><a href="#4-不止如此，还有更多命令" class="headerlink" title="4. 不止如此，还有更多命令"></a>4. 不止如此，还有更多命令</h3><p>Docker 提供了非常丰富的命令。所谓一图胜千言，我们可以从下面的图片了解到很多信息和它们之前的用途。（可以直接跳过阅读，建议收藏，便于扩展阅读）</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker15_zu4x2k.png" alt="dcoker15"></p><p>如果希望获取更多信息，可以阅读官方使用文档。</p><table><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><a href="https://docs.docker.com/engine/reference/commandline/attach/" target="_blank" rel="noopener">docker attach</a></td><td>Attach local standard input, output, and error streams to a running container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/build/" target="_blank" rel="noopener">docker build</a></td><td>Build an image from a Dockerfile</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/builder/" target="_blank" rel="noopener">docker builder</a></td><td>Manage builds</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/checkpoint/" target="_blank" rel="noopener">docker checkpoint</a></td><td>Manage checkpoints</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/commit/" target="_blank" rel="noopener">docker commit</a></td><td>Create a new image from a container’s changes</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/config/" target="_blank" rel="noopener">docker config</a></td><td>Manage Docker configs</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/container/" target="_blank" rel="noopener">docker container</a></td><td>Manage containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/cp/" target="_blank" rel="noopener">docker cp</a></td><td>Copy files/folders between a container and the local filesystem</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/create/" target="_blank" rel="noopener">docker create</a></td><td>Create a new container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/deploy/" target="_blank" rel="noopener">docker deploy</a></td><td>Deploy a new stack or update an existing stack</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/diff/" target="_blank" rel="noopener">docker diff</a></td><td>Inspect changes to files or directories on a container’s filesystem</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/engine/" target="_blank" rel="noopener">docker engine</a></td><td>Manage the docker engine</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/events/" target="_blank" rel="noopener">docker events</a></td><td>Get real time events from the server</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/exec/" target="_blank" rel="noopener">docker exec</a></td><td>Run a command in a running container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/export/" target="_blank" rel="noopener">docker export</a></td><td>Export a container’s filesystem as a tar archive</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/history/" target="_blank" rel="noopener">docker history</a></td><td>Show the history of an image</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/image/" target="_blank" rel="noopener">docker image</a></td><td>Manage images</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/images/" target="_blank" rel="noopener">docker images</a></td><td>List images</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/import/" target="_blank" rel="noopener">docker import</a></td><td>Import the contents from a tarball to create a filesystem image</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/info/" target="_blank" rel="noopener">docker info</a></td><td>Display system-wide information</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/inspect/" target="_blank" rel="noopener">docker inspect</a></td><td>Return low-level information on Docker objects</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/kill/" target="_blank" rel="noopener">docker kill</a></td><td>Kill one or more running containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/load/" target="_blank" rel="noopener">docker load</a></td><td>Load an image from a tar archive or STDIN</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/login/" target="_blank" rel="noopener">docker login</a></td><td>Log in to a Docker registry</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/logout/" target="_blank" rel="noopener">docker logout</a></td><td>Log out from a Docker registry</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/logs/" target="_blank" rel="noopener">docker logs</a></td><td>Fetch the logs of a container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/manifest/" target="_blank" rel="noopener">docker manifest</a></td><td>Manage Docker image manifests and manifest lists</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/network/" target="_blank" rel="noopener">docker network</a></td><td>Manage networks</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/node/" target="_blank" rel="noopener">docker node</a></td><td>Manage Swarm nodes</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/pause/" target="_blank" rel="noopener">docker pause</a></td><td>Pause all processes within one or more containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/plugin/" target="_blank" rel="noopener">docker plugin</a></td><td>Manage plugins</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/port/" target="_blank" rel="noopener">docker port</a></td><td>List port mappings or a specific mapping for the container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/ps/" target="_blank" rel="noopener">docker ps</a></td><td>List containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/pull/" target="_blank" rel="noopener">docker pull</a></td><td>Pull an image or a repository from a registry</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/push/" target="_blank" rel="noopener">docker push</a></td><td>Push an image or a repository to a registry</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/rename/" target="_blank" rel="noopener">docker rename</a></td><td>Rename a container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/restart/" target="_blank" rel="noopener">docker restart</a></td><td>Restart one or more containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/rm/" target="_blank" rel="noopener">docker rm</a></td><td>Remove one or more containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/rmi/" target="_blank" rel="noopener">docker rmi</a></td><td>Remove one or more images</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/run/" target="_blank" rel="noopener">docker run</a></td><td>Run a command in a new container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/save/" target="_blank" rel="noopener">docker save</a></td><td>Save one or more images to a tar archive (streamed to STDOUT by default)</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/search/" target="_blank" rel="noopener">docker search</a></td><td>Search the Docker Hub for images</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/secret/" target="_blank" rel="noopener">docker secret</a></td><td>Manage Docker secrets</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/service/" target="_blank" rel="noopener">docker service</a></td><td>Manage services</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/stack/" target="_blank" rel="noopener">docker stack</a></td><td>Manage Docker stacks</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/start/" target="_blank" rel="noopener">docker start</a></td><td>Start one or more stopped containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/stats/" target="_blank" rel="noopener">docker stats</a></td><td>Display a live stream of container(s) resource usage statistics</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/stop/" target="_blank" rel="noopener">docker stop</a></td><td>Stop one or more running containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/swarm/" target="_blank" rel="noopener">docker swarm</a></td><td>Manage Swarm</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/system/" target="_blank" rel="noopener">docker system</a></td><td>Manage Docker</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/tag/" target="_blank" rel="noopener">docker tag</a></td><td>Create a tag TARGET_IMAGE that refers to SOURCE_IMAGE</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/top/" target="_blank" rel="noopener">docker top</a></td><td>Display the running processes of a container</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/trust/" target="_blank" rel="noopener">docker trust</a></td><td>Manage trust on Docker images</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/unpause/" target="_blank" rel="noopener">docker unpause</a></td><td>Unpause all processes within one or more containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/update/" target="_blank" rel="noopener">docker update</a></td><td>Update configuration of one or more containers</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/version/" target="_blank" rel="noopener">docker version</a></td><td>Show the Docker version information</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/volume/" target="_blank" rel="noopener">docker volume</a></td><td>Manage volumes</td></tr><tr><td><a href="https://docs.docker.com/engine/reference/commandline/wait/" target="_blank" rel="noopener">docker wait</a></td><td>Block until one or more containers stop, then print their exit codes</td></tr></tbody></table><blockquote><p>官方阅读链接：<a href="https://docs.docker.com/engine/reference/commandline/docker/" target="_blank" rel="noopener">docs.docker.com/engine/refe…</a></p></blockquote><h3 id="5-进阶：仓库与软件安装的简化"><a href="#5-进阶：仓库与软件安装的简化" class="headerlink" title="5. 进阶：仓库与软件安装的简化"></a>5. 进阶：仓库与软件安装的简化</h3><p>还记得笔者在文章开头介绍的「镜像、容器和仓库」吗？Docker 的仓库用于存放镜像。我们可以从中心仓库下载镜像，也可以从自建仓库下载。同时，我们可以把制作好的镜像从本地推送到远程仓库。</p><p>首先，笔者先引入一个知识点：Docker 的镜像就是它的文件系统，一个镜像可以放在另外一个镜像的上层，那么位于下层的就是它的父镜像。所以，Docker 会存在很多镜像层，每个镜像层都是只读的，并且不会改变。当我们创建一个新的容器时，Docker 会构建出一个镜像栈，并在栈的最顶层添加一个读写层，如图所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker16_mvf8sq.png" alt="docker16"></p><p>现在，我们可以通过 <code>docker images</code> 命令查看本地的镜像。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker images</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>查询结果，如图所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker17_i7ihcj.png" alt="docker17"></p><p>这里，对几个名词解释一下含义。</p><ul><li>REPOSITORY：仓库名称。</li><li>TAG： 镜像标签，其中 lastest 表示最新版本。注意的是，一个镜像可以有多个标签，那么我们就可以通过标签来管理有用的版本和功能标签。</li><li>IMAGE ID ：镜像唯一ID。</li><li>CREATED ：创建时间。</li><li>SIZE ：镜像大小。</li></ul><p>那么，如果第一次我们通过 <code>docker pull centos:latest</code> 拉取镜像，那么当我们执行 <code>docker run -p 80 --name web -i -t centos /bin/bash</code> 时，它就不会再去远程获取了，因为本机中已经安装该镜像，所以 Docker 会直接创建一个新的容器并启动该程序。</p><p>事实上，官方已经提供了安装好 Nginx 的镜像，我们可以直接使用。现在，我们通过拉取镜像的方式重新构建一个 Web 服务器。首先，我们通过 <code>docker search</code> 来查找镜像。我们获取到 Nginx 的镜像清单。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker search nginx</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>补充一下，我们也可以通过访问 Docker Hub (<a href="https://hub.docker.com/" target="_blank" rel="noopener">hub.docker.com/</a>)搜索仓库，那么 star 数越多，说明它越靠谱，可以放心使用。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602206868/Docker/docker18_wmcxk5.png" alt="docker18"></p><p>现在，我们通过 <code>docker pull nginx</code> 拉取最新的 Nginx 的镜像。当然，我们也可以通过 <code>docker pull nginx:latest</code> 来操作。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker pull nginx</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>然后，我们创建并运行一个容器。与前面不同的是，我们通过 <code>-d</code> 选项告诉 Docker 在后台运行容器的守护进程。并且，通过 <code>8080:80</code> 告诉 Docker 8080 端口是对外开放的端口，80 端口对外开放的端口映射到容器里的端口号。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker run -p 8080:80 -d --name nginx nginx</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>我们再通过 <code>docker ps -a</code> 来查看，发现容器已经后台运行了，并且后台执行了 nginx 命令，并对外开放 8080 端口。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207726/Docker/docker19_ftivwr.png" alt="docker19"></p><p>因此，通过浏览器访问 <code>http://127.0.0.1:8080</code> 即可。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207662/Docker/docker20_qm2lpe.png" alt="dcoker20"></p><h3 id="6-其他选择，使用替代注册服务器"><a href="#6-其他选择，使用替代注册服务器" class="headerlink" title="6. 其他选择，使用替代注册服务器"></a>6. 其他选择，使用替代注册服务器</h3><p>Docker Hub 不是软件的唯一来源，我们也可以切换到国内的其他替代注册服务器，例如阿里云。我们可以登录 <a href="https://cr.console.aliyun.com" target="_blank" rel="noopener">cr.console.aliyun.com</a> 搜索，并拉取公开的镜像。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207816/Docker/docker21_hjxrow.png" alt="docker21"></p><p>现在，我们输入 <code>docker pull</code> 命令进行拉取。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker pull registry.cn-hangzhou.aliyuncs.com&#x2F;qp_oraclejava&#x2F;orackejava:8u172_DCEVM_HOTSWAPAGEN_JCE</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>这里，笔者继续补充一个知识点：注册服务器的地址。事实上，注册服务器的地址是有一套规范的。完整格式是：[仓库主机/][用户名/]容器短名[:标签]。这里，仓库主机是 registry.cn-hangzhou.aliyuncs.com，用户名是 qp_oraclejava，容器短名是 orackejava，标签名是 8u172_DCEVM_HOTSWAPAGEN_JCE。事实上，我们上面通过 <code>docker pull centos:latest</code> 拉取镜像，相当于 <code>docker pull registry.hub.docker.com/centos:latest</code> 。</p><h2 id="三、构建我的镜像"><a href="#三、构建我的镜像" class="headerlink" title="三、构建我的镜像"></a>三、构建我的镜像</h2><p>通过上面的学习，笔者相信你已经对 Docker 使用有了一个大致的了解，就好比我们通过 VMware 安装了一个系统，并让它跑了起来，那么我们就可以在这个 Linux 系统（CentOS 或者 Ubuntu ） 上面工作我们想要的任何事情。事实上，我们还会经常把我们安装好的 VMware 系统进行快照备份并实现克隆来满足我们下次快速的复制。这里，Docker 也可以构建定制内容的 Docker 镜像，例如上面我们使用官方提供的安装好 Nginx 的 Docker 镜像。注意的是，我们通过基于已有的基础镜像，在上面添加镜像层的方式构建新镜像而已。</p><p>总结一下，Docker 提供自定义镜像的能力，它可以让我们保存对基础镜像的修改，并再次使用。那么，我们就可以把操作系统、运行环境、脚本和程序打包在一起，并在宿主机上对外提供服务。</p><p>Docker 构建镜像有两种方式，一种方式是使用 <code>docker commit</code> 命令，另外一种方式使用 <code>docker build</code> 命令和 <code>Dockerfile</code> 文件。其中，不推荐使用 <code>docker commit</code> 命令进行构建，因为它没有使得整个流程标准化，因此，在企业的中更加推荐使用 <code>docker build</code> 命令和 <code>Dockerfile</code> 文件来构建我们的镜像。我们使用<code>Dockerfile</code> 文件可以让构建镜像更具备可重复性，同时保证启动脚本和运行程序的标准化。</p><h3 id="1-构建第一个-Dockerfile-文件"><a href="#1-构建第一个-Dockerfile-文件" class="headerlink" title="1. 构建第一个 Dockerfile 文件"></a>1. 构建第一个 Dockerfile 文件</h3><p>现在，我们继续实战。这里，我们把一开始搭建的 Web 服务器构建一个镜像。首先，我们需要创建一个空的 Dokcerfile 文件。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mkdir dockerfile_test</span><br><span class="line">cd dockerfile_test&#x2F;</span><br><span class="line">touch Dockerfile</span><br><span class="line">nano Dockerfile</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>紧接着，我们需要编写一个 Dockerfile 文件，代码清单如下</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">FROM centos:7</span><br><span class="line">MAINTAINER LiangGzone &quot;lianggzone@163.com&quot;</span><br><span class="line">RUN rpm -ivh http:&#x2F;&#x2F;nginx.org&#x2F;packages&#x2F;centos&#x2F;7&#x2F;noarch&#x2F;RPMS&#x2F;nginx-release-centos-7-0.el7.ngx.noarch.rpm</span><br><span class="line">RUN yum install -y nginx</span><br><span class="line">EXPOSE 80</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>最后，我们通过 <code>docker build</code> 命令进行构建。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker build -t&#x3D;&quot;lianggzone&#x2F;nginx_demo:v1&quot; .</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>现在， 我们来通过 <code>docker images</code> 看下我们的新镜像吧。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207847/Docker/docker22_bfvqsi.png" alt="docker22"></p><h3 id="2-理解-Dockerfile-全过程"><a href="#2-理解-Dockerfile-全过程" class="headerlink" title="2. 理解 Dockerfile 全过程"></a>2. 理解 Dockerfile 全过程</h3><p>哇，我们通过编写一个 Dockerfile 文件顺利构建了一个新的镜像。这个过程简单得让人无法相信。现在，让我们来理解一下这个全过程吧。首先， <code>FROM centos:7</code> 是 Dockerfile 必须要的第一步，它会从一个已经存在的镜像运行一个容器，换句话说，Docker 需要依赖于一个基础镜像进行构建。这里，我们指定 centos 作为基础镜像，它的版本是 7 (CentOS 7)。然后，我们通过 <code>MAINTAINER LiangGzone &quot;lianggzone@163.com&quot;</code> 指定该镜像的作者是 LiangGzone，邮箱是 <a href="mailto:lianggzone@163.com">lianggzone@163.com</a>。这有助于告诉使用者它的作者和联系方式。接着，我们执行两个 RUN 指令进行 Nginx 的下载安装，最后通过 <code>EXPOSE 80</code> 暴露 Dokcer 容器的 80 端口。注意的是，Docker 的执行顺序是从上而下执行的，所以我们要明确整个流程的执行顺序。除此之外，Docker 在执行每个指令之后都会创建一个新的镜像层并且进行提交。</p><p>我们使用 <code>docker build</code> 命令进行构建，指定 <code>- t</code> 告诉 Docker 镜像的名称和版本。注意的是，如果没有指定任何标签，Docker 将会自动为镜像设置一个 lastest 标签。还有一点，我们最后还有一个 <code>.</code> 是为了让 Docker 到当前本地目录去寻找 Dockerfile 文件。注意的是，Docker 会在每一步构建都会将结果提交为镜像，然后将之前的镜像层看作缓存，因此我们重新构建类似的镜像层时会直接复用之前的镜像。如果我们需要跳过，可以使用 <code>--no-cache</code> 选项告诉 Docker 不进行缓存。</p><h3 id="3-Dockerfile-指令详解"><a href="#3-Dockerfile-指令详解" class="headerlink" title="3. Dockerfile 指令详解"></a>3. Dockerfile 指令详解</h3><p>Dockerfile 提供了非常多的指令。笔者这里特别整理了一份清单，建议收藏查看。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207891/Docker/docker23_wlxpzl.png" alt="docker23"></p><blockquote><p>官方地址：<a href="https://docs.docker.com/engine/reference/builder/#usage" target="_blank" rel="noopener">docs.docker.com/engine/refe…</a></p></blockquote><h4 id="指令辨别一：RUN、CMD、ENTRYPOINT"><a href="#指令辨别一：RUN、CMD、ENTRYPOINT" class="headerlink" title="指令辨别一：RUN、CMD、ENTRYPOINT"></a>指令辨别一：RUN、CMD、ENTRYPOINT</h4><p><code>RUN</code> 、 <code>CMD</code> 、 <code>ENTRYPOINT</code> 三个指令的用途非常相识，不同在于，<code>RUN</code> 指令是在容器被构建时运行的命令，而<code>CMD</code> 、 <code>ENTRYPOINT</code> 是启动容器时执行 shell 命令，而 <code>RUN</code> 会被 <code>docker run</code> 命令覆盖，但是 <code>ENTRYPOINT</code> 不会被覆盖。事实上，<code>docker run</code> 命令指定的任何参数都会被当作参数再次传递给 <code>ENTRYPOINT</code> 指令。<code>CMD</code> 、 <code>ENTRYPOINT</code> 两个指令之间也可以一起使用。例如，我们 可以使用 <code>ENTRYPOINT</code> 的 exec 形式设置固定的默认命令和参数，然后使用任一形式的 <code>CMD</code> 来设置可能更改的其他默认值。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">FROM ubuntu</span><br><span class="line">ENTRYPOINT [&quot;top&quot;, &quot;-b&quot;]</span><br><span class="line">CMD [&quot;-c&quot;]</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><h4 id="指令辨别二：ADD、COPY"><a href="#指令辨别二：ADD、COPY" class="headerlink" title="指令辨别二：ADD、COPY"></a>指令辨别二：ADD、COPY</h4><p><code>ADD</code> 、 <code>COPY</code> 指令用法一样，唯一不同的是 <code>ADD</code> 支持将归档文件（tar, gzip, bzip2, etc）做提取和解压操作。注意的是，<code>COPY</code> 指令需要复制的目录一定要放在 Dockerfile 文件的同级目录下。</p><h3 id="4-将镜像推送到远程仓库"><a href="#4-将镜像推送到远程仓库" class="headerlink" title="4. 将镜像推送到远程仓库"></a>4. 将镜像推送到远程仓库</h3><h4 id="远程仓库：Docker-Hub"><a href="#远程仓库：Docker-Hub" class="headerlink" title="远程仓库：Docker Hub"></a>远程仓库：Docker Hub</h4><p>镜像构建完毕之后，我们可以将它上传到 Docker Hub 上面。首先，我们需要通过 <code>docker login</code> 保证我们已经登录了。紧接着，我们使用 <code>docker push</code> 命令进行推送。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker push lianggzone&#x2F;nginx_demo:v1</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>这里，我们了解下它的使用，格式是 <code>docker push [OPTIONS] NAME[:TAG]</code> ，其中，笔者设置 NAME 是 lianggzone/nginx_demo，TAG 是 v1。 （笔者注：推送 Docker Hub 速度很慢，耐心等待) 最后，上传完成后访问：<a href="https://hub.docker.com/u/lianggzone" target="_blank" rel="noopener">hub.docker.com/u/lianggzon…</a>/，如图所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207937/Docker/docker24_ik3k18.png" alt="docker24"></p><h4 id="远程仓库：阿里云"><a href="#远程仓库：阿里云" class="headerlink" title="远程仓库：阿里云"></a>远程仓库：阿里云</h4><p>同时，我们也可以使用国内的仓库，比如阿里云。首先，在终端中输入访问凭证，登录 Registry 实例。如果你不知道是哪个，可以访问 <a href="https://cr.console.aliyun.com/cn-hangzhou/instances/credentials" target="_blank" rel="noopener">cr.console.aliyun.com/cn-hangzhou…</a>。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">docker login --username&#x3D;帐号 registry.cn-hangzhou.aliyuncs.com</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>现在，将镜像推送到阿里云镜像仓库。其中， <code>docker tag [IMAGE_ID] registry.cn-hangzhou.aliyuncs.com/[命名空间]/[镜像名称]:[版本]</code> 和 <code>docker push registry.cn-hangzhou.aliyuncs.com/[命名空间]/[镜像名称]:[版本]</code> 命令的使用如下所示。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">docker tag 794c07361565 registry.cn-hangzhou.aliyuncs.com&#x2F;lianggzone&#x2F;nginx_demo:v1</span><br><span class="line">docker push registry.cn-hangzhou.aliyuncs.com&#x2F;lianggzone&#x2F;nginx_demo:v1</span><br><span class="line">复制代码</span><br></pre></td></tr></table></figure><p>最后，上传完成后访问：<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/repositories" target="_blank" rel="noopener">cr.console.aliyun.com/cn-hangzhou…</a>，如图所示。</p><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1602207984/Docker/docker25_lsx27z.png" alt="docker25"></p><h3 id="5-Dockerfile-的-Github-源码地址"><a href="#5-Dockerfile-的-Github-源码地址" class="headerlink" title="5. Dockerfile 的 Github 源码地址"></a>5. Dockerfile 的 Github 源码地址</h3><p>这里，附上我整理的 Dockerfile 的仓库。后面，笔者会陆续更新用到的一些常用文件，欢迎 star 关注。</p><blockquote><p><a href="https://github.com/lianggzone/dockerfile-images" target="_blank" rel="noopener">github.com/lianggzone/…</a></p></blockquote><h2 id="附：参考资料"><a href="#附：参考资料" class="headerlink" title="附：参考资料"></a>附：参考资料</h2><ul><li>《Docker实战》</li><li>《第一本Docker书》</li><li><a href="https://docs.docker.com/engine/reference/commandline/cli/?spm=5176.8351553.0.0.6eb21991rT7ejF" target="_blank" rel="noopener">Docker 命令参考文档</a></li><li><a href="https://docs.docker.com/engine/reference/builder/?spm=5176.8351553.0.0.6eb21991rT7ejF" target="_blank" rel="noopener">Dockerfile 镜像构建参考文档</a></li></ul><blockquote><p>原文地址：<a href="http://blog.720ui.com/2019/30min_docker_course/?juejin" target="_blank" rel="noopener">梁桂钊的博客</a></p><p>博客地址：<a href="http://blog.720ui.com/2019/30min_docker_course/?juejin" target="_blank" rel="noopener">blog.720ui.com</a></p></blockquote><p>（完，转载请注明作者及出处。）</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Docker 是一个开放源代码软件项目，项目主要代码在2013年开源于 GitHub。它是云服务技术上的一次创新，让应用程序布署在软件容器下的工作可以自动化进行，借此在 Linux 操作系统上，提供一个额外的软件抽象层，以及操作系统层虚拟化的自动管理机制。&lt;/p&gt;
&lt;p&gt;Docker 利用 Linux 核心中的资源分脱机制，例如 cgroups，以及 Linux 核心名字空间（name space），来创建独立的软件容器（containers），属于操作系统层面的虚拟化技术。由于隔离的进程独立于宿主和其它的隔离的进程，因此也称其为容器。Docker 在容器的基础上进行了进一步的封装，从文件系统、网络互联到进程隔离等等，极大的简化了容器的创建和维护，使得其比虚拟机技术更为轻便、快捷。Docker 可以在单一 Linux 实体下运作，避免因为创建一个虚拟机而造成的额外负担。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="Docker" scheme="http://yoursite.com/tags/Docker/"/>
    
  </entry>
  
  <entry>
    <title>Long Polling vs WebSockets vs Server-Sent Events</title>
    <link href="http://yoursite.com/2020/09/29/Long-Polling-vs-WebSockets-vs-Server-Sent-Events/"/>
    <id>http://yoursite.com/2020/09/29/Long-Polling-vs-WebSockets-vs-Server-Sent-Events/</id>
    <published>2020-09-29T17:31:21.000Z</published>
    <updated>2020-09-29T17:32:42.615Z</updated>
    
    <content type="html"><![CDATA[<p>Web applications were originally designed as a simple client-server model where the web client initiates an HTTP request requesting some data from the server. For example, a basic web application with the client-server model flow will be as follows.</p><ol><li>A client makes an HTTP request requesting a web page from a server.</li><li>The server calculates the response</li><li>The server sends the response to the client</li></ol><blockquote><p>As developers began to explore ways to implement more “<strong>real-time</strong>” applications. The HTTP protocol made these sort of use cases very challenging, as a result, creative ways to manipulate HTTP request-response model into a more real-time model began to emerge.</p></blockquote><a id="more"></a><h1 id="Ajax-Polling"><a href="#Ajax-Polling" class="headerlink" title="Ajax Polling:"></a><strong>Ajax Polling:</strong></h1><p>In Ajax polling, a client makes XHR(XMLHttpRequest)/Ajax requests to server repeatedly at some regular interval to check for new data.</p><p>A flow for Ajax polling will as follow.</p><ol><li>A client initiates requests at a small regular intervals (e.g 0.5 Seconds)</li><li>The server prepares the response and sends it back to to the client just like normal HTTP requests.</li></ol><p>Making repeated requests to server wastes resources as each new incoming connection must be established, the HTTP headers must be passed, a query for new data must be performed, and a response (usually with no new data to offer) must be generated and delivered. The connection must be closed and any resources cleaned up.</p><h1 id="Long-Polling"><a href="#Long-Polling" class="headerlink" title="Long Polling:"></a><strong>Long Polling:</strong></h1><p>As in regular polling, rather than having to repeat this process multiple times for every client until new data for a given client becomes available, Long polling is technique where the server elects to hold a client connection open for as long as possible, delivering a response only after data becomes available or timeout threshold has been reached. After receiving response client immediately sends the next request.</p><p>On the client side, only a single request to the server needs to be managed. When the response is received, the client can initiate a new request, repeating this process as many times as necessary.</p><p><img src="https://miro.medium.com/max/60/1*m-FBYS8LqUwnUzniHzO8Xg.png?q=20" alt="Image for post"></p><p><img src="https://miro.medium.com/max/2760/1*m-FBYS8LqUwnUzniHzO8Xg.png" alt="Image for post"></p><p>Source: <a href="https://www.pubnub.com/blog/" target="_blank" rel="noopener">https://www.pubnub.com/blog/</a></p><p>A flow for Long polling will look as follows</p><ol><li>A client initiates an XHR/AJAX request, requesting some data from a server.</li><li>The server does not immediately respond with request information but waits until there is new information available.</li><li>When there is new information available, the server responds with new information.</li><li>The client receives the new information and immediately sends another request to the server restarting the process.</li></ol><p><strong><em>Some challenges in long polling\</em></strong></p><ul><li>Message ordering and delivery guarantees.</li></ul><blockquote><p>Message ordering cannot be guaranteed if the same client opens multiple connections to the server.</p><p>If the client was not able to receive the message then there will be possible message loss.</p></blockquote><ul><li>Performance and scaling</li><li>Device support and fallbacks</li></ul><h1 id="WebSockets"><a href="#WebSockets" class="headerlink" title="WebSockets:"></a><strong>WebSockets:</strong></h1><p>WebSocket is a <strong>computer communication protocol</strong> which provides <strong>full-duplex</strong> communication channels over a <strong>single TCP connection</strong>.</p><ul><li>It is different from HTTP but compatible with HTTP.</li><li>Located at layer 7 in the OSI model and depends on TCP at layer 4.</li><li>Works over port 80 and 443 ( in case of TLS encrypted) and supports HTTP proxies and intermediaries.</li><li>To achieve compatibility, the WebSocket handshake uses <em>Upgrade</em> header to update the protocol to the WebSocket protocol.</li></ul><p>The WebSocket protocol enables interaction between a client and a web server with lesser overheads, providing real-time data transfer from and to the server. WebSockets keeps the connection open, allowing messages to be passed back and forth between the client and the server. In this way, a two-way ongoing conversation can take place between the client and the server.</p><p><img src="https://miro.medium.com/max/60/1*0w3tMXm7jr174bqOprcdOg.png?q=20" alt="Image for post"></p><img src="https://miro.medium.com/max/1858/1*0w3tMXm7jr174bqOprcdOg.png" alt="Image for post" style="zoom:50%;" /><p>Source: <a href="https://www.pubnub.com/blog/" target="_blank" rel="noopener">https://www.pubnub.com/blog/</a></p><p>A WebSocket connection flow will look something like this.</p><ol><li>A client initiates a WebSocket handshake process by sending a request which also contains <em>Upgrade</em> header to switch to WebSocket protocol along with other information.</li><li>The server receives WebSocket handshake request and process it.</li></ol><p>2(a). If the server can establish the connection and agrees with client terms then sends a response to the client, acknowledging the WebSocket handshake request with other information.</p><p>2(b). If the server can not establish the connection then it sends response acknowledging it cannot establish WebSocket connection.</p><p>\3. Once the client receives a successful WebSocket connection handshake request, WebSocket connection will be opened. Now, client and servers can start sending data in both directions allowing real-time communication.</p><p>\4. The connection will be closed once the server or the client decides to close the connection.</p><h1 id="Server-Sent-Events"><a href="#Server-Sent-Events" class="headerlink" title="Server Sent Events:"></a><strong>Server Sent Events:</strong></h1><p>Unlike WebSockets, Server-Sent Events are a <strong>one-way communication channel</strong> where events flow from <strong>server to client only</strong>. Server-Sent Events allows browser clients to receive a stream of events from a server over an HTTP connection without polling.</p><p>A client subscribes to a “stream” from a server and the server will send messages (“event-stream”) to the client until the server or the client closes the stream. It is up to the server to decide when and what to send the client, for instance as soon as data changes.</p><p><img src="https://miro.medium.com/max/60/1*RWFAf6L6LYgd9kJaUxgmpQ.png?q=20" alt="Image for post"></p><p><img src="https://miro.medium.com/max/3200/1*RWFAf6L6LYgd9kJaUxgmpQ.png" alt="Image for post"></p><p>Source: <a href="https://medium.com/conectric-networks/a-look-at-server-sent-events-54a77f8d6ff7" target="_blank" rel="noopener">https://medium.com/conectric-networks/a-look-at-server-sent-events-54a77f8d6ff7</a></p><p>A flow for server send events will be as follows.</p><ol><li>Browser client creates a connection using an EventSource API with a server endpoint which is expected to return a stream of events over time. This essentially makes an HTTP request at given URL.</li><li>The server receives a regular HTTP request from the client and opens the connection and keeps it open. The server can now send the event data as long as it wants or it can close the connection if there are no data.</li><li>The client receives each event from the server and process it. If it receives a close signal from the server it can close the connection. The client can also initiate the connection close request.</li></ol><blockquote><p>As SSE is based on HTTP, it is more compliant with existing IT infrastructure like (Load Balancer, Firewall, etc), unlike WebSockets which can be blocked by some firewall. Server-Sent events are not supported by all browsers.</p></blockquote><h1 id="Acknowledgement"><a href="#Acknowledgement" class="headerlink" title="Acknowledgement"></a>Acknowledgement</h1><p>Author: Vivek Kumar Singh</p><p>original article: <a href="https://medium.com/system-design-blog/long-polling-vs-websockets-vs-server-sent-events-c43ba96df7c1" target="_blank" rel="noopener">https://medium.com/system-design-blog/long-polling-vs-websockets-vs-server-sent-events-c43ba96df7c1</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Web applications were originally designed as a simple client-server model where the web client initiates an HTTP request requesting some data from the server. For example, a basic web application with the client-server model flow will be as follows.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;A client makes an HTTP request requesting a web page from a server.&lt;/li&gt;
&lt;li&gt;The server calculates the response&lt;/li&gt;
&lt;li&gt;The server sends the response to the client&lt;/li&gt;
&lt;/ol&gt;
&lt;blockquote&gt;
&lt;p&gt;As developers began to explore ways to implement more “&lt;strong&gt;real-time&lt;/strong&gt;” applications. The HTTP protocol made these sort of use cases very challenging, as a result, creative ways to manipulate HTTP request-response model into a more real-time model began to emerge.&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Long Polling - Concepts and Considerations</title>
    <link href="http://yoursite.com/2020/09/29/Long-Polling-Concepts-and-Considerations/"/>
    <id>http://yoursite.com/2020/09/29/Long-Polling-Concepts-and-Considerations/</id>
    <published>2020-09-29T17:29:03.000Z</published>
    <updated>2020-09-29T17:30:47.972Z</updated>
    
    <content type="html"><![CDATA[<p>In this article, we’ll provide an insight into the technique known as “long polling”, how it came to be, how it’s implemented, how you might use it in your own applications, and how we use it at Ably.</p><a id="more"></a><h2 id="A-brief-history-–-how-did-long-polling-come-about"><a href="#A-brief-history-–-how-did-long-polling-come-about" class="headerlink" title="A brief history – how did long polling come about?"></a>A brief history – how did long polling come about?</h2><p>In the early days of the web, <a href="https://en.wikipedia.org/wiki/Hypertext_Transfer_Protocol" target="_blank" rel="noopener">HTTP</a> made sense as a simple request-response protocol because it was designed as a way to distribute structured documents to interested readers, each document generally having links to others that could be browsed at the reader’s leisure. A request-response model was the right strategy because a browser needed only make a request for the resource that the user wished to consume, and the job was done.</p><p>The technology of the time was vastly more limited than that of today, and holding a connection open would not only have been a significant issue for web server load management, but would have had little-to-no value anyway in relation to the goals and use cases that the early web was designed to solve. Early browsers such as Mosaic and the first version of Netscape Navigator were implemented with this in mind and had no support for JavaScript or other features that might position the web as anything more than a distributed resource-sharing network.</p><p>In 1995, <a href="https://en.wikipedia.org/wiki/Netscape" target="_blank" rel="noopener">Netscape Communications</a> hired Brendan Eich to implement scripting capabilities in Netscape Navigator, and over a ten-day period, the JavaScript language was born. Its capabilities as a language were initially very limited compared to modern-day JavaScript, and its ability to interact with the browser’s document object model (DOM) was even more limited. JavaScript was mostly useful for providing limited enhancements to enrich document consumption capabilities, examples being in-browser form validation and lightweight insertion of dynamic HTML into an existing document.</p><p>As the <a href="https://en.wikipedia.org/wiki/Browser_wars" target="_blank" rel="noopener">browser wars</a> heated up, with Microsoft’s Internet Explorer reaching version 4 and beyond, the battle for the most robust feature set led to Microsoft’s introduction of what ultimately became the <a href="https://xhr.spec.whatwg.org/" target="_blank" rel="noopener">XMLHttpRequest</a> object which all browsers have now universally supported for well over a decade.</p><p><img src="https://images.ctfassets.net/ee3ypdtck0rk/5Wrkjt1K4mc9B0EUsSW1GS/61ce12ea8b81913c1574f3216a10f0a1/browser-wars.png" alt="browser-wars"></p><p><code>XMLHttpRequest</code> could be thought of as a black swan event for the web, because it opened up the potential for web developers to start building truly-dynamic web applications that could communicate with the server silently in the background, without interrupting the user’s browsing experience. Because browser DOM capabilities had also been expanding significantly, particularly as Internet Explorer approached version 6, dynamic web “applications” started to become commonplace, offering everything from rich web-based email interfaces to in-browser website content management systems.</p><p>Progress begets progress, as they say, and naturally, developers began to explore ways to implement applications having more of a “realtime” aspect to their function. Web-based chat rooms and simple games were early examples at the time. The HTTP protocol made these sorts of use cases very challenging to implement though, and it was common to see applications polling servers repeatedly to check for new data, seeing as servers had no way to proactively notify a user whenever new data became available. Given the extreme inefficiency of this approach, creative ways to manipulate the HTTP request-response model into more of a real-time medium began to emerge. Among these techniques, probably the most popular to emerge was <strong>long polling</strong>.</p><h2 id="How-does-long-polling-work"><a href="#How-does-long-polling-work" class="headerlink" title="How does long polling work?"></a>How does long polling work?</h2><p><a href="https://en.wikipedia.org/wiki/Push_technology#Long_polling" target="_blank" rel="noopener">Long polling</a> is essentially a more efficient form of the original polling technique. Making repeated requests to a server wastes resources, as each new incoming connection must be established, the HTTP headers must be parsed, a query for new data must be performed, and a response (usually with no new data to offer) must be generated and delivered. The connection must then be closed, and any resources cleaned up. Rather than having to repeat this process multiple times for every client until new data for a given client becomes available, long polling is a technique where the server elects to hold a client’s connection open for as long as possible, delivering a response only after data becomes available or a timeout threshold has been reached.</p><p>Implementation is mostly a server-side concern. On the client side, only a single request to the server needs to be managed. When the response is received, the client can initiate a new request, repeating this process as many times as is necessary. The only difference to basic polling, as far as the client is concerned, is that a client performing basic polling may deliberately leave a small time window between each request so as to reduce its load on the server, and it may respond to timeouts with different assumptions than it would for a server that does not support long polling. With long polling, the client may be configured to allow for a longer timeout period (via a <code>Keep-Alive</code> header) when listening for a response – something that would usually be avoided seeing as the timeout period is generally used to indicate problems communicating with the server.</p><p><img src="https://images.ctfassets.net/ee3ypdtck0rk/1UuClqOdPD71XXPcFC8th1/67a75ff8542382ade6fd809671d96b44/long-polling-f6e3a73a589fe25d7c7b622a8487a2e8a27a11f00b22b574abb021fbcd7ac2db.png" alt="long-polling"></p><p>Apart from these concerns, there is little else that a client needs to do that would be different than if it were engaging in basic polling. By contrast, the server needs to manage the unresolved state of multiple connections, and it may need to implement strategies for preserving session state when multiple servers and load balancers are in use (commonly referred to as session “stickiness”). It also needs to gracefully handle connection timeout issues, which are much more likely to occur than with designed-for-purpose protocols such as <a href="https://www.ably.io/concepts/websockets" target="_blank" rel="noopener">WebSockets</a>, a standard which did not arrive until years after long polling was established as a conventional technique for pseudo-realtime communication.</p><h2 id="Considerations-when-using-long-polling"><a href="#Considerations-when-using-long-polling" class="headerlink" title="Considerations when using long polling"></a>Considerations when using long polling</h2><p>As long polling is really just an improvisation applied to an underlying request-response mechanism, it comes with an additional degree of complexity in its implementation. There are various concerns you’ll need to account for when considering the design of your system’s architecture.</p><h3 id="Message-ordering-and-delivery-guarantees"><a href="#Message-ordering-and-delivery-guarantees" class="headerlink" title="Message ordering and delivery guarantees"></a>Message ordering and delivery guarantees</h3><p>Reliable message ordering can be an issue with long polling because it’s possible for multiple HTTP requests from the same client to be in flight simultaneously. For example, if a client has two browser tabs open consuming the same server resource, and the client-side application is persisting data to a local store such as <code>localStorage</code> or <code>IndexedDb</code>, there is no in-built guarantee that duplicate data won’t be written more than once. This could also happen if the client implementation uses more than one connection at a time, whether deliberately or as a result of a bug in the code.</p><p>Another issue is that a server may send a response, but network or browser issues may prevent the message from being successfully received. Unless some sort of message receipt confirmation process is implemented, a subsequent call to the server may result in missed messages.</p><p>Depending on the server implementation, confirmation of message receipt by one client instance may also cause another client instance to never receive an expected message at all, as the server could mistakenly believe that the client has already received the data it is expecting.</p><p>All of these concerns, and more need to be considered when implementing robust support for long polling in any realtime messaging system.</p><h3 id="Performance-and-scaling"><a href="#Performance-and-scaling" class="headerlink" title="Performance and scaling"></a>Performance and scaling</h3><p>Unfortunately, such complexity is difficult to scale effectively. To maintain the session state for a given client, that state must either be shareable among all servers behind a load balancer – a task with significant architectural complexity – or subsequent client requests within the same session must be routed to the same server to which their original request was processed. This form of deterministic “sticky” routing is problematic by design, especially when routing is performed on the basis of IP address, as it can place undue load on a single server in a cluster while leaving other servers mostly idle instead of spreading the load around efficiently. This can also become a potential denial-of-service attack vector – a problem which then requires further layers of infrastructure to mitigate that might otherwise have been unnecessary.</p><h3 id="Device-support-and-fallbacks"><a href="#Device-support-and-fallbacks" class="headerlink" title="Device support and fallbacks"></a>Device support and fallbacks</h3><p>In modern times (2018 at the time of this article), long polling can be less relevant for web application development given the widespread availability of real-time communication standards such as WebSockets and WebRTC. That said, there are cases where proxies and routers on certain networks will block WebSocket and WebRTC connections, or where network connectivity can make long-lived connection protocols such as these less practical. Besides, for certain client demographics, there may still be numerous devices and clients in use that lack support for newer standards. For these, long polling can serve as a good fail-safe fallback to ensure support for everyone, irrespective of their situation.</p><p>Given that long polling is implemented on the back of <code>XMLHttpRequest</code>, which is near universal in its support by devices that still have a non-negligible degree of remaining relevant on the modern web, there’s usually not much of a need to support further fallback layers. In cases where exceptions must be handled though, or where a server can be queried for new data but does not support long polling (let alone other more modern technology standards), basic polling can sometimes still be of limited use, and can be implemented using <code>XMLHttpRequest</code>, or via JSONP through simple HTML script tags.</p><p>Long polling can be implemented via JSONP if absolutely necessary. That said, given the time and effort – not to mention the inefficiency of resource consumption – involved in implementing these approaches, care should be taken to assess whether their support is worth the added cost when developing new applications and system architectures. In many cases, you may be able to get away with exclusive support for something more modern, such as <a href="https://www.ably.io/concepts/websockets" target="_blank" rel="noopener">WebSockets</a>. Alternatively, you may wish to offload the management of these sorts of concerns to a specialist cloud provider such as <a href="https://www.ably.io/" target="_blank" rel="noopener">Ably</a>.</p><h2 id="Open-source-long-polling-solutions"><a href="#Open-source-long-polling-solutions" class="headerlink" title="Open source long polling solutions"></a>Open source long polling solutions</h2><p>Most libraries don’t implement long polling in isolation from other transports because, in general, long polling is usually accompanied with other transport strategies, either as a fallback, or with those transports as fallbacks when long polling doesn’t work. In 2018 and beyond, standalone long polling libraries are particularly uncommon, given that it’s a technique that is quickly losing relevance in the face of widespread support for more modern alternatives.</p><p>Nevertheless, below are a handful of options for a few different languages:</p><h3 id="Go"><a href="#Go" class="headerlink" title="Go"></a>Go</h3><p>Golongpoll is a decent library for implementing long polling in the Go language:</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">import  &quot;github.com&#x2F;jcuga&#x2F;golongpoll&quot;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; This launches a goroutine and creates channels for all the plumbing</span><br><span class="line">manager, err :&#x3D; golongpoll.StartLongpoll(golongpoll.Options&#123;&#125;)  &#x2F;&#x2F; default options</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; Pass the manager around or create closures and publish:</span><br><span class="line">manager.Publish(&quot;subscription-category&quot;, &quot;Some data.  Can be string or any obj convertible to JSON&quot;)</span><br><span class="line">manager.Publish(&quot;different-category&quot;, &quot;More data&quot;)</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; Expose events to browsers</span><br><span class="line">&#x2F;&#x2F; See subsection on how to interact with the subscription handler</span><br><span class="line">http.HandleFunc(&quot;&#x2F;events&quot;, manager.SubscriptionHandler)</span><br><span class="line">http.ListenAndServe(&quot;127.0.0.1:8081&quot;, nil)</span><br></pre></td></tr></table></figure><p>You can find out more about this library from the original <a href="https://github.com/jcuga/golongpoll" target="_blank" rel="noopener">GitHub repo</a>.</p><h3 id="PHP"><a href="#PHP" class="headerlink" title="PHP"></a>PHP</h3><p>As per its <a href="https://github.com/panique/php-long-polling/blob/master/README.md" target="_blank" rel="noopener">README</a>, php-long-polling is</p><blockquote><p><em>“a very simple demonstration of long-polling with AJAX (jQuery) and PHP […] This is an improved, cleaned and documented fork of</em> <a href="https://github.com/lincolnbrito/php-ajax-long-polling" target="_blank" rel="noopener"><em>php-ajax-long-polling</em></a><em>“</em></p></blockquote><p>As the code for this library is extremely simple you can take a look at the <a href="https://github.com/panique/php-long-polling/blob/master/server/server.php" target="_blank" rel="noopener">implementation</a> directly – it’s fairly self-explanatory.</p><p>You can find out more about this library from the original <a href="https://github.com/panique/php-long-polling" target="_blank" rel="noopener">GitHub repo</a>.</p><h3 id="Node-js"><a href="#Node-js" class="headerlink" title="Node.js"></a>Node.js</h3><p>From its <a href="https://github.com/fanout/pollymer/blob/master/README.md" target="_blank" rel="noopener">README</a>, we find out that</p><blockquote><p>*”Pollymer is a general-purpose AJAX library that provides conveniences for long-polling applications, such as request retries, exponential backoff between requests, randomized request delaying, and workarounds for browser ‘busy’ indications. It also implements multiple transports to ensure cross-domain access works in all major browsers”.*</p></blockquote><p>Optional extras include support for JSON-P and logging.</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">var req &#x3D; new Pollymer.Request();</span><br><span class="line">req.on(&#39;finished&#39;, function(code, result, headers) &#123; ... &#125;);</span><br><span class="line">req.on(&#39;error&#39;, function(reason) &#123; ... &#125;);</span><br><span class="line">var headers &#x3D; &#123; ... &#125;;</span><br><span class="line">var body &#x3D; &#39;some data&#39;;</span><br><span class="line">req.maxTries &#x3D; 2; &#x2F;&#x2F; try twice</span><br><span class="line">req.start(&#39;POST&#39;, &#39;http:&#x2F;&#x2F;example.com&#x2F;path&#39;, headers, body);</span><br></pre></td></tr></table></figure><p>You can find out more about this library from the original <a href="https://github.com/fanout/pollymer" target="_blank" rel="noopener">GitHub repo</a>.</p><h3 id="Python"><a href="#Python" class="headerlink" title="Python"></a>Python</h3><p>Simple-Comet-Server is a minimalist HTTP long-polling server for the Python language. It supports multiple channels and cross-domain requests. It requires Python &gt;= 2.5 and the <a href="https://twistedmatrix.com/trac/" target="_blank" rel="noopener">Twisted framework</a>. Unlike the other libraries mentioned above, this server runs as a kind of broker that sits between the client and the server-side applications. When the server wishes to publish messages to a client, it can make a request to an HTTP endpoint that can be easily hidden behind a firewall, ensuring that is only accessible by server-side processes.</p><p>The <a href="https://github.com/jedisct1/Simple-Comet-Server/blob/master/README.markdown" target="_blank" rel="noopener">README </a>provides examples for:</p><ul><li>Registering a channel</li><li>Pushing data to a channel</li><li>Registering a client</li><li>Monitoring the server</li><li>Reading data from channels</li></ul><p>You can find out more about this library from the original <a href="https://github.com/jedisct1/Simple-Comet-Server" target="_blank" rel="noopener">GitHub repo</a>.</p><h2 id="Long-polling-and-Ably"><a href="#Long-polling-and-Ably" class="headerlink" title="Long polling and Ably"></a>Long polling and Ably</h2><p>Ably is an enterprise-ready <a href="https://ably.com/channels" target="_blank" rel="noopener">pub/sub messaging platform</a>. We make it easy to efficiently design, quickly ship, and seamlessly scale critical realtime functionality delivered directly to end-users. Everyday we deliver billions of realtime messages to millions of users for thousands of companies.</p><p>We power the apps people, organizations, and enterprises depend on everyday like <strong>Lightspeed System’s</strong> realtime device management platform for over seven million school-owned devices, <strong>Vitac’s</strong> live captioning for 100s of millions of multilingual viewers for events like the Olympic Games, and <strong>Split’s</strong> realtime feature flagging for one trillion feature flags per month.</p><p>We’re the only pub/sub platform with a <a href="https://ably.com/platform" target="_blank" rel="noopener">suite of baked-in services</a> to build complete realtime functionality: <a href="https://ably.com/documentation/realtime/presence" target="_blank" rel="noopener">presence</a> shows a driver’s live GPS location on a home-delivery app, <a href="https://www.ably.io/documentation/core-features/history" target="_blank" rel="noopener">history</a> instantly loads the most recent score when opening a sports app, <a href="https://www.ably.io/documentation/realtime/connection#connection-state-recovery" target="_blank" rel="noopener">stream resume</a> automatically handles reconnection when swapping networks, and our <a href="https://ably.com/reactor" target="_blank" rel="noopener">integrations</a> extend Ably into third-party clouds and systems like AWS Kinesis and RabbitMQ. With 25+ SDKs we target every major platform across web, mobile, and IoT.</p><p>Our platform is mathematically modelled around Four Pillars of Dependability so we’re able to ensure messages don’t get lost while still being delivered at low latency over a <a href="https://ably.com/security-and-compliance" target="_blank" rel="noopener">secure</a>, reliable, and <a href="https://www.ably.io/uptime" target="_blank" rel="noopener">highly available</a> <a href="https://ably.com/network" target="_blank" rel="noopener">global edge network</a>.</p><p>Developers from startups to industrial giants choose to build on Ably because they simplify engineering, minimize DevOps overhead, and increase development velocity.</p><p>As part of our offering, we’ve implemented a robust suite of transports in the <a href="https://www.ably.io/download" target="_blank" rel="noopener">client libraries</a> that we make available for communication with our servers, with WebSockets being the primary protocol for realtime communications. However, long polling is fully-supported as an automatic fallback transport when newer standards such as WebSockets are not available on the client-side.</p><h2 id="References-and-further-reading"><a href="#References-and-further-reading" class="headerlink" title="References and further reading"></a>References and further reading</h2><ul><li><a href="https://www.ably.io/concepts/websockets" target="_blank" rel="noopener">WebSockets – A Conceptual Deep-Dive</a></li><li><a href="https://www.ably.io/blog/tag/ably-engineering" target="_blank" rel="noopener">Ably Engineering blog</a>, which covers a wide spectrum of engineering challenges related to building a globally-distributed serverless realtime infrastructure at scale</li><li><a href="https://tools.ietf.org/id/draft-loreto-http-bidirectional-07.html" target="_blank" rel="noopener">IETF: Known Issues and Best Practices for the Use of Long Polling and Streaming in Bidirectional HTTP</a></li><li><a href="https://blog.baasil.io/why-you-shouldnt-use-long-polling-fallbacks-for-websockets-c1fff32a064a" target="_blank" rel="noopener">The Myth of Long Polling</a></li><li><a href="http://websocket.org/quantum.html" target="_blank" rel="noopener">HTML5 WebSocket: A Quantum Leap in Scalability for the Web</a></li><li><a href="https://en.wikipedia.org/wiki/Push_technology#Long_polling" target="_blank" rel="noopener">Wikipedia: Push Technology</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;In this article, we’ll provide an insight into the technique known as “long polling”, how it came to be, how it’s implemented, how you might use it in your own applications, and how we use it at Ably.&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Understand Apache Cassandra Architecture part3</title>
    <link href="http://yoursite.com/2020/09/29/Understand-Apache-Cassandra-Architecture-part3/"/>
    <id>http://yoursite.com/2020/09/29/Understand-Apache-Cassandra-Architecture-part3/</id>
    <published>2020-09-29T17:25:26.000Z</published>
    <updated>2020-09-29T17:27:15.616Z</updated>
    
    <content type="html"><![CDATA[<p>Table of contents</p><ol><li>Architecture</li><li>Client requests</li><li><strong>Read/Write procedure</strong></li><li><strong>Acknowledgement</strong> </li></ol><a id="more"></a><h3 id="Read-Write-procedure"><a href="#Read-Write-procedure" class="headerlink" title="Read/Write procedure"></a>Read/Write procedure</h3><h4 id="Write-Process"><a href="#Write-Process" class="headerlink" title="Write Process"></a>Write Process</h4><ul><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601396657/Cassandra/Cassandra3_cpm821.png" alt="Cassandra-img-3"><ul><li>The above graph shows the write request to the memtable and Commit log, and the data in memtable will be written to disk. Besides the written data, the index will also be written on disk.</li></ul></li><li>First write the data into the memtable data structure in the memory, and append it to the commitlog in the disk at the same time.</li><li>After the memtable content exceeds the specified capacity, it will be placed in the queue that will be flushed to the disk (memtable_flush_queue_size configures the queue length).</li><li>If the data to be flushed to the disk exceeds the queue length, C will lock the write and flush the memory data into the SSTable in the disk, and then the commit log will be cleared.</li></ul><h4 id="Read-Process"><a href="#Read-Process" class="headerlink" title="Read Process"></a>Read Process</h4><ul><li><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601396657/Cassandra/Cassandra4_zyu9gk.png" alt="Cassandra-img-3"></p><ul><li><p>First check BloomFilter①, each SSTable has a Bloomfilter to check whether the data corresponding to the requested PK exists in the SSTable before any disk IO</p></li><li><p>BF may be misjudged but not missed: the judgment exists, but it may not actually exist. If the judgment does not exist, it must not exist, and the process will not access this SSTable (red).</p></li><li><p>If the data is likely to exist ②, check the PartitionKey cache (index cache), and then perform different steps according to whether the index entry is found in the cache:</p><ul><li><p><strong>Found in index cache</strong></p><p>③ Search the corresponding data from the compression offset map.<br>④ Take out the compressed data from the disk and return the result set.</p></li><li><p><strong>Not in index cache</strong></p><p>⑤ Search Partition summary (partition index sample set) to determine the approximate position of the index entry on the disk.<br>⑥ Get the index entry from the SSTable on the disk.<br>⑦ Find the compression speed with corresponding data from the compression offset map.<br>Get the compressed data from the disk and return the result set.</p></li><li><p>E.g.</p><ul><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601396658/Cassandra/Cassandra5_nbjliv.png" alt="Cassandra-img-3"><ul><li>The first SSTable file is insert (left), and the second SSTable is update data (right).</li></ul></li><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601396657/Cassandra/Cassandra6_bwb00i.png" alt="Cassandra-img-3"><ul><li>It can be seen from the insert/update process that after the read request reaches a certain stage, it must combine all the SSTables and memtables containing the columns in the requested row to generate the requested data.</li></ul></li><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601396657/Cassandra/Cassandra7_gxldav.png" alt="Cassandra-img-3"><ul><li>For example, to update the email column in a row containing user data, Cassandra does not rewrite the entire row to a new data file.</li><li>It just write the new email into the new data file, the username and so on are still in the old data file.</li><li>The red line in the above figure represents the row fragments that Cassandra needs to integrate to generate the results requested by the user.</li><li>In order to save CPU and disk I/O, Cassandra caches the merged result, and can directly update the row in the cache without re-merging</li></ul></li></ul></li></ul></li></ul></li></ul><h3 id="Acknowledgement"><a href="#Acknowledgement" class="headerlink" title="Acknowledgement"></a>Acknowledgement</h3><p>The seires is translated from <a href="https://zqhxuyuan.github.io/2015/08/25/2015-08-25-Cassandra-Architecture/#%E8%AF%BB%E8%AF%B7%E6%B1%82" target="_blank" rel="noopener">Apache Cassandra架构理解</a> with some notes and insights. Thanks to the author @<a href="https://zqhxuyuan.github.io/" target="_blank" rel="noopener">任何忧伤,都抵不过世界的美丽</a>.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Table of contents&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Architecture&lt;/li&gt;
&lt;li&gt;Client requests&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Read/Write procedure&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Acknowledgement&lt;/strong&gt; &lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
      <category term="Database" scheme="http://yoursite.com/tags/Database/"/>
    
      <category term="Cassandra" scheme="http://yoursite.com/tags/Cassandra/"/>
    
  </entry>
  
  <entry>
    <title>Understand Apache Cassandra Architecture part2</title>
    <link href="http://yoursite.com/2020/09/29/Understand-Apache-Cassandra-Architecture-part2/"/>
    <id>http://yoursite.com/2020/09/29/Understand-Apache-Cassandra-Architecture-part2/</id>
    <published>2020-09-29T17:24:39.000Z</published>
    <updated>2020-09-29T17:27:01.210Z</updated>
    
    <content type="html"><![CDATA[<p>Table of contents</p><ol><li>Architecture</li><li><strong>Client requests</strong></li><li>Read/Write procedure</li><li>Acknowledgement </li></ol><a id="more"></a><h3 id="Client-Requests"><a href="#Client-Requests" class="headerlink" title="Client Requests"></a>Client Requests</h3><h4 id="Write-Requestcd-b"><a href="#Write-Requestcd-b" class="headerlink" title="Write Requestcd b"></a>Write Requestcd b</h4><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601397026/Cassandra/Cassandra8_srrgxv.png" alt="cassandra-img8"></p><ul><li>The coordinator will send the write requests to all the replica with the corresponding row, only if the replica nodes are available, it executes the write requests.</li><li>Write a consistency level decide how many replicas need to return a successful message. (here, success means the data has been correctly written to commit log and memtable).</li><li>The above example is a single-node data center, 11 nodes, replication factor is 3, write consistency level  is ONE. (The red arrow representing if there is one node successful write, it can respond to the client)</li></ul><h4 id="Read-Request"><a href="#Read-Request" class="headerlink" title="Read Request"></a>Read Request</h4><ul><li><p>Direct read</p></li><li><p>Read repair</p></li><li><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601397026/Cassandra/Cassandra9_wrdc6a.png" alt="cassandra-img8"></p><ul><li><p>The number of replicas associated with the <strong><u>direct read</u></strong> request [1] is determined by the consistency level (two nodes R1 and R3 are requested in the figure above).<br>  The <u>background</u> <strong>read repair request</strong> [2] is sent to the replica (R2) that has not received the direct read request to ensure that the requested row is <u>consistent</u> on all replicas.</p><ul><li><p>The coordinator first contacts all replicas with clear consistency level, and those nodes return the requested data.</p></li><li><p>If multiple nodes are contacted, the rows from each replica will be compared in memory. If they are inconsistent, the coordinator will use the replica with the latest data to return the result to the client.</p></li><li><p>The coordinator contacts and compares the data from the remaining replicas with the corresponding row in the background. If they are inconsistent, they will send a write request to the outdated replica to update with the latest data: read repair.</p></li><li><p>The above example is a read situation with a single data center, 11 nodes, a replication factor of 3, and a consistency level of QUORUM (3/2+1=2) (two nodes are requested, but the latest data is returned to the client in the end )</p></li><li><blockquote><p>Q: How does the read request ensure data consistency? </p><p>A: The direct read request sends the query request to the node (1, 7) where the two copies are located. Because there are two copies, it will compare which of the two copies is the latest.<br>Q: Is the comparison operation on the coordinating node or on each replica node? </p><p>A: Of course, it should be on the coordinating node, because there is no way to know the copies of other nodes on each replica node.<br>Q: So does the comparison operation send the data of these two copies to the coordinating node? </p><p>A: No, it only needs to pass the timestamp, because the only thing to compare is which copy data is the latest.<br>Q: How to judge that the data of the two copies are inconsistent? </p><p>A: In fact, the md5 judgment value is different, indicating that the data of the two copies are different.<br>Because it is not necessary to transmit all the query results of the two copies to the coordinating node when comparing, after determining which is the latest, that copy needs to transmit the query results to the coordinating node. Then the coordinating node returns the data to the client. That is, the red part in the figure is the flow of the result data. The black round-trip arrow is only the transfer timestamp to compare which is the latest data.</p></blockquote></li></ul></li></ul></li></ul><h4 id="Coordination-Nodes"><a href="#Coordination-Nodes" class="headerlink" title="Coordination Nodes"></a>Coordination Nodes</h4><ul><li>How is the node connected by the client specified? Is it the node specified by addContactPoint? But ContactPoint is generally set as one of the seed nodes.<ul><li>If the CQL client connects to the C cluster, the node to which the CQL connects is the coordinating node. But if you use Driver API, the specified ContactPoint is not a coordination node!</li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Table of contents&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Architecture&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Client requests&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;Read/Write procedure&lt;/li&gt;
&lt;li&gt;Acknowledgement &lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
      <category term="Database" scheme="http://yoursite.com/tags/Database/"/>
    
      <category term="Cassandra" scheme="http://yoursite.com/tags/Cassandra/"/>
    
  </entry>
  
  <entry>
    <title>Understand Apache Cassandra Architecture Part1</title>
    <link href="http://yoursite.com/2020/09/25/Understand-Apache-Cassandra-Architecture-Part1/"/>
    <id>http://yoursite.com/2020/09/25/Understand-Apache-Cassandra-Architecture-Part1/</id>
    <published>2020-09-25T22:45:22.000Z</published>
    <updated>2020-09-29T17:26:47.525Z</updated>
    
    <content type="html"><![CDATA[<p>Table of contents</p><ol><li>Architecture</li><li>Client request</li><li>Read/Write procedure</li><li>Acknowledgement </li></ol><a id="more"></a><h3 id="Architecture"><a href="#Architecture" class="headerlink" title="Architecture"></a>Architecture</h3><ul><li><p>Basic: peer to peer distributed system, every node in cluster is equal, data is evenly distributed on every node and exchange information every seconds</p><ul><li>The <strong><u>commit log</u></strong> capture <u>writes</u> to ensure data <strong>durability</strong>.</li><li>Data is written to <strong>memtable</strong> first, after it’e full, the data will be written to <strong>SSTable</strong>.</li><li>All the writting contents will be automatically partitioned and replicated.</li></ul></li><li><p>Table structure: Cassandra is <u>objected to row</u>. Users can connect to any nodes and query data using SQL or similar CQL.</p><ul><li>In cluster, each application constains one keyspace, and each keysapce contains multiple tables.</li></ul></li><li><p>Read/Write request: when one client connect to a node to read or write, the node becomes the <strong><u>coordinator</u></strong> bewteen client and node containing the data, and decide which node should   the request forward to based on the cluster configuration.</p><ul><li>CQL is a kind of client, Driver also can be client. node designited by <code>-h</code> using CQL is the coordinator.</li><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1600985440/Cassandra/cassandra1_ue8oxm.png" alt="cassandra1"><ul><li>explanation: coordinator(10) is responsible for communicate with client, while the true data nodes are node (1, 4, 6) representing three replicas seperately. And finally the data on node 1 will be returned.</li></ul></li></ul></li><li><table><thead><tr><th align="center">KeyWord</th><th align="left">Explain</th></tr></thead><tbody><tr><td align="center"><code>Gossip</code></td><td align="left">Peer to the peer communication protocol, used for exchange location and status information between nodes in the Cassandra cluster.</td></tr><tr><td align="center"><code>Partitioner</code></td><td align="left">It decides how to distribute data among nodes( the first replica to be stored which node)</td></tr><tr><td align="center"><code>Replica placement strategy</code></td><td align="left">Decide which nodes store the other replicas of each row of data.  Multiple replicas are stored in more that one node in the cluster to ensure reliability and fault tolerance.</td></tr><tr><td align="center"><code>Snitch</code></td><td align="left">Defines the replication placement strategy and topology of routing requests.</td></tr><tr><td align="center"><code>Virtual nodes</code></td><td align="left">Defines the relationship between physical nodes and data.</td></tr><tr><td align="center"><code>Token Ring</code></td><td align="left">*Token Ring is a LAN protocol, defined in IEEE 802.5, in which all workstations are connected to a ring, and each workstation can only transmit data to the directly adjacent workstation. The transmission authority is granted to the workstation through the token information surrounding the ring.</td></tr></tbody></table></li></ul><h4 id="Gossip"><a href="#Gossip" class="headerlink" title="Gossip"></a>Gossip</h4><ul><li>Cassandra uses peer-to-peer protocol <strong><u>gossip</u></strong> to exchange <strong>location</strong> and <strong>status</strong> information in a cluster among nodes.</li><li>Gossip runs every second and exchanges information with at most three nodes, which all the nodes can know others information.<ul><li><strong><u>Seeds</u></strong> in configuration files: note: All the nodes in one cluster should be consistent. If they are not, “<u>split brain</u>“ may happen, where there will be two clusters. Normally, the seed node starts first and discovers other nodes in the cluster in the early stage.</li><li>Each node exchanges information with other nodes. Due to randomness and possibility, all nodes in the cluster will be listed. At the same time, each node will save the information of all other nodes in the cluster.<br>  This way, if you connect to any node, you can know all other nodes in the cluster. For example, if CQL is connected to a node in the cluster, the status of all nodes in the cluster can be obtained.<br>  In other words, the state of any node information about nodes in the cluster should be consistent!</li></ul></li></ul><h4 id="Failure-Detection-and-Recovery"><a href="#Failure-Detection-and-Recovery" class="headerlink" title="Failure Detection and Recovery"></a>Failure Detection and Recovery</h4><ul><li>Gossip can detect if other nodes are in the normal state to avoid requests being routed to unreachable and low-performance nodes. </li><li>The sensitivity of failure detection can be adjusted by configuring <strong>phi_convict_threshold</strong>.</li><li>Gossip will <u>periodically</u> contact the failure nodes to see if they recover rather than removing them directly.You will need to add or remove nodes mandatorily by using <strong>nodetool</strong>.</li><li>Once a node is marked as a failure, other replicas will help it to store the missing writes(hinted handoff should be turned on, if the failure time exceeds max_hint_window_in_ms, those writes will not be stored.) Failure nodes need to repair after recovery. We normally run nodetool repair on all the nodes to ensure consistency.<ul><li><u>Dynamic snitch feature</u>: The query request is routed to a node. If the node crashes or responds slowly, it should be able to query copies on other nodes</li><li>Delete node: After the node fails, it is still in the cluster, and the node can be offline from the cluster through removenode. The difference is that if the status does not exist, it is offline. The DN is still in the cluster.</li><li>Failed node data: The data cannot be stored normally to the failed node, so it will be temporarily saved by other nodes, and after it is restored, the missed writes will be added.</li></ul></li></ul><h4 id="Consistent-Hashing"><a href="#Consistent-Hashing" class="headerlink" title="Consistent Hashing"></a>Consistent Hashing</h4><p>Details about consistent hashing can be found in another <a href="https://10kshuaizhang.github.io/2020/08/13/Consistent-Hashing/" target="_blank" rel="noopener">article</a> in my blog.</p><h4 id="Hash-Value-Space-and-Token"><a href="#Hash-Value-Space-and-Token" class="headerlink" title="Hash Value Space and Token"></a>Hash Value Space and Token</h4><ul><li>The  calculation of the key on which node exists is to use the method of moving forward to find the first node on the ring. Moving is a calculation process.</li><li>If you can calculate the value space of the nodes (vnodes) in the cluster in the entire hash ring in advance, after hashing the key, you can see which hash value space it falls on. The relationship between the value space and the node is already known, so you can directly locate which node the key falls on. This is how <strong><u>token</u></strong> works.<ul><li>E.g. Each row of data in a table is identified by the primary key, and the table assigns a hash value to each primary key, and each node (vnode) in the cluster has one or more hash value intervals. In this way, the data can be placed in the node (vnode) corresponding to the hash value interval containing the hash value according to the hash value corresponding to the primary key.</li></ul></li></ul><h4 id="Virtual-Nodes"><a href="#Virtual-Nodes" class="headerlink" title="Virtual Nodes"></a>Virtual Nodes</h4><ul><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1601072845/Cassandra/Cassandra2_xullxz.png" alt="Cassandra-img-2" style="zoom:67%;" />  * Upper image: No virtual nodes are used, the number of tokens in the ring equals the number of machines in the cluster. For example, there are a total of 6 nodes above, so the number of tokens is 6.  * Because the copy factor is 3, a record must exist on three nodes in the cluster. The simple way is to calculate the hash value of the <u>rowkey</u> and which token in the ring falls on. The data is on that node, and the remaining two copies fall on its following two nodes  on the token ring.  * In this figure, A, B, C, D, E, and F are the key range, and the real value is the hash ring space. For example, the interval from 0 to 2^32 is divided into 10 parts. Each segment is 1/10 of 2^32. Node 1 contains A, F, and E means that the data whose key range is A, F, and E will be stored on node 1. </li><li>If you do not use virtual nodes, you need to manually calculate and allocate a token for <strong><u>each node in the cluster</u></strong>.<ul><li>Each token determines the <u>position of the node</u> in the ring and the <u>range of a continuous data hash value</u>.</li><li>As shown in the upper part of the above figure, each node is assigned a separate token to represent a position in the ring, and each node stores the row key to a hash value and then falls within the corresponding range. Each node also contains copies of rows from other nodes.</li></ul></li><li>The  Advantages of Virtual Nodes<ul><li>No need to calculate and allocate token for each node</li><li>No need to rebalance cluster load after adding and removing nodes</li><li>Rebuilding dead nodes is faster</li><li>Improved the use of heterogeneous machines in the same cluster</li></ul></li></ul><h4 id="Data-Replication"><a href="#Data-Replication" class="headerlink" title="Data Replication"></a>Data Replication</h4><ul><li><p>There are currently two replication strategies available:</p><ul><li>SimpleStrategy: Only used in a single data center. The first replica is placed in the node determined by the partitioner, and the remaining replicas are placed in the subsequent nodes in the clockwise direction of the above node.</li><li>NetworkTopologyStrategy: Can be used for more complex multiple data centers. You can specify how many replicas are stored in each data center.</li></ul></li><li><p>The replication strategy is specified when creating the keyspace, such as</p><ul><li><pre><code class="sql"><span class="keyword">CREATE</span> KEYSPACE Excelsior <span class="keyword">WITH</span> <span class="keyword">REPLICATION</span> = { <span class="string">'class'</span> : <span class="string">'SimpleStrategy'</span>,<span class="string">'replication_factor'</span> : <span class="number">3</span> };  <span class="keyword">CREATE</span> KEYSPACE Excalibur <span class="keyword">WITH</span> <span class="keyword">REPLICATION</span> = {<span class="string">'class'</span> :<span class="string">'NetworkTopologyStrategy'</span>, <span class="string">'dc1'</span> : <span class="number">3</span>, <span class="string">'dc2'</span> : <span class="number">2</span>};</code></pre></li><li><p>The names of the data centers dc1 and dc2 must be consistent with the names configured in snitch. The topology strategy above indicates that 3 copies are configured in dc1 and 2 copies are configured in dc2.</p></li></ul></li></ul><h4 id="Partitioners"><a href="#Partitioners" class="headerlink" title="Partitioners"></a>Partitioners</h4><ul><li><p>In Cassandra, each row of the table is identified by a unique primary key. The partitioner is actually <strong><u>a hash function</u></strong> used to calculate the token of the primary key. </p><ul><li><p>Cassandra places the corresponding row in the cluster based on this token value.</p></li><li><p>Note: If you use virtual nodes (vnodes), you don’t need to manually calculate tokens. If you do not use virtual nodes, you must manually calculate the tokens and assign the obtained value to the <u>initial_token</u> parameter in the cassandra.ymal main configuration file.</p></li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Table of contents&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Architecture&lt;/li&gt;
&lt;li&gt;Client request&lt;/li&gt;
&lt;li&gt;Read/Write procedure&lt;/li&gt;
&lt;li&gt;Acknowledgement &lt;/li&gt;
&lt;/ol&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
      <category term="Cassandra" scheme="http://yoursite.com/tags/Cassandra/"/>
    
      <category term="Databse" scheme="http://yoursite.com/tags/Databse/"/>
    
  </entry>
  
  <entry>
    <title>Serializability versus Linearizability</title>
    <link href="http://yoursite.com/2020/09/23/Serializability-versus-Linearizability/"/>
    <id>http://yoursite.com/2020/09/23/Serializability-versus-Linearizability/</id>
    <published>2020-09-23T22:42:47.000Z</published>
    <updated>2020-09-23T22:43:27.359Z</updated>
    
    <content type="html"><![CDATA[<p>I was reading my notes of 《Design Data-Intensive Application》, which talks about linearizability versus serializability. So I checked some materials and found a good article about this topic. Thanks to the author Peter Bailis. You can also check the original page <a href="http://www.bailis.org/blog/linearizability-versus-serializability/" target="_blank" rel="noopener">here</a>.</p><a id="more"></a><blockquote><p>24 Sep 2014</p><p>Linearizability and serializability are both important properties about interleavings of operations in databases and distributed systems, and it’s easy to get them confused. This post gives a short, simple, and hopefully practical overview of the differences between the two.</p><h4 id="Linearizability-single-operation-single-object-real-time-order"><a href="#Linearizability-single-operation-single-object-real-time-order" class="headerlink" title="Linearizability: single-operation, single-object, real-time order"></a>Linearizability: single-operation, single-object, real-time order</h4><p><em><a href="http://cs.brown.edu/~mph/HerlihyW90/p463-herlihy.pdf" target="_blank" rel="noopener">Linearizability</a> is a guarantee about single operations on single objects.</em> It provides a real-time (i.e., wall-clock) guarantee on the behavior of a set of single operations (often reads and writes) on a single object (e.g., distributed register or data item).</p><p>In plain English, under linearizability, writes should appear to be instantaneous. Imprecisely, once a write completes, all later reads (where “later” is defined by wall-clock start time) should return the value of that write or the value of a later write. Once a read returns a particular value, all later reads should return that value or the value of a later write.</p><p>Linearizability for read and write operations is synonymous with the term “atomic consistency” and is the “C,” or “consistency,” in Gilbert and Lynch’s <a href="http://lpd.epfl.ch/sgilbert/pubs/BrewersConjecture-SigAct.pdf" target="_blank" rel="noopener">proof of the CAP Theorem</a>. We say linearizability is <em>composable</em> (or “local”) because, if operations on each object in a system are linearizable, then all operations in the system are linearizable.</p><h4 id="Serializability-multi-operation-multi-object-arbitrary-total-order"><a href="#Serializability-multi-operation-multi-object-arbitrary-total-order" class="headerlink" title="Serializability: multi-operation, multi-object, arbitrary total order"></a>Serializability: multi-operation, multi-object, arbitrary total order</h4><p><em>Serializability is a guarantee about transactions, or groups of one or more operations over one or more objects.</em> It guarantees that the execution of a set of transactions (usually containing read and write operations) over multiple items is equivalent to <em>some</em> serial execution (total ordering) of the transactions.</p><p>Serializability is the traditional “I,” or isolation, in <a href="http://sites.fas.harvard.edu/~cs265/papers/haerder-1983.pdf" target="_blank" rel="noopener">ACID</a>. If users’ transactions each preserve application correctness (“C,” or consistency, in ACID), a serializable execution also preserves correctness. Therefore, serializability is a mechanism for guaranteeing database correctness.<a href="http://www.bailis.org/blog/linearizability-versus-serializability/#fn:mechanism" target="_blank" rel="noopener">1</a></p><p>Unlike linearizability, serializability does not—by itself—impose any real-time constraints on the ordering of transactions. Serializability is also not composable. Serializability does not imply any kind of deterministic order—it simply requires that <em>some</em> equivalent serial execution exists.</p><h4 id="Strict-Serializability-Why-don’t-we-have-both"><a href="#Strict-Serializability-Why-don’t-we-have-both" class="headerlink" title="Strict Serializability: Why don’t we have both?"></a>Strict Serializability: Why don’t we have both?</h4><p>Combining serializability and linearizability yields <em>strict serializability</em>: transaction behavior is equivalent to some serial execution, and the serial order corresponds to real time. For example, say I begin and commit transaction T1, which writes to item <em>x</em>, and you later begin and commit transaction T2, which reads from <em>x</em>. A database providing strict serializability for these transactions will place T1 before T2 in the serial ordering, and T2 will read T1’s write. A database providing serializability (but not strict serializability) could order T2 before T1.<a href="http://www.bailis.org/blog/linearizability-versus-serializability/#fn:implementation" target="_blank" rel="noopener">2</a></p><p>As <a href="http://cs.brown.edu/~mph/HerlihyW90/p463-herlihy.pdf" target="_blank" rel="noopener">Herlihy and Wing</a> note, “linearizability can be viewed as a special case of strict serializability where transactions are restricted to consist of a single operation applied to a single object.”</p><h4 id="Coordination-costs-and-real-world-deployments"><a href="#Coordination-costs-and-real-world-deployments" class="headerlink" title="Coordination costs and real-world deployments"></a>Coordination costs and real-world deployments</h4><p>Neither linearizability nor serializability is achievable without coordination. That is we can’t provide either guarantee with availability (i.e., CAP “AP”) under an asynchronous network.<a href="http://www.bailis.org/blog/linearizability-versus-serializability/#fn:hardness" target="_blank" rel="noopener">3</a></p><p>In practice, your database is <a href="http://www.bailis.org/blog/when-is-acid-acid-rarely/" target="_blank" rel="noopener">unlikely to provide serializability</a>, and your multi-core processor is <a href="http://preshing.com/20120930/weak-vs-strong-memory-models/" target="_blank" rel="noopener">unlikely to provide linearizability</a>—at least by default. As the above theory hints, achieving these properties requires a lot of expensive coordination. So, instead, real systems often use cheaper-to-implement and often <a href="http://www.bailis.org/blog/understanding-weak-isolation-is-a-serious-problem/" target="_blank" rel="noopener">harder-to-understand</a> models. This trade-off between efficiency and programmability represents a fascinating and challenging design space.</p><h4 id="A-note-on-terminology-and-more-reading"><a href="#A-note-on-terminology-and-more-reading" class="headerlink" title="A note on terminology, and more reading"></a>A note on terminology, and more reading</h4><p>One of the reasons these definitions are so confusing is that linearizability hails from the distributed systems and concurrent programming communities, and serializability comes from the database community. Today, almost everyone uses <em>both</em> distributed systems and databases, which often leads to overloaded terminology (e.g., “consistency,” “atomicity”).</p><p>There are many more precise treatments of these concepts. I like <a href="http://link.springer.com/book/10.1007%2F978-3-642-15260-3" target="_blank" rel="noopener">this book</a>, but there is plenty of free, concise, and (often) accurate material on the internet, such as <a href="https://www.cs.rochester.edu/~scott/458/notes/04-concurrent_data_structures" target="_blank" rel="noopener">these notes</a>.</p><h4 id="Notes"><a href="#Notes" class="headerlink" title="Notes"></a>Notes</h4><ol><li><p>But it’s not the only mechanism!</p><p> Granted, serializability is (more or less) the most <em>general</em> means of maintaining database correctness. In what’s becoming one of my favorite “underground” (i.e., relatively poorly-cited) references, <a href="http://en.wikipedia.org/wiki/H._T._Kung" target="_blank" rel="noopener">H.T. Kung</a> and <a href="http://en.wikipedia.org/wiki/Christos_Papadimitriou" target="_blank" rel="noopener">Christos Papadimitriou</a> dropped a paper in SIGMOD 1979 on “<a href="http://www.eecs.harvard.edu/~htk/publication/1979-sigmod-kung-papadimitriou.pdf" target="_blank" rel="noopener">An Optimality Theory of Concurrency Control for Databases</a>.” In it, they essentially show that, if all you have are transactions’ syntactic modifications to database state (e.g., read and write) and <em>no</em> information about application logic, serializability is, in some sense, “optimal”: in effect, a schedule that is not serializable might modify the database state in a way that produces inconsistency for some (arbitrary) notion of correctness that is not known to the database.</p><p> However, if <em>do</em> you know more about your user’s notions of correctness (say, you <em>are</em> the user!), you can often do a lot more in terms of concurrency control and can circumvent many of the fundamental overheads imposed by serializability. Recognizing when you don’t need serializability (and subsequently exploiting this fact) is the best way I know to “beat CAP.” <a href="http://www.bailis.org/blog/linearizability-versus-serializability/#fnref:mechanism" target="_blank" rel="noopener">↩</a></p></li><li><p>Note that some implementations of serializability (such as two-phase locking with long write locks and long read locks) actually provide strict serializability. As <a href="http://cs.brown.edu/~mph/HerlihyW90/p463-herlihy.pdf" target="_blank" rel="noopener">Herlihy and Wing</a> point out, other implementations (such as some MVCC implementations) may not.</p><p> So, why didn’t the early papers that defined serializability call attention to this real-time ordering? In some sense, real time doesn’t really matter: all serializable schedules are equivalent in terms of their power to preserve database correctness! However, there are some weird edge cases: for example, returning NULL in response to every read-only transaction is serializable (provided we start with an empty database) but rather unhelpful.</p><p> One tantalizingly plausible theory for this omission is that, back in the 1970s when serializability theory was being invented, everyone was running on single-site systems anyway, so linearizability essentially “came for free.” However, I believe this theory is unlikely: for example, database pioneer <a href="http://en.wikipedia.org/wiki/Phil_Bernstein" target="_blank" rel="noopener">Phil Bernstein</a> was already looking at distributed transaction execution in his SDD-1 system <a href="http://www.dtic.mil/cgi-bin/GetTRDoc?AD=ADA131789" target="_blank" rel="noopener">as early as 1977</a> (and there are <a href="http://ieeexplore.ieee.org/xpl/login.jsp?tp=&arnumber=4567884" target="_blank" rel="noopener">older references</a> yet). Even in this early work, Bernstein (and company) are careful to stress that “there may in fact be <em>several</em> such equivalent serial orderings” [emphasis theirs]. To further put this theory to rest, Papadimitriou makes clear in his seminal <a href="https://www.cs.purdue.edu/homes/bb/cs542-06Spr-bb/SCDU-Papa-79.pdf" target="_blank" rel="noopener">1979 JACM</a> article that he’s familiar with problems inherent in a distributed setting. (If you ever want to be blown away by the literature, look at how much of the foundational work on concurrency control was done by the early 1980s.) <a href="http://www.bailis.org/blog/linearizability-versus-serializability/#fnref:implementation" target="_blank" rel="noopener">↩</a></p></li><li><p>For distributed systems nerds: achieving linearizability for reads and writes is, in a formal sense, “easier” to achieve than serializability. This is probably deserving of another post (encouragement appreciated!), but here’s some intuition: terminating atomic register read/write operations <a href="http://www.cse.huji.ac.il/course/2004/dist/p124-attiya.pdf" target="_blank" rel="noopener">are achievable</a> in a fail-stop model. Yet atomic commitment—which is needed to execute multi-site serializable transactions (think: AC is to 2PC as consensus is to Paxos)—is not: the <a href="http://www.cs.utexas.edu/~lorenzo/corsi/cs380d/past/03F/notes/fischer.pdf" target="_blank" rel="noopener">FLP result</a> says consensus is unachievable in a fail-stop model (hence <em>with One Faulty Process</em>), and (non-blocking) atomic commitment is <a href="http://link.springer.com/chapter/10.1007/BFb0022140" target="_blank" rel="noopener">“harder” than consensus</a> (<a href="http://infoscience.epfl.ch/record/83471/files/1596162953p115-delporte.pdf" target="_blank" rel="noopener">see also</a>). Also, keep in mind that linearizability for read-modify-write <a href="http://cs.brown.edu/~mph/Herlihy91/p124-herlihy.pdf" target="_blank" rel="noopener">is harder than</a> linearizable read/write. (linearizable read/write《 consensus《 atomic commitment) </p></li></ol></blockquote><p>Also, there are some excellent Q&amp;A sections on the original page, you can check them if interested.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I was reading my notes of 《Design Data-Intensive Application》, which talks about linearizability versus serializability. So I checked some materials and found a good article about this topic. Thanks to the author Peter Bailis. You can also check the original page &lt;a href=&quot;http://www.bailis.org/blog/linearizability-versus-serializability/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;here&lt;/a&gt;.&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记11</title>
    <link href="http://yoursite.com/2020/08/29/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B011/"/>
    <id>http://yoursite.com/2020/08/29/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B011/</id>
    <published>2020-08-29T16:27:02.000Z</published>
    <updated>2020-09-10T18:01:27.085Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter 9 Consistency and Consensus</strong></p><ul><li>In this chapter, we will talk about some examples of algorithms and protocols for building <strong><u>fault-tolerant distributed systems</u></strong>.</li><li>We will assume that all the problems from Chapter 8 can occur:<ul><li>packets can be lost, reordered, duplicated</li><li>Arbitrarily delayed in the network</li><li>Clocks are approximate the best</li><li>and nodes can pause (e.g., due to garbage collection) or crash at any time.</li></ul></li><li>The best way of building fault-tolerent system is to find some <strong>general-purpose abstractions with useful guarantees</strong>, implement them once, and then let applications rely on those guarantees.</li><li>This is the same approach as we used with transactions in Chapter 7: <u>by using a transaction</u>, the application can pretend:<ul><li>that there are no crashes (<strong>atomicity</strong>),</li><li>that nobody else is concurrently accessing the database (<strong>isolation</strong>)</li><li>that storage devices are perfectly reliable (<strong>durability</strong>)</li></ul></li><li><u>One of the most important abstractions</u> for distributed systems is <strong>consensus</strong>: that is, getting all of the nodes to agree on something.<ul><li><u>Once you have an implementation of consensus, applications can use it for various purposes</u>.</li><li>Correct implementations of consensus help avoid problems.</li></ul></li></ul><a id="more"></a><h4 id="Consistency-Guarantees-Weak-vs-Strong"><a href="#Consistency-Guarantees-Weak-vs-Strong" class="headerlink" title="Consistency Guarantees (Weak vs. Strong)"></a>Consistency Guarantees (Weak vs. Strong)</h4><ul><li>Inconsistencies occur no matter what replication method the database uses (single-leader, multi-leader, or leaderless replication).<ul><li>Most replicated databases provide at least <strong>eventual consistency</strong>. A better name for eventual consistency may be <strong><u>convergence</u></strong>, as we expect all the replicas to eventually converge to the same value.</li><li><strong>Weak guarantee</strong>: it doesn’t say anything about when the replicas will converge.</li></ul></li><li><strong>Eventual consistency</strong> is hard for application developers because it is so different from the behavior of variables in a normal single-threaded program.</li><li>System with <strong>Strong guarantees</strong> may have worse performance or be less fault-tolerant than systems with weaker guarantees.</li><li><strong>Distributed consistency models</strong> vs. <strong>Hierarchy of transaction isolation levels</strong><ul><li><strong>transaction isolation</strong> is primarily about <u>avoiding race conditions</u> due to concurrently executing transactions;</li><li><strong>distributed consistency</strong> is mostly about <u>coordinating the state of replicas</u> in the face of delays and faults.</li></ul></li></ul><h4 id="Linearizability-C-SLOW-not-used-very-often-in-practice"><a href="#Linearizability-C-SLOW-not-used-very-often-in-practice" class="headerlink" title="Linearizability (C: SLOW, not used very often in practice)"></a>Linearizability (C: SLOW, not used very often in practice)</h4><ul><li><p>Idea behind <strong>Linearizability</strong> (also know as <strong>atomic consistency, strong consistency, immediate consistency</strong>, or <strong>external consistency</strong>)</p><ul><li><strong>The basic idea is to make a system appear as if there were only one copy of the data, and all operations on it are atomic</strong>. (Even though there may be multiple replicas in reality, the application does not need to worry about them.)</li></ul></li><li><p><strong>In a linearizable system</strong>, as soon as the client successfully completes a write, all clients reading from the databases must be able to see the value just written.</p><p>  In other words, linearizability is a <strong>recency guarantee</strong>. </p>  <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587294/Design Data Intensive Application/p2-ch9-img1_f1gdg3.png" alt="p2-ch9-img1" style="zoom:50%;" /></li><li><p><strong>What Makes a System Linearizable?</strong> To make a system appear as if there is only a single copy of the data. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587297/Design Data Intensive Application/p2-ch9-img2_de6emd.png" alt="p2-ch9-img1" style="zoom:50%;" /></p><ul><li><strong>automic compare-and-set (cas)</strong> <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587295/Design%20Data%20Intensive%20Application/p2-ch9-img3_soo79u.png" alt="p2-ch9-img1" style="zoom:50%;" /><ul><li>when the operation in the rectangle was truly executed, we project a bold black line on the database axis.</li></ul></li><li><strong>Linearizability vs. Serializability</strong><ul><li><strong>Serializability</strong> is an <strong><u>isolation property of transaction</u></strong>, where every transaction may read and write multiple objects (rows, documents, records).</li><li><strong>Linearizability</strong> is a <u>recency guarantee on reads and writes</u> of a register (an individual object).<ul><li>2PL &amp; actual serial execution are typically linearizable.</li><li><strong><u>serializable snapshot isolation</u></strong> is <strong>NOT</strong> linearizable.</li></ul></li><li>A database may aprovide both <strong>serializability</strong> and <strong>linearizability</strong>, and this combination is know as <u>strict serializability</u> or <u>strong one-copy seriazability</u>.</li></ul></li><li><strong>Relying on Linearizability</strong><ul><li>In what circumstances in linearizability useful?<ul><li><strong>Locking and leader election</strong>: (e.g. ZooKeeper, etc.)<ul><li>Lock must be linearizable: all nodes must agree which node owns the lock; otherwise it is useless.</li><li>Distributed locking is also used at a much more granular level in some distributed databases, such as Oracle Real Application Clusters (RAC)</li></ul></li></ul></li></ul></li><li><strong>Constraints and Uniqueness guarantees</strong><ul><li>if you want to enforce this constraint as the data is written (such that two people try to concurrently create a user or a file with same name, one of them will be returned an error), you need <strong>linearizavility</strong>. (C: kind like “lock”)</li><li>a hard uniqueness constraint, such as the one you typically find in relational databases, requires linearizability.</li></ul></li><li><strong>Cross-channel timing dependencies</strong><ul><li>The linearizability vilation was only notices because there was an additional communication channel in the system. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587295/Design%20Data%20Intensive%20Application/p2-ch9-img4_dovp96.png" alt="p2-ch9-img4" style="zoom:50%;" /></li><li>If the file storage service is linearizable, then this system should work fine.</li><li>If it is not leanearizable, there is the risk of a race condition<ul><li>The message queue (steps 3 and 4 in Figuer 9-5) might be faster than the internal replication inside the storage service.</li></ul></li><li>This problem arises because there are <u>two different communication channels</u> between the web server and the resizer: the file storage and the message queue.</li></ul></li></ul></li><li><p><strong>Implementing Linearizable System</strong></p><ul><li><strong>Replication &amp; Linearizable</strong><ul><li>Single-leader replication (potentially linearizable);</li><li>Consensus algorithms (linearizable) [e.g. Zookeeper and etcd]</li><li>Multi-leader replication (not linearizable)</li><li>Leaderless replication (probably not linearizable)</li></ul></li><li><strong>linearizability and quorums</strong><ul><li>it seems as though strict quorum reads and writes should be linearizable in  a Dynamo-style model. However, when we have variable network delays, it is possible to have race conditions. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587294/Design%20Data%20Intensive%20Application/p2-ch9-img5_w4g1ca.png" alt="p2-ch9-img4" style="zoom:50%;" /></li><li>it is safest assume that a <u>leaderless system with Dynamo-style replication does not provide linearizability</u>.</li></ul></li></ul></li><li><p><strong>The Cost of Linearizability</strong><img src="https://res.cloudinary.com/shuaiz/image/upload/v1598587296/Design%20Data%20Intensive%20Application/p2-ch9-img6_bavmw6.png" alt="p2-ch9-img4" style="zoom:50%;" /></p><ul><li><strong>The CAP therom</strong>(very narrow scope)<ul><li>CAP is sometimes presented as <strong>Consistency, Availability, Partition tolerence</strong>: pick 2 out of three</li><li><strong>The Unhelpeful CAP Theorem</strong><ul><li>Unfortunately, putting it this way is misleading because <strong>network partitions are a kind of fault</strong>, so that aren’t something about which you have a  choice, <strong><u>they will happen whether you like it or not</u></strong>.</li><li>A better way of phrasing CAP would be either <strong>Consistent</strong> or <strong>Available</strong> when <u><strong>Partitioned</strong></u>. (Aka, CP or AP)</li><li>All in all, there is a lot of misunderstanding and confusion around CAP, and <strong>it does not help us understand system better, so CAP is best avoided</strong></li></ul></li></ul></li><li><strong>Linearizability and network delays</strong><ul><li>Although linearizability is a useful guarantee, surprisingly few system are actually linearizable in practice.</li><li>Many distributed databases that choose not to provide linearizable guarantees: <u>they do so primarily to increase performance, not so much for fault tolerance</u>.</li><li>Linearizability is <strong>slow</strong> – and this is true all the time, not only during a network fault.</li></ul></li></ul></li></ul><h4 id="Ordering-Guarantees"><a href="#Ordering-Guarantees" class="headerlink" title="Ordering Guarantees"></a>Ordering Guarantees</h4><ul><li>ordering has been a recurring theme in this book (e.g. Order of writes, Serizability, timestamps and clocks etc. )</li><li>There are deep connections between <strong>ordering</strong>, <strong>linearizability</strong>, and <strong>consensus</strong>.</li><li><strong>Ordeing and Causality</strong><ul><li>There are several reasons why ordering keeps coming up, and one of the reason is that it helps preserve <strong>causality</strong>.<ul><li>E.g. <u>causal dependency</u> between the questions and the answer;</li></ul></li><li><strong>Causality impose an ordering on events</strong>: cause comes before effect<ul><li>a message is sent before that message is received;</li><li>the question comes before the answer</li></ul></li><li>if a system obeys the ordering imposed by causality, we say that it is <strong>causality consistent</strong>.<ul><li>E.g. <u>snapshot isolation</u> provides causal consistency</li></ul></li><li><strong>The causal order is NOT a total order</strong><ul><li>A <strong>total order</strong> allows any two elements to be compared, so if you have two elements, you can always say which one is greater and which one is smaller.</li><li>However, mathematical sets are not totally ordered: is {a, b} greater than {b ,c}?<ul><li>We say they are <strong>incomparable</strong>, and therefore mathematical sets are partially ordered.</li></ul></li><li><strong>Linearizability</strong>: In a linearizable system, we have a <u>total order</u> of operations</li><li><strong>Causality</strong>: two events are ordered if they are causally related (one happend before the other), but they are incomparable if they are concurrent. Hence <strong><u>causality defines a partial order</u></strong>.</li></ul></li><li>There are <strong>no concurrent operations in a linearizable datastore</strong>: there must be a single timeline along which all operations are <strong>totally ordered</strong>.</li><li><strong>Distributed VCS</strong> such as <u>Git</u>, their version histories are very much like the graph of causal dependencies.<ul><li>Often one commit happends after another, in a straight line, but sometimes tou get branches (when several people concurrently work on a project), and merges are created when those concurrently created commits are combined.</li></ul></li><li><strong>Linearizability is stronger than causal consistency</strong><ul><li>what is the relationship between the causal order and linearizability?<ul><li><strong>linearizability implies casusality</strong>: any system that is linearizable will preserve causality correctly.</li></ul></li><li><strong>Causal Consistency</strong> is the <strong><u>Strongest</u></strong> possible consistency model that does <strong><u>NOT slow down</u></strong> due to network delay, and remains available in the face of the network failures.</li><li>In many cases, systems that appear to require linearizability in fact only really require causal consistency, which can be implemented more efficiently.</li></ul></li><li><strong>Capturing causal dependencies</strong><ul><li>In order to maintain causality, you need to know which operation happens before which other operation.</li><li>In order to determine causal dependencies, we need some way of describing the “knowledge” of a node in the system.</li><li>The techniques for determining which operation happened before which other operation are similar to what we discussed in “Detecting Concurrent Writes”</li><li>In order to determine the causal ordering, the database needs to know which version of the data was read by application. (e.g. Version Vectors, or conflic detection of SSI)</li></ul></li></ul></li><li><strong>Sequence Number Ordering</strong><ul><li>Although causality is an important theoretical concept, actually keeping track of all causal dependencies can become impractical.</li><li>Better way: we can use sequence numbers of timestamps to order events.<ul><li>A <strong>timestamp</strong> need not to come from a time-of-day clock but “<strong>Logical Clock</strong>“.</li><li>Such sequence numbers or timestamps are conpact (only a few bytes in size), and they provide a total order.</li></ul></li><li><strong>Non-causal sequence number generators</strong><ul><li>If there is not a single leader (perhaps because you are using a multi-leader or leaderless database, or because the database is partitioned), it is less clear how to generate sequence numbers for operations.<ul><li>Each node can generate its own independent set of sequence numbers. (e.g. one node odd, another node even; unique node identifier);</li><li>You can attach a timestamp from a time-of-day clock (physical clock) to each operation;</li><li>You can preallocate blocks of sequence numbers.</li></ul></li><li>They all have a problem: <strong>the sequence numbers they generate are not consistent with causality</strong>.</li></ul></li><li><strong>Lamport timestamps</strong><ul><li>a simple method for <strong>generating sequnce numbers that is consistent with causality</strong></li><li><strong>The Lamport timestamp is then simply a pair of (counter, node ID).</strong> <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598708519/Design%20Data%20Intensive%20Application/p2-ch9-img7_vfjktj.png" alt="p2-ch9-img4" style="zoom:50%;" /></li><li>A lamport timestamp bears no relationship to a physical time-of-day clock, but it provides total ordering<ul><li>if you have two timestamps, the one with a greater counter value is the greater timestamp;</li><li>if the counter values are the same, the one withe the greater node ID is the greater timestamp.</li></ul></li><li>Key idea: <strong>every node and every client keeps track of the maximum counter value it has seen so far, and including that maximun on every request</strong>.<ul><li>When a node receives a request or response with a maximum counter value greater that its own counter value, it immediately increases its own counter to that maximum. (: kind like “Read repair”? )</li></ul></li><li><u>As long as the maximum counter value is carried along with every operation</u>, this scheme ensures that the ordering from the Lamport timestamps is consistent with causality, because every causal dependency results in an increased timestamp.</li><li><strong>vs. Version Vector</strong><ul><li>version vectors can distingush whether two operations are concurrent or whether one is causally dependent on the other;</li><li>whereas Lamport timestamps always enforce a total ordering.</li></ul></li><li><strong>Timestamp ordering is not sufficient</strong><ul><li>The problem here is that the total order of operations enly emerges after you have collected all of the operations.</li><li>To conclude: in order to implement something like a uniqueness constraint for usernames, it’s not sufficient when that order is finalized.<ul><li>This idea of knowing when your total order is finalized is captured in the topic of <strong>total order broadcast</strong>.</li></ul></li></ul></li></ul></li></ul></li><li><strong>Total Order Broadcast</strong><ul><li>if your program runs only on a single CPU core, it is easy to define a total ordering of operations: it is simply the order in which they were executed by the CPU. (but it is (no-surprising) tricky in distributed-system)</li><li>In the distributed systems literature, this problem is known as <strong>total order broadcast or atomic broadcast</strong>.</li><li>Total order broadcast –&gt; protocol for exchanging messages between nodes. With two safety properties:<ul><li><strong>Reliable delivery</strong>: No message are lost, if a message is delivered to one node, it is delivered to all nodes.</li><li><strong>Total ordered delivery</strong>: Message are delivered to every node in the same order.</li></ul></li><li><strong>Using total order broadcast</strong>: Log-styple (e.g. Consensus services such as ZooKeeper and etcd)<ul><li>There is a <u>strong connection between <strong>total order broadcast and consensus</strong></u>.</li><li>Use cases:<ul><li>Can be used for DB replication (aka. State machine replication)</li><li>Can be used  to implement serializable transactions.</li><li>Useful for implementing a lock service that provides fencing tokens. (e.g. zxid in ZooKeeper)</li></ul></li><li><strong>Total order broadcast</strong> is that the order is fixed at the time the message are delivered.</li><li>Another way of looking at total order broadcast is that it is a way of creating a log (as in a replication log, transaction log, or write-ahead log)<ul><li>Delivering a message is like appending to the log.</li></ul></li></ul></li><li><strong>Implementing linearizable storage using total order broadcast</strong><ul><li><strong>Total order broadcast</strong> is <u>synchronous</u>: messages are guaranteed to be delivered reliably in a fixed order, but there is no guarantee about when a message will be delivered (so one recipient may lag behind the others).<ul><li>By contrast, <strong>linearizability</strong> is a recency guarantee: a read is guaranteed to see the latest value written.</li></ul></li><li>While this procedure ensures <strong>linearizable writes</strong>, it doesn’t guarantee linearizable reads. (due to async. update)<ul><li>To be precise, the procedure described here provides <strong>sequential consistency</strong>, sometimes also known as <strong>timeline consistency</strong>, a <u>slightly weaker guarantee</u> than linearizability.</li><li>There are ways to make reads linearizable tho.</li></ul></li></ul></li><li><strong>implementing total order broadcast using linearizable storage</strong><ul><li>The easiest way is to assume you have a linearizable register that stores an integer and that has an atomic <u>increment-and-get</u> operation.<ul><li>Alternatively, an <u>atomic compare-and-set</u> operation would also do the job.</li></ul></li><li>It can be proved that a linearizable compare-and-set (or increment-and-get) register and total order broadcast are both equivalent to consensus.</li></ul></li></ul></li></ul><h4 id="Distributed-Transactions-and-Consensus"><a href="#Distributed-Transactions-and-Consensus" class="headerlink" title="Distributed Transactions and Consensus"></a>Distributed Transactions and Consensus</h4><ul><li><p><strong>Consensus</strong> is one of the most important and fundamental problems in distributed computing: the goal is simply to get sveral nodes to agree on something.</p></li><li><p>Why is Consensus important?</p><ul><li>Leader election</li><li>Atomic commit: aka. atomic commit problem.</li></ul></li><li><p><strong>The Impossibility of Consensus</strong>: FLP-result with every restrictive rules.</p></li><li><p><strong>Automic Commit and Two-Phase Commit (2PC)</strong></p><ul><li>Atomicity prevents failed transactions from littering the database with half-finished results and half-updated state</li><li><strong>From single-node to <u>distributed atomic commit</u></strong><ul><li>a node must only commit once it is certain that all other nodes in the transaction are also going to commit.</li></ul></li><li><strong>Introduction to two-phase commit</strong><ul><li><u>Two-phase commit is an algorithm for <strong>achieving atomic transaction</strong> commit across multiple nodes.</u> <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598710825/Design%20Data%20Intensive%20Application/p2-ch9-img8_chdlye.png" alt="p2-ch9-img8" style="zoom:50%;" /></li><li>2PC uses a new component that does not normally appear in single-node transactions: a <strong>coordinator</strong> (aka. <strong>tsansaction manager</strong>).</li><li>(This process is somewhat like the traditional marriage ceremony in Western cultures).</li></ul></li><li><strong>A system of promises</strong><ul><li>There is no more going back: if the decision was to commit, that decision must be enforced, no matter how many retries it takes.<ul><li>If a participant has crashed in the meantime, the transaction will be committed when it recovers – since the participant voted “yes”, it cannot refuses to commit when it recovers.</li></ul></li></ul></li><li><strong>Coordinator failure</strong><ul><li>If the coordinator fails before sending the prepare requests, a participant can safely abort the transaction.</li><li>But once the participant has received a prepared request and voted “yes”, it can no longer abort unilaterally – <strong>it must wait to hear back from the coordinator</strong>.</li><li>If the coordinator crashes or the network fails at this point, the participant can do nothing but wait.<ul><li>A participant’s transaction in this state is called <strong><u>in doubt or uncertain</u></strong>. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598710824/Design%20Data%20Intensive%20Application/p2-ch9-img9_gptfcy.png" alt="p2-ch9-img8" style="zoom:50%;" /></li></ul></li><li>when the coordinator recovers, it determines the status of all in-doubt transaction bby reading its transaction log.</li><li>Thus, <u>the commit point of 2PC comes down to a regular single-node atomic commit on the coordinator</u>.</li></ul></li><li><strong>Three-phase commit</strong><ul><li>Two-phase commit is called a <u>blcoking atomic commit protocal</u> due to the fact that 2PC can become stuck waiting for the coordinator to recover.</li><li>3PC assumes a netwrok with bounded delay and nodes with bounded response times.</li><li>Non-blocking atomic commit requires a perfect failure detector.</li></ul></li></ul></li><li><p><strong>Distributed Transactions in Practice</strong></p><ul><li>Many cloud services choose not to implement <u>distributed transactions</u> due to the operational problems that they engender.</li><li>Some implementations of distributed transactions carry a heavy performance penalty.<ul><li>E.g. distributed transactions in MySQL are reported to be over 10 times slower than single-node transactions.</li></ul></li><li><strong>Two quite different types of distributed transactions</strong><ul><li><strong>Database-internal distributed transactions</strong><ul><li>Some distributed databases (i.e., databases that hat use replcation and partitioning in their standard configuration) support interbnal transactions among the nodes of that database.</li></ul></li><li><strong>Heterogeneous distributed transactions</strong><ul><li>In a heterogeneous transaction, the participants are two or more different technologies: e.g. two databases from different venders, or even non-database systems such as message brokers.</li></ul></li></ul></li><li><strong>Exactly-once message processing</strong><ul><li>Heterogeneous distributed transactions allow diverse systems to be integrated in powerful ways.<ul><li>E.g. Message Queue work with DB.</li></ul></li><li>by automatically committing the message and the side effects of its processing, we can ensure that the message is effectively processed exactly once, even if it required a few retries before it succeeded.</li></ul></li><li><strong>XA transactions</strong>: X/Open XA (short for eXtended Architecture)<ul><li>a standard for implementing <strong>two-phase commit across heterogeneous technologies</strong><ul><li>XA is not a network protocal – it is merely a C API for interfacing with a transaction coordinator. (e.g. JTA –&gt; JDBC, JMS)</li></ul></li><li>XA is supported by many traditional relational databases (including PostgreSQL, MySQL, DB2, SQL Server, and Oracle) and message brokers (including ActiveMQ, HornetQ, MSMQ and IBM MQ).</li></ul></li><li><strong>Holding locks while in doube</strong><ul><li>Database transactions usually take a row-level exclusive lock on any rows they modify, to prevent dirty writes.</li></ul></li><li><strong>Recovering from coordinator failure</strong><ul><li>In theory, if the coordinator is crashed and is restored, it should cleanly recover its state from the log and resolve any in-doubt transactions.</li><li>However, in practice, orphaned in-doubt transactions do occur – that is, transactions for which the coordinator cannot decide the outcome for whatever reason.<ul><li>The only way out if for an administrator to manually decide whether to commit or roll back the transactions. (potentially requires a lot of manual effort)</li></ul></li><li>Many XA implementations have an emergency escape hatch alled <strong>heuristic decisions</strong>. (a euphemism for probably bvreaking atomicity)</li></ul></li><li><strong>Limitations of distributed transactions</strong><ul><li><strong>Transaction coordinator</strong> is itself a kind of database (in which transaction outcomes are stored), and so it needs to be approached with the same care as any other important database. (it like Single Point of Failure)</li><li><strong>Distributed transactions</strong> thus have a <strong>tendency of amplifying failures</strong>, which runs counter to our goal of building fault-tolerant systems.</li></ul></li></ul></li><li><p><strong>Fault-Tolerant Consensus</strong></p><ul><li><strong>Consensus problem</strong>: one or more nodes may propose values, and the consensus algorithm decides on one of those values.</li><li><strong>Consensues algorithm must satisfy the following properties</strong>: (Termination is a <strong>liveness</strong> property, whereas the other nodes decide differently)<ul><li><strong>Unifor agreement</strong>: No two nodes decide differently</li><li><strong>Intergrity</strong>: No one decides twice.</li><li><strong>Validity</strong>: If a node deices value v, then v was proposed by some node.</li><li><strong>Termination</strong>: Every node that does not crash eventually decides some value. (Formalizes the idea of fault tolerance)</li></ul></li><li><strong>Core idea</strong>: everyone decides on the same out come, and once you have decided, you cannot change you mind. (C: ALP 13, Disagree &amp; Comiit)</li><li>The <strong>termination property</strong> is subject to the <u>assumption</u> that fewer than half of the nodes are crashed or unreachable.</li><li>Most consensus algorithms <u>assume</u> that there are no Byzantine faults.</li><li><strong>Consensus algorithms and total order broadcast</strong><ul><li>The best-known fault-tolerant consensus algorithms are <u>Viewstampsed Replication (VSR), <strong>Paxos</strong>, Raft, and Zab</u>.</li><li>They decide on a sequence of values, which makes them <strong>total order broadcast</strong> algorithms, as discussed previously.</li></ul></li><li><strong>Single-leader replication and consensus</strong></li><li><strong>Epoch numbering and quorums</strong><ul><li>All of the consensus protocal discussed so far internally use a leader in some form of another, but they don’t guarantee that the leader is unique.</li><li>Instead, they can make a <strong>weaker guarantee</strong><ul><li>The protocal defines an <strong>epoch number</strong> (called the <strong>ballot</strong> number in Paxos, <strong>view</strong> number in Viewstamped Replication, and <strong>term</strong> number in Raft) and guarantee that within each epoch, the leader is unique.</li></ul></li><li>How does a leader know that it hasn’t been ousted by another node?<ul><li>It must collect votes from a quorum of nodes.</li><li>The quorum typically, but not always, consists of a majority of nodes.</li></ul></li><li>We have <u>two rounds of voting</u>: <strong>once</strong> to choose a leader, and a <strong>second</strong> time to vote on a leader’s proposal.</li><li><strong>Key insight is</strong> that the quorums for those two votes must overlap: if a vote n a proposal succeeds, at least one of the nodes that voted for it must have also participated in most recent leader’s election.</li><li>Vs. <strong>2PC</strong> which the coordinator is not elected <ul><li>Moreover, the consensus algorithms define a recovery process by which nodes can get into a consistent state after a new leader is elected, ensuring that the safety properties are always met.</li></ul></li></ul></li><li><strong>Limitations of consensus</strong><ul><li>Consensus algorithms are a huge breakthrough for distributed systems <ul><li>they bring concrete safety properties (agreement, integratity, and validity) to systems where everything else is uncertain,</li><li>they nevertheless remain fault-tolerant (able to make progress as long as a majority of nodes are working and reachable).</li><li>they can also implement linearizable atomic operations in a fault-tolerant way.</li></ul></li><li>But everything come at a cost</li><li>Consensus systems <strong>always require a strict majority to operate.</strong> (1 out of 3, 2 out of 5 etc.)</li><li>Most consensus algorithms <strong>assume</strong> a fixed set of nodes that participate in voting, which means that you can’t just add or remove nodes in the cluster.</li><li>Consensus systems generally rely on timeouts to detect failed node. (within highly variable netwrok delays, this could be lost of false failed) result in frequent leader elections result in terrible performance.</li><li>Sometimes, consensus algorithms are particularly <strong>sensitive to network problems</strong>.</li></ul></li></ul></li><li><p><strong>Membership and Coordination Service</strong></p><ul><li><p>projects like <strong>ZooKeeper</strong> or <strong>etcd</strong> are often described as “distributed key-value stores” or “coordination and configuration services”. (Useful for distributed coordination)</p></li><li><p>ZooKeeper not for general-purpose databases, but indirectly via some other project.</p><ul><li>E.g. <u>HBase, Hadoop YARN, OpenStack Nova, and <strong>Kafka</strong></u> all rely on ZooKeeper running in the background.</li></ul></li><li><p>ZooKeeper and etcd are designed to hold small amounts of data that can fit entirely in memory (although they still write to disk for durability)</p><ul><li>That small amount of data is replicated across all the nodes using a <strong>fault-tolerant total order broadcast</strong> algorithm.</li></ul></li><li><p><strong>total order broadcast is just what you need for database replication</strong></p><ul><li>if each message represents a write to the database, applying the same writes in teh same order keeps replicas consistent with each other.</li></ul></li><li><p>ZooKeeper not only <strong>total oder broadcast</strong> (and hence consensus), but also:</p><ul><li><strong>Linearizabble atomic operations</strong>: Using an atomic compare-and-set operation, you can implement a lock;</li><li><strong>Total ordering of operations</strong>: fencing token –&gt; monotonically increasing transaction ID (zxid) and version number (cversion)</li><li><strong>Failure detection</strong>: the client and server periodically exchange heartbeats to check that the other node is still alive;</li><li><strong>Change notifications</strong>: Noy only can on client read locks and values that were created bby another client, but it can also watch them for changes. (C: this feature used a lot for monitoring services)</li></ul></li><li><p><strong>Allocating work to nodes</strong></p><ul><li>Useful for single-leader databases, but it’s also useful for job schedulers and similar stateful systems.</li><li>Partitioned resources (database, message streams, file storage, distributed actor system, ect.) and need to decide which partition to assign to which node.</li><li>If done correctly, this approach allows the application to automatically recover from faults without human intervention.</li><li>ZooKeeper runs in a fixed number of nodes (usually three or five) and performs its majority votes among those nodes while supporting a potentially large number of clients.</li><li>If the application state needs to be replicated from one node to another, other tools (such as Apache BookKeeper) can be used.</li></ul></li><li><p><strong>Service discovery</strong></p><ul><li><strong>Zookeeper, etcd</strong>, and <strong>Consul</strong> are also often used for service discovery – that is, to find out which IP address you need to connect to in prder to reach a particular service.</li><li>In cloud data center environments, where it is common for virtual machines to continually come and go, you often don’t know the IP addresses of you services ahead of time.<ul><li>Instead, you can configure you service such that when they start up thet register their network endpoints in a service registry, where they can then be found by other services.</li></ul></li><li>Some consensus systems support read-only caching repilcas.</li></ul></li><li><p><strong>Membership services</strong></p><ul><li><strong>ZooKeeper</strong> and <strong>friends</strong> can be seen as part of a long history of research into membership services.</li><li><strong>A membership service</strong> determines which nodes are currently active and live members of a cluster.</li></ul></li></ul></li></ul><h4 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h4><ul><li>In this chapter we examined the topics of <strong>consistency</strong> and <strong>consensus</strong> form several different angles.<ul><li><strong>Linearizability</strong> (Consistency Model)<ul><li>Its goal is to make replicated data appear as though there were only a single copy, and to make all operations act on it atomically. (Downside: being slow especially when there is large network delays)</li></ul></li><li><strong>Causality</strong> (weaker consistency model)<ul><li>which imposes an ordering on events in a system (what happend before what, based on cause and effect).</li><li><strong>Causal consistency</strong> does not have the coordination overhead of linearizability and is much less sensitive to network problems.</li></ul></li></ul></li><li>We saw that achieving <strong>consensus</strong> means deciding something in such a way that all nodes agree on what was was decided, and such that the decision is irrevocable.<ul><li>Wide range of problems are actully reducible to consensus and are equivalent to each other<ul><li><strong>Linearizable compare-and-set registers</strong>: The register needs to atomically decide whether to set its value, based in whether its current value equals the parameter given in the operation.</li><li><strong>Atomic transaction commit</strong>: A database must decide whether to commit or abort a disrupted transaction.</li><li><strong>Total order broadcast</strong>: The messageing system must decide on the order in which to deliver messages.</li><li><strong>Locks and leases</strong>: When several cilents are racing to grab a lock or lease, the lock decides which one successfully acquired it.</li><li><strong>Membership/Coordination service</strong>: Given a failure detector (e.g., timeouts), the system must decide which nodes are alive, and which should be considered dead because of their sessions timeout out.</li><li><strong>Uniqueness constraint</strong>: When several transactions concurrently try to create conflicting records with the same key, the constraint must decide which one to allow and which should fail with a constraint violation.</li></ul></li><li>How to handle leader-failure<ol><li>wait for the leader to recover, and accept that the system will be blocked in the meantime.</li><li>Manually fail over by getting humans to choose a new leader node and reconfigure the system to use it.</li><li>Use an algorithm to automatically choose a new leader</li></ol></li></ul></li><li>Tools like <strong>ZooKeeper</strong> play an important role in providing an “outsourced” <strong>consensus</strong>, <strong>failure detection</strong>, and <strong>membership service</strong> that application can use.</li><li>Not every system necessarily requires consensus: for example, <strong>leaderless</strong> and <strong>multi-leader</strong> replication systems typically do not use global consensus.</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter 9 Consistency and Consensus&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;In this chapter, we will talk about some examples of algorithms and protocols for building &lt;strong&gt;&lt;u&gt;fault-tolerant distributed systems&lt;/u&gt;&lt;/strong&gt;.&lt;/li&gt;
&lt;li&gt;We will assume that all the problems from Chapter 8 can occur:&lt;ul&gt;
&lt;li&gt;packets can be lost, reordered, duplicated&lt;/li&gt;
&lt;li&gt;Arbitrarily delayed in the network&lt;/li&gt;
&lt;li&gt;Clocks are approximate the best&lt;/li&gt;
&lt;li&gt;and nodes can pause (e.g., due to garbage collection) or crash at any time.&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;The best way of building fault-tolerent system is to find some &lt;strong&gt;general-purpose abstractions with useful guarantees&lt;/strong&gt;, implement them once, and then let applications rely on those guarantees.&lt;/li&gt;
&lt;li&gt;This is the same approach as we used with transactions in Chapter 7: &lt;u&gt;by using a transaction&lt;/u&gt;, the application can pretend:&lt;ul&gt;
&lt;li&gt;that there are no crashes (&lt;strong&gt;atomicity&lt;/strong&gt;),&lt;/li&gt;
&lt;li&gt;that nobody else is concurrently accessing the database (&lt;strong&gt;isolation&lt;/strong&gt;)&lt;/li&gt;
&lt;li&gt;that storage devices are perfectly reliable (&lt;strong&gt;durability&lt;/strong&gt;)&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;u&gt;One of the most important abstractions&lt;/u&gt; for distributed systems is &lt;strong&gt;consensus&lt;/strong&gt;: that is, getting all of the nodes to agree on something.&lt;ul&gt;
&lt;li&gt;&lt;u&gt;Once you have an implementation of consensus, applications can use it for various purposes&lt;/u&gt;.&lt;/li&gt;
&lt;li&gt;Correct implementations of consensus help avoid problems.&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记10</title>
    <link href="http://yoursite.com/2020/08/26/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B010/"/>
    <id>http://yoursite.com/2020/08/26/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B010/</id>
    <published>2020-08-26T23:44:38.000Z</published>
    <updated>2020-09-22T16:55:40.570Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter8 The Trouble with Distributed System</strong></p><ul><li>this chapter is a thoroughly pessimistic and depressing overview of things that may go wrong in a distributed system.<ul><li>Network issues</li><li>Clocks &amp; timing issues</li></ul></li></ul><a id="more"></a><h4 id="Faults-and-Partial-Failures"><a href="#Faults-and-Partial-Failures" class="headerlink" title="Faults and Partial Failures"></a>Faults and Partial Failures</h4><ul><li>Single machines software is <strong><u>deterministic</u></strong><ul><li>An individual computer with good software is usually either fully functional or entirely broken, but not something in between.</li></ul></li><li>In distributed system, we are no longer operating in an idealized system model – we have no choice but to confront the messy reality of the physical world.</li><li>The difficulty is that <strong>partial failures</strong> are <strong>non-deterministic</strong>: if you try to do anything involving multiple nodes and the network, it may sometimes work and sometimes unpredictably fail.<ul><li><u>This <strong>non-determinism</strong> and possibility of <strong>partial failures</strong> is what makes distributed systems hard to work with</u>.</li></ul></li><li><strong>Cloud-Computing and Super-Computing</strong>: (C: Vertical scaling and Horizontal Scaling)<ul><li>High-performance computing (HPC) vs Cloud Computing <ul><li>Supercomputer is more like a single-node computer than a distributed system: it deals with partial failure by letting it escalate into total failure.</li></ul></li><li>If we want to make distributed system work, <u>we must accept the possibility of partial failure and fault-tolerance mechanisms into the software</u>.<ul><li>build a reliable system from unreliable components.</li></ul></li><li>Even in smaller systems consisting of only a few nodes, it’s important to think about partial failure.</li><li>The <u>fault handling must be part of the software design</u>, and you (as operator of the software) need to know what behavior to expect from software in the case of a fault.<ul><li>Important to consider a wide range of possible faults – even fairly unlikely ones.</li></ul></li><li><u>In distributed systems, <strong>suspicion, pessimism</strong>, and <strong>paranoia</strong> pay off</u>.</li></ul></li></ul><h4 id="Unreliable-Networks"><a href="#Unreliable-Networks" class="headerlink" title="Unreliable Networks"></a>Unreliable Networks</h4><ul><li>the <u>distributed systems</u> we focus on in this book are <strong>shared-nothing systems</strong>: i.e., a bunch of machines connected by a network.<ul><li>Why ?<ul><li>It’s comparatively cheap because it requires no special hardware;</li><li>it can make use of commoditized cloud computing services;</li><li>it can achieve high reliability redundancy across multiple geographically distributed datacenters.</li></ul></li></ul></li><li>The internet and most interval networks in data centers (often Ethernet) are asynchronous packet networks. If you send a request and expect a response, many things could go wrong. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598455635/Design Data Intensive Application/p2-ch8-img1_hz1atv.png" alt="p2-ch8-img1" style="zoom:50%;" /><ul><li>If you send a request to another node and don’t get a response, it is impossible to tell why.<ul><li>One solution: use Timeout.</li></ul></li></ul></li><li><strong>Network Faults on Practice</strong><ul><li>Nevertheless, nobody is immune from network problems. Handling network faults doesn’t necessarily mean tolerating them.</li><li>You do need to know how you software reacts to network problems and ensure that the system can recover from them.<ul><li><strong><u>Chaos Monkey</u></strong> (e.g. Netflix) is needed to test this “<strong>Reliability</strong>“</li></ul></li></ul></li><li><strong>Detecting Faults</strong>: (C : <strong><u>Gossip</u></strong> protocal, end request/signal across each other and then broadcasting; or <strong>ZooKeeper</strong>)<ul><li>Many systems need to automatically detect fault nodes.</li><li>Unfortunately, the uncertainty about the network makes it difficult to tell whether a node is working or not.</li></ul></li><li><strong>Timeouts and Unbounded Delays</strong><ul><li>If a timeout is the only sure way of detecting a fault, then how long should the timeout be?<ul><li>There is unfortunately no simple answer.</li></ul></li><li><strong>Prematurely declaring a node is problematic</strong><ul><li>If the node is actually alive in the middle of performing some action (for example, sending an email), and another node takes over, the action may end up being performed twice.</li><li>When a node is declared dead, its responsibilities need to be transferred to other nodes, which places additional load on other nodes and network.</li></ul></li><li><strong>Asynchronous network have unbounded delays</strong> (that is, they try to deliver packets as quickly as possible, but there is no upper limit on the time it may take for a packet to arrive)</li><li><strong>Networking congestion and queueing</strong><ul><li>The variability of packet delays on computer networks is most often due to queueing. (e.g. Network, CPU, VMs, TCP flow-control etc.)</li><li><strong>TCP</strong> considers a packet to be lost if it is not acknowledged within some timeout (which is calculated from observed round-trip times), and lost packets are automatically retransmitted.</li><li><strong>TCP vs. UDP</strong> (C: delayed message meaningless/worthless –&gt; UDP)<ul><li>Trade-off between <u>reliability</u> and <u>variability of delays</u>: as UDP does not perform flow control and does not retransmit lost packets.</li></ul></li><li>You can only choose timeouts experimentally.</li><li>Systems can continually measure response times and their variability (jitter), and automatically adjust: <u>Phi Accrual failure detector</u>, which is used for example in <u>Akka</u> and <u>Cassandra</u>. TCP retransmission timeouts also work similarly.</li></ul></li></ul></li><li><strong>Synchronous vs. Asynchronous Networks</strong><ul><li>Phone call networks is <strong>synchronous</strong>: even as data passes though several routers, it does not suffer from queueing, because the 16 bits of space for the call have already been <strong>reserved</strong> in the next hop of the network. <ul><li><strong><u>bounded delay</u></strong>: No queueing, the maximum end-to-end latency of the network is fixed.</li></ul></li><li>Can we not simply make network delays predictable?<ul><li>Ethernet and IP are <u>packet-switch protocals</u>, which suffer from queueing and thus unbounded delays in the network. These protocols do not have the concept of a circuit.</li><li>Why do datacenter networks and the internet use packet switching?<ul><li>The answer is that they are <strong>optimized for bursty traffic</strong>.</li></ul></li></ul></li><li>With careful use of <u>quality of service</u> (<u>Qos</u>, prioritization and scheduling of packets) and <u>admission control</u> (rate-limiting senders), it is possible to emulate circuit switching on packet networks, or provide statistically bounded delay.</li><li><strong>Latency and Resource Utilization</strong><ul><li>Phone line: resource is divided up in a static way</li><li>Internet: shares network bandwidth dynamically <ul><li>This approach has downside of queueing, but the advantage is that it <strong>maximize utilization of the wire</strong></li><li>A similar situation arises with COUs.</li></ul></li><li><strong>Latency guarantees</strong> are achievable in certain environments, if resources are statically partitioned (e.g., dedicated hardware and exclusive bandwidth allocations).<ul><li>Comes at the cost of reduced utilization </li></ul></li><li><strong>Variable delays</strong> in networks are not a law of nature, but simply the result of a <strong>cost/benefit trade-off</strong>.</li></ul></li></ul></li></ul><h4 id="Unreliable-Clocks"><a href="#Unreliable-Clocks" class="headerlink" title="Unreliable Clocks"></a>Unreliable Clocks</h4><ul><li><p><strong>Clocks and Time</strong> are important. Applications depend on clocks in various ways.</p></li><li><p>In a distributed system, time is a tricky business, because communication is not instantaneous.</p></li><li><p>Each machine on the network has its own clock, which is an actual hardware device: usually a <strong><u>quartz crystal oscillator</u></strong>. But these devices are not perfectly accurate, so each machine has its own notion of time.</p><ul><li>It is possible to synchronize clocks to some degree: the most commonly used mechanism is <strong><u>Network Time Protocol (NTP)</u></strong></li></ul></li><li><p><strong>Monotonic vs. Time-of-Day Clocks</strong>: Modem computers have at least two different kinds of clocks: a <strong>time-of-day</strong> clock and a <strong>monotonic</strong> clock.</p><ul><li><strong>Time-of-day clocks</strong>: it returns the current data and time according to some calendar (also known as well-clock time)<ul><li>Time-of-day clocks are usually synchronized with NTP, which means that a timestamp from one machine (ideally) means the same as a timestamp on another machine.</li></ul></li><li><strong>Monotonic clocks</strong>: is suitable for <u>measureing a duration (time interval</u>, such as a timeout or a service’s response time.<ul><li>They are <strong>guaranteed to always move forward</strong> (whereas a <u>time-of-day</u> clock may jump back in time).</li><li>NTP may adjust the frequncy at which the monotonic clock moves forward (this is known as slewing the clock) if it detects that the computer’s local quarz is moving faster or slower that the NTP server.</li></ul></li></ul></li><li><p><strong>Clock Synchronization and Accuracy</strong></p><ul><li>Monotonic clocks don’t need synchronization, but time-of-day clocks need to be set according to an <strong>NTP</strong> server or other external time source in order to be useful.<ul><li>Hardware clocks and NTP can be fickle beast.</li></ul></li><li>It is possible to achieve very good clock accuracy if you care about it sufficiently to invest significant resources.<ul><li>E.g. using GPS receivers, the Precision Time Protocal (PTP), and careful deployment and monitoring.</li></ul></li></ul></li><li><p><strong>Relying on Synchronized Clocks</strong></p><ul><li><p>Robust software needs to be prepared to deal with incorrect clocks.</p><ul><li>Incorrect clocks easily go unnoticed.</li></ul></li><li><p>If some piece of software is relying on an accurately synchronized clock, the result is <u>more likely to be silent and subtle data loss than a dramatic crash</u>.</p></li><li><p>Thus, if you use software that requires synchronized clocks, it is essential that you also carefully monitor the clock offsets between all the machines.</p></li><li><p><strong>Timestamps for ordering events</strong></p><ul><li><p>Consider one particular situation in which it is tempting, but dangerous, to rely on clocks: ordering of events across multiple nodes. E.g. (LWW) <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598481999/Design Data Intensive Application/p2-ch8-img2_esujdx.png" alt="p2-ch8-img2" style="zoom:50%;" /></p></li><li><p>This conflict resolution strategy is called <u>last write wins (LWW)</u>, and it is widely used in bother multi-leader replication and leaderless databases such as <u>Cassandra</u> and Riak. (C: write to cache first?)</p><ul><li>DB writes can mysteriously disappear;</li><li>LWW can’t distinguish between writes that occurred sequentially in quick succession</li><li>It is possible for two nodes to independently generate writes with the same timestamp.</li></ul></li><li><p>For correct ordering you would <strong>need the clock source to be significantly more accurate than the thing you are measuring</strong> (namely network delay)</p></li><li><p>So-called <strong><u>logical clocks</u></strong>, which are based on <u>incrementing counters</u> rather than an oscillating quartz crystal, are a safer alternative for ordering events.</p><ul><li><u>Logical clocks only make sure the relative ordering of events</u> (whether one even happened before or after another)</li></ul></li></ul></li><li><p><strong>Clock readings have a confidence interval</strong></p><ul><li>Thus, it doesn’t make sense to think of a clock reading as point in time – it is more like a <strong>range of times</strong>, within a confidence interval.</li><li>Google’s <u>TrueTime API</u> in <u>Spanner</u>, which explicitly reports the confidence interval on the local clock.<ul><li>When you ask it for the cuncurrent time, you get back two values: [earlist, latest]</li></ul></li></ul></li><li><p><strong>Synchronized clocks for global snapshots</strong></p><ul><li>With lots of small, rapid transactions, creating transaction IDs in a distributed system becomes an untenable bottleneck.</li><li>Can we use the timestamps from synchronized time-of-day as transaction IDs?<ul><li>If we could get the synchronization good enough, they would have the right properties: later transactions have a higher timestamp.</li></ul></li><li>Spanner needs to keep the clock uncertainty as small as possible.<ul><li>For this purpose, <u>Google deploys a <strong>GPS receiver or atomic clock</strong> in each datacenter, allowing clocks to be synchronized to within about <strong>7 ms</strong></u>.</li></ul></li></ul></li><li><p><strong>Process Pauses</strong></p><ul><li>Is it crazy to assume that a thread might be paused for so long? Unfortunately not. E.g.<ul><li>Many programming language runtimes (such as the Hava Virtual Machine) have a <u>garbage collector (GC)</u> that occasionally needs to stop all running threads.</li><li>In virtualized environments, a virtual machine can be suspended  (pausing the execution of all processes and saving the contents of memory to disk) and resumed (restoring the contents of memory and continuing execution).</li><li>If the application performs synchronous disk access, a thread may be paused waiting for a slow disk I/O operation to complete.</li></ul></li></ul></li><li><p><strong>Response time guarantee</strong></p><ul><li>In some systems, there is a specific deadline by which the software must respond; if it doesn’t meet the deadline, that may cause a failure of the entire system. These are so-called <strong>Hard real-time systems</strong>.</li><li>A <strong>real-time operating system (RTOS)</strong> that allows processes to be scheduled with a guaranteed allocation of CPU time in specified intervals is needed.</li><li>For most server-side data processiong systems, real-time guarantees are simply not ecnomical or appropriate.</li></ul></li><li><p><strong>Limiting the impact or garbage collection</strong></p><ul><li><strong>An emerging idea is to treat GC pauses like brief planned outages</strong> of a node, and to let other nodes handle requests from clients while one node is collecting its garbage.</li></ul></li></ul></li></ul><h4 id="Knowledge-Truth-and-Lies"><a href="#Knowledge-Truth-and-Lies" class="headerlink" title="Knowledge, Truth, and Lies"></a>Knowledge, Truth, and Lies</h4><ul><li><p>For Distributed Systems: there is <strong>no shared memory</strong>, only <strong>message passing</strong> via an unreliable network with variable delays, and the system may suffer from <u>partial failures, unreliable clocks, and processing pauses</u>.</p><ul><li>A node in the network cannot know anything for sure – it can only make guesses based on the messages it receives (or doesn’t receive) via network.</li><li>A node can only find out what state another node is in (what data it has stored, whether it is correctly functioning, etc.) by exchanging messages with it.</li></ul></li><li><p>Although it is possible to make software well behaved in an unreliable system model, it is not straightforward to do so.</p></li><li><p><strong>The Truth Is Deifned by Majority</strong></p><ul><li><p>Netwrok with an <u>asymmetric fault</u>: a node is able to receive all messages sent to it, but any outgoing message from that node are dropped or delayed.</p></li><li><p><u>In a distributed system a node cannot necessarily trust its own judgment of a situation</u>.</p></li><li><p>Instead, many distributed algorithms rely on a <strong>Quorum</strong>, that is, voting among the nodes: <u>decisions require some minimum number of votes from several nodes in order to reduce the dependence on any one particular node</u>.</p><ul><li>Most commonly, the quorum is an absolute <strong>majority of more than half the nodes</strong>.</li></ul></li><li><p><strong>The leader and the clock</strong></p><ul><li><img src="https://res.cloudinary.com/shuaiz/image/upload/v1598481916/Design Data Intensive Application/p2-ch8-img3_h3rahr.png" alt="p2-ch8-img3" style="zoom:50%;" /></li></ul></li><li><p><strong>Fencing tokens</strong></p><ul><li><p>we need to ensure that a node that is under a false belief of being “the chosen one” cannot disrupt the rest of the system. A fairly simple technique that achieves this goal is called <strong>fencing</strong></p><p>  . <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598482200/Design Data Intensive Application/p2-ch8-img4_wbr7xt.png" alt="p2-ch8-img4" style="zoom:50%;" /></p><ul><li>If ZooKeeper is used as a lock service, the transaction ID zxid or the node version cversion can be used as a fencing token.</li></ul></li></ul></li></ul></li><li><p><strong>Byzantine Faults</strong></p><ul><li>Distributed system problems become much harder if there is a risk that nodes may “<strong>lie</strong>“ (send arbitrary fault or corrupted response)<ul><li>E.g. if a node may claim to have received a particular message when in fact it didn’t.  (Such behaviors is knwo as  a <strong>Byzantine fault</strong>, and the problem of reaching cosensues in this untrusting environment is known as the <strong><u>Byzantine Generals Problem</u></strong>)</li></ul></li><li>A system is <strong>Byzantine fault-tolerant</strong> if it continues to operate correctly even if some of the nodes are malfunctioning and not obey the protocal, or if malicious attackers are interfering with the network.</li><li><strong>Weak forms of lying</strong><ul><li>It can be worth adding mechanisms to software that guard against weak forms of “lying”<ul><li>E.g. invalid messages due to hardware issues, software bugs, and misconfigurations.</li></ul></li><li>Solutions: e.g.<ul><li><strong><u>checksum</u></strong> in the application-level protocal;</li><li>A publicly accessible application must carefully <u><strong>sanitize</strong></u> any inputs from users;</li><li>NTP clients can be configured with multiple server addresses.</li></ul></li></ul></li></ul></li><li><p><strong>System Model and Reality</strong></p><ul><li>Many algorithms have been designed to solve distributed system problems <ul><li>Algorithms need to be tolerate various faults of distributed system that we discussed in this chapter.</li><li>Algorithms need to be written in a way that does not depend too heavily on the details of the hardware and software configuration on which they are run.</li></ul></li><li>This is requires that we somehow formalize the kinds of <u>fault that we expect to happen in a system</u>. We do this by defining a <strong>system model</strong>, which is an abstraction that describes what things an algorithm may assume.</li><li><strong>Timing assumptions</strong>, three system models are in common use<ul><li><strong>Synchronous</strong> model: assumes bounded network delay, bounded process pauses, and bounded clock error.</li><li><strong>Partially synchronous</strong> model: means that a system behaves like a synchronous system most of the time, but it sometimes exceeds the bounds for netwrok delay, process pauses, and clock drift.</li><li><strong>Asynchronous</strong> model: an algorithm is not allowed to make any timing assumptions– in fact, it does not even have a clock (so it cannot use timeouts)</li></ul></li><li><strong>Node failures</strong>. The three most common system models of nodes are<ul><li><strong>Crash-stop faults</strong>: an algorithm may assume that the node can fail in only one way, namely by crashing.</li><li><strong>Crash-recovery faults</strong>: We assume that nodes may crash at any moment, and perhaps start responding again after some unknown time.</li><li><strong>Byzantine (arbitrary) faults</strong>: Nodes may do absolutely anything, including trying to trick and deceive other nodes.</li></ul></li><li>For modeling real systems, the <u>partially synchronous model</u> with <u>crash-recovery faults</u> is generally <strong>the most useful model</strong>.</li><li><strong>Correctness of an algorithm</strong><ul><li><strong>An algorithm is correct in some system model</strong> if it always satisfies its properties in all situations that we assume may occur in that system model.</li></ul></li><li><strong>Safety and Liveness</strong><ul><li>What distinguishes the two kinds of properties?<ul><li>A giveaway is that <u>liveness</u> properties often include the word <strong>“eventually”</strong> in their defination. (And yes, you guessed it – eventual consistency is a liveness property.)</li><li><strong>Safety</strong>: is often informally defiend as <strong><u>nothing bad happens</u></strong></li><li><strong>Liveness</strong>: as something <strong><u>good eventually happens</u></strong>.</li><li>In the example, <strong>uniqueness</strong> and <strong>monotonic sequence</strong> are <u>safety</u> properties, but <strong>availability</strong> is a <u>liveness</u> property.</li><li><strong>If a safety property is violated</strong>, we can point at a particular point in time at which it was broken (e.g., if the uniqueness properly was violated, we can identify the particular operation in which duplicate fencing token were returned)<ul><li>After a safety property has been violated, the violation cannot be undone – the damage is already done.</li></ul></li><li><strong>A liveness property works the other way round</strong>: it may not hold at some point in time (e.g., a node may have sent a request but not yet received a response)<ul><li>but there is always hope that it may be satisfied in the future (namely by receiving a response).</li></ul></li><li>An advantage of distinguishing between <strong>safety</strong> and <strong>liveness</strong> properties is that it helps us deal with difficult system models.</li></ul></li></ul></li><li><strong>Mapping system models to the real world</strong><ul><li><strong>Abstrct system models</strong> are incredibly helpful for distilling down the complexity of read systems to a manageable set of faults that we can reason about, so that we can understand the problem and try to solve it systematically.</li><li>Provding an algorithm correct does not mean its implementation on a real system will necessarily always behave correctly.</li></ul></li></ul></li></ul><h4 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h4><ul><li>In this chapter we have discussed a wide range of problems that can occur in distributed systems.<ul><li>Network delay;</li><li>Node out of sync (even with NTP)</li><li>Pause of execution (maybe due to GC)</li></ul></li><li>Such <strong>partial failures</strong> can occur is the difining <u>characteristic</u> of distributed systems.</li><li>In distributed systems, we try to build tolerance of partial failures into software, so that the system as a whole may continue functioning even when some of its constituent parts are broken.</li><li>To tolerate faults, the first step is to detect them, but even that is hard.</li><li>Once a fault is detected, making a system tolerate it is not easy either: there is no global variable, no shared memory, no common knowledge or any other kind of shared state between the machines. (e.g. Solution: Quorum to agree)</li><li>If you can avoid opening Pandora’s box and simply keep things on a single machine, it is generally worth doing so. </li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter8 The Trouble with Distributed System&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;this chapter is a thoroughly pessimistic and depressing overview of things that may go wrong in a distributed system.&lt;ul&gt;
&lt;li&gt;Network issues&lt;/li&gt;
&lt;li&gt;Clocks &amp;amp; timing issues&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记9</title>
    <link href="http://yoursite.com/2020/08/25/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B09/"/>
    <id>http://yoursite.com/2020/08/25/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B09/</id>
    <published>2020-08-25T16:13:27.000Z</published>
    <updated>2020-09-10T22:29:01.126Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter7 Transactions</strong></p><ul><li>A <strong>transaction</strong> is a way for an application to group several reads and writes together into a logical unit.<ul><li>Conceptually, all the reads and writes in a transaction are executed as one operation: <u>either that entire transaction succeeds (commit) or it fails (abort, rollback)</u>.</li></ul></li><li>By using transactions, the application is free to ignore certain potential error scenarios and concurrency issues, because the database takes care of them instead (we call these <u>safety quarantees</u>)<ul><li>Vs. <strong>higher performance</strong> or <strong>higher availability</strong></li></ul></li><li><strong>How do you figure out whether you need transactions?</strong><ul><li>In order to answer that question, we first need to understand exactly what <u>safety quanrantees transactions</u> can provide, and what costs are associated with them.</li></ul></li></ul><a id="more"></a><h4 id="The-slippery-Concept-of-a-Transaction"><a href="#The-slippery-Concept-of-a-Transaction" class="headerlink" title="The slippery Concept of a Transaction"></a>The slippery Concept of a Transaction</h4><ul><li>Almost all relational databases today, and some non-relational databases, support transactions. E.g. MySQL, MSSQL, Oracle, PostgreSQL etc.</li><li><strong>The meaning of ACID</strong><ul><li>The <u>safety guarantees</u> provided by <strong>transactions</strong> are often described by the well-known acronym ACID, which stands for <strong>Atomicity, Consistency, Isolation and Durability</strong>.</li><li>System that do not meet the ACID criteria are sometimes called <strong>BASE</strong>, which stands for <u><strong>Basically Available, Soft state, and Eventual consistency</strong>.</u><ul><li>This is even more vague than the defination of ACID</li></ul></li><li><strong>Atomicity</strong>: atomic refers to something that cannot be broken down into smaller parts.<ul><li>If the writes are grouped together into an <strong>automic transaction</strong>, and the transaction cannot be completed (committed) due to a fault, then the transaction is aborted and the database must discard or undo any writes it has make so far in that application.</li></ul></li><li><strong>Consistency</strong>: refers to an application-specific notion of the database being in a “good state”<ul><li>You have certain statements about your data (invariants) that must always be true – e.g. In an accounting system, credits and debits across all acounts must be balanced.<ul><li>However, this depends on the application’s notion of invariants.</li></ul></li><li><strong><u>Atomicity, isolantion and durability</u></strong> are properties of the databases, whereas <strong><u>consistency</u></strong> (in the ACID sense) is <u>a property of the application</u>.</li></ul></li><li><strong>Isolation</strong>: concurrently executing transactions are isolated from each other: they cannot step on each other’s toes. (Old term: serializability)<ul><li>In practice, <u>serializable isolation</u> is rarely used, because it carries a performance penalty.</li></ul></li><li><strong>Durability</strong>: the promise that once a transaction has committed successfully, any data it has written will not be forgotten, even if there is a hardware fault or the database crashes.<ul><li><strong>In a single-node database</strong>, durability typically means that the data has been written to non-volatile storage such as a hard driver or SSD.</li><li><strong>In a replicated database</strong>, durability typically means that the data has been successfully copied to some number of nodes.</li><li><u>perfect durability does not exits</u>. (e.g. What if earth were destroyed)</li></ul></li><li><strong>Replication and Durability</strong><ul><li>In practice, there is not one technique that can provide absolute guarantees. There are only various risk-reduction techniques, including writing to disk, replicating to remote machines, and backups – and they can and should be used together.</li></ul></li></ul></li><li><strong>Single-Object and Multi-Object Operations</strong><ul><li><u>Multi-object transactions</u> are often needed if several pieces of data need to be kept in sync. (e.g. Email app, with “unread” counter, a way of the normalization.)</li><li><strong>Violating isolation</strong>: one transaction reads another transaction’s  uncommitted writes. (A “dirty read”)</li><li><strong>Multi-object transactions</strong> require some way determining which read and write operations belong to the same transaction.<ul><li>On the other hand, many non-relational databases don’t have such a way of grouping operations together.</li></ul></li><li><strong>Single-Object writes</strong><ul><li>Increment operation, which removes the need for a read-modify-write cycle.</li><li><strong>Compare-and-set</strong> and other single-object operations have been dubbed “light-weight transactions” or even “ACID” for marketing purposes</li></ul></li><li><strong>The need for multi-object transactions</strong><ul><li>Many distributed datastores have abondoned multi-object transactions because they are difficult to implement across partitions, and they can get in the way in some scenarios where every high availability or performance is required.</li><li><u>In a relational data model</u>, a row in one table often has a foreign key reference to row in another tabel.</li><li><u>In a document data model</u>, the fields that need to be updated together are often within the same document, which is treated as a single object </li><li><u>In databases with secondary indexes</u> (almost everything except pure key-value stores), this indexes also need to be updated every time you change a  value.</li></ul></li></ul></li><li><strong>Handle errors and aborts</strong><ul><li>A key feature of a transaction is that it can be <u>aborted and safely retried</u> if an error occurred.<ul><li>If the database is in danger of violating its guarantee of atomicity, isolation or eurability, it would rather abandon the transaction entirely that allow it to remain half finished.</li><li>But datastores with leaderless replication won’t obey such rules but work much more on a “best effort” basis, so it’s the application’s responsibility to recover from errors.</li></ul></li><li>Although retrying an aborted transactions is a simple and effective error handling mechanism, it isn’t perfect.</li></ul></li></ul><h4 id="Weak-Isolation-Levels"><a href="#Weak-Isolation-Levels" class="headerlink" title="Weak Isolation Levels"></a>Weak Isolation Levels</h4><ul><li><p>if two trasactions don’t touch the same data, they can safely be run in parallel, because neither depends on the other.</p></li><li><p>Databases have long tried to hide concurrency issues from application developers by providing <strong>transaction isolation</strong>.</p><ul><li><strong>serializable isolation</strong> means that the database guarantees that <u>transactions have the same effect as if they ran serially</u>(i.e., one at a time, without any concurrency)</li></ul></li><li><p><strong>Serializable isolation</strong> has a performance cost, and many databases don’t want to pay that price.</p><ul><li>Therefore common for systems to use <u>weaker levels of isolation</u>, which protect against some concurrency issues, but not all.</li></ul></li><li><p>Rather than blindly relying on tools, we need to develop a good understanding of the kinds of concurrency problems that exist, and how to prevent them.</p></li><li><p><strong>Several <u>weak (non-serializable) isolation</u> levels that are used in practice</strong></p><ul><li><strong>Read committed</strong></li><li><strong>Snapshot Isolation and Repeatable Read</strong></li><li><strong>Preventing Lost Updates</strong></li><li><strong>Write Skew and Phantoms</strong></li></ul></li><li><p><strong>Read Committed</strong>: the most basic level if transaction isolation with two guarantees (default setting in Oracle 11g, PostgreSQL, SQL Server 2012, MemSQL and many other databases)</p><ul><li>When reading from database, you will only see data that has been commiteed (<u>no dirty reads</u>)</li><li>when writing to the databases, you will only overwrite data has been committed (<u>no dirty writes</u>)</li><li><strong>No dirty reads</strong><ul><li>Imagine a transaction has written some data to the database, but the traesaction has not yet committed or aborted. Can another transaction see that uncommitted data? If yes, that is called a dirty read.</li></ul></li><li><strong>No dirty Writes</strong><ul><li>what happens if the earlier write is part of a transaction that has not yet committed, so the later write overwrites an uncommitted value? This is called called a dirty write.</li><li>by delaying the second write until the first write’s trasactions has committed or aborted.</li></ul></li><li><strong>Implementing read committed</strong><ul><li>Database <strong><u>prevent dirty writes</u></strong> by using <strong><u>row-level locks</u></strong>: when a transaction wants to modify a particular object (row or document), it must first acquire a lock on that object.</li><li>Most databases  <strong><u>prevent dirty reads</u></strong> using the approach: for every object that is written, the database <u>remembers both the old commited value and the new value set by the trasaction</u> that currently holds the write lock.</li></ul></li></ul></li><li><p><strong><u>Snapshot Isolation</u> and <u>Repeatable Read</u></strong>: (it is supported by PostgreSQL, MySQL with the InnoDB storage engine, Oracle, SQL Server, and others)</p><ul><li>Snapshot isolation is a popular feature</li><li><strong>Non-repeatable read  or read skew</strong>: if Alice were to read the balance of account 1 again at the end of the transaction, she would see a different value ($600) than she saw in her previously query.</li><li><strong>Snapshot isolation</strong> is the most common solution to this problem. The idea is that each trasaction reads from a <strong><u>consistent snapshot</u></strong> of the database – that is, the transaction sees all the data that was committed in the database at the start of the transaction. (C: simillar with Version Vector?)<ul><li>Even if the data is subsequently changed by another transaction, each transaction sees only the old data from that particular point in time.</li></ul></li><li><strong>Snapshot Isolation</strong> is a boon <u>for long-running, read-only queries</u> such as backup and anylatics.</li><li><strong>Implementing snapshot isolation</strong><ul><li>Like read commited isolation, implementations of snapshot isolation typically use write locks to prevent dirty wirtes.<ul><li>Write can block the process of another transaction that writes to the same object.</li><li>However, <u>reads do not require any locks</u>.</li></ul></li><li>From a performance point of view, a key principle of snapshot isolation is <u>readers never block writers, and writers never block readers</u>.</li><li>Because it maintains several version of an object side by side, this technique is knwo as <strong>Multi-version Concurrency Control (MVCC)</strong>.</li></ul></li><li><strong>Visibility rules for observering a consistent snapshot</strong><ul><li>when a transaction reads from database, transaction IDs are used to decide which objects it can see and which are invisible</li><li>An object is visible if both of the following conditions are true:<ul><li>At the time when the reader’s transaction started, the transaction that created the obejcts had already committed.</li><li>The object is marked for deletion, or if it is, the transaction that reqeusted deletion had yet not committed at the time when the reader’s transaction started.</li></ul></li><li>By never updating values in place but instead creating a new version every time a value is changed, the database can provide a consistent snapshot while incurring only a small overhead.</li><li><strong>Indexes and snapshot isolation</strong><ul><li><u>append-only/copy-on-write</u> variant that does not overwrite pages of the tree when they are updated, but instead creates a new copy of each modified page.</li></ul></li></ul></li><li><strong>Repeatbale read and naming confusion</strong><ul><li>Many databases that implement it call it by different names. In Oracle it is called serializable, and in PostgreSQL and MySQL, it is called repeateable read.</li><li>It defines repeatable read, which looks superficially similar to snapshot isolation. (C: Because the concept of “snapshot-isolation” haven’t invented yet back then)</li><li>As a result, nobody really knows what repeatable read means.</li></ul></li></ul></li><li><p><strong>Preventing Lost Updates</strong></p><ul><li>There are several other interesting kinds of conflicts that can occur between concurrently writing trasactions. The best knwos of there is the <strong>lost updates</strong> probelm.</li><li><strong>The lost update problem can occur</strong> if an application <u>reads some value from the database, modifies it and writes back the modified value (a read-modify-wirte cycle)</u><ul><li>If two transactions do this concurrently, one of the modifications can be lost, because the second write does not include the first modification.</li><li>E.g.<ul><li>Incrementing a counter or updating an account balance</li><li>making a local change to a complex value</li><li>Two users editing a wiki page at the same time.</li></ul></li></ul></li><li><strong>Atomic write operations</strong>: (e.g. MongoDB, Redis)<ul><li>Many databases provide <u>atomic update operations</u>, which remove the need to implement read-modify-write cycels in application code.<ul><li>Not all writes can easily be expressed in terms of atomic operations – for example, updates to a wiki page involve arbitrary text editing.</li></ul></li><li><strong>cursor stability</strong>: Atomic operations are usually implemented by taking an exclusive lock on the object when it is read so that no other transaction can read it until the update has been applied.<ul><li>Another option is to simply force all atomic operations to be executed on a <u>single thread</u>.</li></ul></li></ul></li><li><strong>Explicit locking</strong><ul><li>If the databses’ built-in atomic operations don’t provide the necessary functionality, it is for the <u>application to explicitly lock objects that are going to be updated</u>.<ul><li>E.g. The <strong>FOR UPDATE</strong> clause indicates that the database should take a lock on all rows returned by this query.</li></ul></li></ul></li><li><strong>Automatically detecting lost updates</strong>: (e.g. PostgreSQL, Oracle, SQL)<ul><li>An alternative is to allow them to execute in parallel and, if the transaction manager detects a lost update, abort the transaction and force it to retry its read-modify-write cycle.</li></ul></li><li><strong>Compare-and-set</strong><ul><li>In databases that don’t provide transactions, you sometimes find an atomic <strong>compare-and-set</strong> operation<ul><li>Avoid lost updates by allowing an update to happen only if the value has not changed since you last read it.</li></ul></li></ul></li><li><strong>Conflict resolution and replication</strong><ul><li>Locks and compare-and-set operations assume that there is a single up-to-date copy of the data. Thus techniques based on locks or compare-and-set do not apply in this context.</li><li><strong>Solution</strong>: allow concurrent writes to create several conflicting versions of a value (also known as siblings), and to use application code or special data structures to resolve and merge these versions after the fact.</li><li>The last write wins (LWW) conflict resolution method is <u>prone to lost updates</u> and unfortunately, LWW is the default in many replicated databases.</li></ul></li></ul></li><li><p><strong>Write Skew and Phantoms</strong> (e.g. Hospital doctor on call schedule App)</p><ul><li><strong>Write skew</strong>. it is neither a dirty write nor a lost update, because the two transactions are updating two different objects (Alice’s and Bob’s on-call schedule, respectively)<ul><li>You can think of <strong>Write skew</strong> as a generalization of the lost update problem.</li></ul></li><li><strong>Write skew</strong> can occur if two trasactions read the same objects, and then update some of those objects (differently trasactions may update different objects)<ul><li>Automatically preventing write skew requires true serializable isolation.</li><li>The second-best option in this case is probably to explicit lock the rows that the transaction depends on.</li></ul></li><li><strong>More examples of write skew</strong><ul><li>Meeting room booking system;</li><li>Multiplayer game;</li><li>Claming a username;(C: reason why website tend to use Email as username, because Email usually guarantee to be unique) Unique constraint could solve this problem.</li><li>Preventing double-spending</li></ul></li><li><strong>Phantoms causing Write skew</strong><ul><li>Where a write in one transation chanegs the result of a search query in another transaction, is called a phantom.</li></ul></li><li><strong>Materializing conflics</strong>: (last resort, a serializable isolation level is much preferable in most cases)<ul><li>If the problem of phantiom is that there is no object to which we can attach the locks, perhaps we can artificially introduce a lock object into the database?</li><li>This approach is called <strong>materializing conflicts</strong>, because it takes a phantom and turns it into a lock conflict on a concrete set of rows that exist in the database.</li></ul></li></ul></li></ul><h4 id="Serializability"><a href="#Serializability" class="headerlink" title="Serializability"></a>Serializability</h4><ul><li><strong>Serializable isolation</strong> is usually regarded as the <u>strongest isolation</u> level.<ul><li>it guarantees that even though transactions may executed in parallel, the end result is the same as if they had executed one at a time, serially, without any concurrency.</li></ul></li><li>Three techniques of implementation: (Focus one node in this chapter)<ul><li>Literally executing transactions in a <strong>Serial-order</strong></li><li><strong>Two-phase locking</strong>, which for several decades was the only viable option;</li><li><strong>Optimistic concurrency control</strong> techniques such as <strong>serializable snapshot isolation</strong></li></ul></li><li><strong>Actual Serial Execution</strong>: (e.g. Redis, VoldDB/H-store, Datomic)<ul><li>The simplest way of avoiding concurrency problems is to <strong>remove the concurrency entirely</strong>: to execute only one transaction at a time, in serial order on a single thread.</li><li>Two reasons why this starting pick momentum late until 2007<ul><li>RAM became cheap enough that for many use cases is now feasible to keep the entire active dataset in memory</li><li>Database designers realized that OLTP transactions are usually short and only make a small number of reads and writes.</li></ul></li><li>Sometimes perform better than a system that supprort concurrency, because it can avoid the coordination overhead of locking, but its throughput is limited to that of a single CPU core.</li><li><strong>Encapsulating transactions in stored procedures</strong><ul><li>System with single-threaded serial transaction processing don’t allow interactive multi-statement transactions.<ul><li>Instead, the application must submit the entire transaction code to the database ahead of time as a <strong>stored procedure</strong></li></ul></li><li><strong>Pros and cons of stored procedures</strong><ul><li>Each database vender has its own language for stored procedures (Oracle has PL/SQL, SQL Server has T-SQL, PostgreSQL has PL/pgSQL, etc.)</li><li>Code running in a database is difficult to manage: compared to an application server.</li><li>A database is often much more performance-sensitive than an application server, because a single database instance is often shared by many application servers. (C: wider “negative” impact if a badly written SP)</li></ul></li><li>Modern implementations of stored procedures have abandoned PL/SQL and use existing general-purpose programming languages instead: VoltDB uses Java or Groovy, Datomic uses Java or Clojure, and Redis uses Lua.</li></ul></li><li><strong>Partitioning</strong><ul><li>for applications with high write throughput, the single-thread transaction processor can become a serious bottleneck.</li><li>If you can find a way of partitioning you dataset so that each trasaction only needs to read and write data within a single partition, then each partition can have its own transaction processiong thread running independently from the others.</li><li>Whether transactions can be single-partition depends very much on the structure of the data used by the application.</li></ul></li><li><strong>Summary of serial execution</strong><ul><li>A viable way of achieving serializable isolation within certain constraints <ul><li>Every transaction must be small and fast, because it takes only one slow transaction to stall all transactions processing.</li><li>It is limited to use cases where the active dataset can fit in memory. (C: use anti-catching if the dataset is in disk)</li><li>Write thoughput must be low enought to be handled on a single CPU core or transactions need to be partitioned withought requiring cross-partition coordination.</li><li>Cross-partition transactions are possible, but there is a hard limit to the extent to which they can be used.</li></ul></li></ul></li></ul></li><li><strong>Two-phase Locking (2PL)</strong>: (2PL is not 2PC)<ul><li>Several transactions are allowed to concurrently read same the same object as long as nobody is writing to it. But as soon as anyone wants to write (modify or delete) an object, exclusive access is required.<ul><li>If transaction A has read an object and transaction B wants to write to that object, B must wait until A commits or aborts before it can continue. (This ensures that B can’t change the object unexpectedly behind A’s back.)</li><li>If transaction A has written an object and transactions B wants to read that object, B must wait until A commits or aborts before it can continue. (Reading an old version of object, like in Figure 7-1, is not acceptable under 2PL.)</li></ul></li><li>In 2PL, writes don’t just <strong>block other writers; they also block readers</strong> and vice versa.<ul><li>Because 2PL provides serializability, it protects against all the race conditions discussed earlier, including lost updates and write skew.</li></ul></li><li><strong>Implementation of two-phase locking</strong><ul><li><u>Shared</u> and <u>Exclusive</u> lock</li><li>The <u>first phase</u> (while the transaction is execuding) is when the locks are acquired. and the <u>second phase</u> (at the end of the transaction) is when all the locks are released.</li><li>Since so many locks are in use, it can happend quite easily that transaction A is stuck waiting for transaction B to release its lock, and vice versa –&gt; <strong>deadlock</strong><ul><li>The database automatically detects deadlocks between transactions and aborts one of them so that the others can make progress.</li><li>Application need retry after abort</li></ul></li></ul></li><li><strong>Performance of two-phase locking</strong><ul><li>transactions throughput and response times queries are significantly worse under two-phase locking than under weak isolation.</li><li>databases running 2PL can have quite unstable latencies, and they can be very slow at high percentiles.</li></ul></li><li><strong>Predicate locks</strong><ul><li>a database with serializable isolation must <u>prevent phantoms</u>.</li><li><strong>predicate lock</strong>: it works similarly to the shared/exclusive lock described earlier, but rather than belonging to a particular object (e.g., one row in a table), it belongs to all objects that match some search condition.</li><li>A predicate lock applies even to objects that do not yet exist in the database, but which might be added in the future. (phantoms)</li></ul></li><li><strong>Index-range locks</strong><ul><li>Unfortunately, <u>predicate locks</u> do not preform well: if there are many locks by active transactions, checking for matching locks becomes time-consuming.</li><li>Most databases with 2PL actually implement <strong><u>index-range locking</u></strong> (also known as next-key locking), which is a simplified approximation of predicate locking.</li><li>Index-range locks are not as precise as predicate locks would be, but since they have much lower overheads, the are a good compromise.</li></ul></li></ul></li><li><strong><u>Serializable Snapshot Isolation (SSI)</u></strong>: (new default in the future)<ul><li>It provides full <strong>serializability</strong>, but has only a small performance penalty compared to a <strong>snapshot isolation</strong><ul><li>SSI is fairly new: it was first described in 2008 and is the subject of Michael Cahill’s PhD thesis.</li></ul></li><li><strong>Pessimistic vs. optimistic concurrency control</strong><ul><li>Two-phase locking is a so-called <strong>pessimistic</strong> concurrency control mechanism.<ul><li><u>Serial execution</u> is, in a sense, pessimistic to the extreme.</li></ul></li><li><u>Serializable snapshot isolation</u> is an <strong>optimistic</strong> concurrency control technique.</li></ul></li><li>contention can be reduced with conmmutative atomic operations </li><li>SSI is based on <u>snapshot isolation</u> – that is, all reads within a transaction are made from a consistent snapshot of the database.<ul><li>On top of snapshot isolation, <strong>SSI adds an algorithm for <u>detecting serialization conflicts among writes</u> and determining which transactions to abort</strong>.</li></ul></li><li><strong>Decisions based on an outdated premise</strong><ul><li>How does the database know if a query result might have changed? There are two cases to consider<ul><li><strong>Detecting reads</strong> of a stale MVCC object version (uncommitted write occurred before the read)</li><li><strong>Detecting writes</strong> that affect prior reads (the write occurs after that read)</li></ul></li></ul></li><li><strong>Detecting stale MVCC reads</strong><ul><li>in order to prevent this anomaly, the database needs to track when a transaction ignores another transaction’s writes due to MVCC visibility rules. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598371028/Design Data Intensive Application/p2-ch7-img01_llpsok.png" alt="p2-ch7-img01" style="zoom:50%;" /></li><li>By avoiding unnecessary aborts, SSI preserve snapshot isolation’s support for long-running reads from a consistent snapshot.</li></ul></li><li><strong>Detecting writes that affect prior reads</strong><ul><li>The second case to consider is when another transaction modifies data after it has been read. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598371030/Design Data Intensive Application/p2-ch7-img02_idbdbh.png" alt="p2-ch7-img01" style="zoom:50%;" /></li><li>When a transaction writes to the database, it must look in the indexes for any other transactions that have recently read the affected data.<ul><li>This process is similar to acquiring a write lock on the affected key range, but rather than blocking until the readers have committed, the lock acts as a <strong>tripwire</strong>: it simply notifies the transactions that the data they read may not longer be up to date.</li></ul></li></ul></li><li><strong>Performance of serializable snapshot isolation</strong><ul><li>Compared to two-phase locking, the big advantage of serializable snapshot isolation is that one transaction doesn’t need to block waiting for locks held by another transaction.<ul><li>Like under sanpshot isolation, writes don’t block readers, and vice versa.</li><li>The rate of aborts significantly affects the overall performance of SSI.</li></ul></li></ul></li></ul></li></ul><h4 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h4><ul><li>Transactions are an abstraction layer that allows an application to pretend that certain concurrency problems and certain kinds of hardware and software faults don’t exist.</li><li>Concurrency control: (isolation levels)<ul><li>Read committed </li><li>Snapshot isolation (sometimes called repeatable read)</li><li>Serializable </li></ul></li><li>Race conditions <ul><li>Dirty reads</li><li>Dirty writes</li><li>Read skew (non-repeatable reads)<ul><li>Solved by: snapshot isolation which implemented with MVCC</li></ul></li><li>Lost updates </li><li>Write skew<ul><li>Solved by: serializable isolation </li></ul></li><li>Phantom reads<ul><li>Solved by: index-range locks</li></ul></li></ul></li><li>Only <u>Serializable isolation</u> protects against all of these issues. We discussed three different approaches to implementing serializable transactions<ul><li>Literally executing transactions in a serial order</li><li>Two-phase locking</li><li>Serializable snapshot isolation (SSI) (C: best approach)</li></ul></li></ul><h4 id="End"><a href="#End" class="headerlink" title="End"></a>End</h4><p>The illustration structure of chapter 7 is not very clear, my recommendation is to read this blog first : <a href="https://www.jyt0532.com/2019/04/21/transactions/" target="_blank" rel="noopener">jyt0523’s blog</a>. It’s good for people who are new to this topics or read this book for the first time.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter7 Transactions&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;A &lt;strong&gt;transaction&lt;/strong&gt; is a way for an application to group several reads and writes together into a logical unit.&lt;ul&gt;
&lt;li&gt;Conceptually, all the reads and writes in a transaction are executed as one operation: &lt;u&gt;either that entire transaction succeeds (commit) or it fails (abort, rollback)&lt;/u&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;By using transactions, the application is free to ignore certain potential error scenarios and concurrency issues, because the database takes care of them instead (we call these &lt;u&gt;safety quarantees&lt;/u&gt;)&lt;ul&gt;
&lt;li&gt;Vs. &lt;strong&gt;higher performance&lt;/strong&gt; or &lt;strong&gt;higher availability&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;How do you figure out whether you need transactions?&lt;/strong&gt;&lt;ul&gt;
&lt;li&gt;In order to answer that question, we first need to understand exactly what &lt;u&gt;safety quanrantees transactions&lt;/u&gt; can provide, and what costs are associated with them.&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>DynamoDB</title>
    <link href="http://yoursite.com/2020/08/23/DynamoDB/"/>
    <id>http://yoursite.com/2020/08/23/DynamoDB/</id>
    <published>2020-08-23T16:52:37.000Z</published>
    <updated>2020-08-23T16:53:35.124Z</updated>
    
    <content type="html"><![CDATA[<p>AmazonDynamoDB被设计成用来托管的NoSQL数据库服务、可预期的性能、可实现无缝扩展性和可靠性等核心问题。</p><a id="more"></a><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><p>Amazon DynamoDB是一个完全托管的<a href="https://baike.baidu.com/item/NoSQL" target="_blank" rel="noopener">NoSQL</a>数据库服务，可以提供快速的、可预期的性能，并且可以实现无缝扩展。只需要在<a href="https://baike.baidu.com/item/AWS/7630528" target="_blank" rel="noopener">AWS</a>（Amazon Web Service）管理控制台上面，用鼠标轻松点击几下，用户就可以自己创建一个新的Amazon DynamoDB数据库表，并可以根据实际需求对表进行扩展和收缩，这个过程既不需要停止对外服务，也不会降低服务性能。通过AWS管理控制台，用户还可以看见资源利用情况和各种性能指标。Amazon DynamoDB可以使得用户把操作和扩展分布式数据库的沉重负担，交付给AWS来处理，这样，用户就不需要担心硬件配置、复制、软件安装和打补丁包、簇扩展等。</p><h3 id="特性"><a href="#特性" class="headerlink" title="特性"></a>特性</h3><p><strong>1、可扩展性：</strong>AWS DynamoDB的设计，可以支持在吞吐量和存储能力上的无缝扩展；<br>额定的吞吐量：在创建一个表的时候，可以很简单地设定你的表所需要的处理请求的能力。AWS DynamoDB会根据你的处理能力要求，为你的表分配专用的资源，从而满足你的性能需求，并且会把数据分区到足够多的服务器上，从而满足你对处理能力的要求。如果你的吞吐量需求改变了，你可以使用AWS管理控制台或者AWS DynamoDB API对你的表所需要的处理请求的能力进行修改。当扩展正在进行时，你仍然可以获得先前级别的处理能力。</p><p>自动存储扩展：AWS DynamoDB没有对一个表中可以存储的数据量的大小进行限制，当你使用AWS DynamoDB的写操作API存入更多数据时，就会自动为你分配更多的存储空间。</p><p>完全分布式、非共享架构：AWS DynamoDB可以实现水平扩展，可以无缝地把一个表扩展到多个（几百个）服务器上。</p><p><strong>2、快速、可预期的性能：</strong>AWS DynamoDB的服务端的平均延迟，通常是几毫秒(millisecond)。运行在固态盘上面的服务，可以在任何扩展级别下维持一致性和低延迟。</p><p><strong>3、简易的管理：</strong>AWS DynamoDB是一个完全托管的服务，你只需要简单地创建一个数据库表，剩下的所有事情都交给AWS服务来处理。你不需要担心硬件和软件的配给、建立、配置，也不要担心软件安装和打补丁包，更不必担心如何运行一个可靠的分布式数据库簇，或者把数据分区到多个实例。</p><p><strong>4、内置的容错性：</strong>AWS DynamoDB具有内在的容错能力，可以自动、同步地把你的数据复制到一个Region中的多个可用的Zone中，即使遇到单个机器或设施的实效，你的数据也可以得到很好的保护。</p><p><strong>5、灵活性</strong>：AWS DynamoDB没有固定的模式（schema）。相反，每个项目(item)都具有不同数量的属性，可以支持多种数据类型，比如string、number和set。</p><p><strong>6、强一致性、原子计数器：</strong>和许多非关系型数据库不同，AWS DynamoDB使你的开发工作变得更加简单，它可以支持读操作的强一致性，从而保证你可以总是获得最新的数据。读操作支持多个本地(native)数据类型，比如number、string和multi-value attribute。这种服务也可以支持原子计数器（Atomic Counter），允许你通过一个简单的API调用就可以自动增加和减少数值属性。</p><p><strong>7、性价比高：</strong>AWS DynamoDB被设计成在任何级别的负载下都可以取得很高的性价比。你可以从一个免费的等级开始，这种级别只支持每个月4000万次的数据库操作，对于超出这个免费级别的范围，你只需要为你使用的资源支付以小时计费的很低的费用。由于具备了简易的管理和高效的请求定价能力，采用AWS DynamoDB相比采用其他非关系型数据库而言，具有更低的总体拥有成本（TCO：total cost of owership）。</p><p><strong>8、安全：</strong>AWS DynamoDB使用可靠的密钥方法，只允许授权用户访问数据，而绝对不允许非授权用户的非法访问。AWS DynamoDB集成了AWS Identiy and Access Management(简称AWS IAM)，可以实现更细粒度的访问控制。</p><p><strong>9、集成的监视功能：</strong>AWS DynamoDB可以在AWS管理控制台中，把关于你的表的关键性能指标都显示出来。同时还集成了Amazon CloudWatch，可以让用户每个表的请求吞吐量和延迟，从而很容易实现对资源的跟踪。</p><p><strong>10、弹性的MapReduce集成：</strong>AWS DynamoDB同时集成了Amazon Elastic MapReduce（简称Amazon EMR）。Amazon EMR可以支持对大型的数据集执行复杂的分析操作，并且采用AWS中按需付费的Hadoop框架。随着AWS DynamoDB的发布，对于用户而言，很容易使用Amazon EMR来对存储在DynamoDB中的数据集进行分析，并且把分析结果存储在Amazon Simple Storage Service（简称Amazon S3）中，而原来的数据仍然原封不动地保存在AWS DynamoDB中。用户也可以使用Amazon EMR来访问存储在多个地方的数据，比如AWS DynamoDB、Amazon RDS和Amazon S3，并且对来自这些地方的数据进行合并以后，做更加复杂的分析，并且把分析结果存储在Amazon S3中。</p><h3 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h3><p><strong>DynamoDB和SimpleDB的区别</strong></p><p>熟悉Amazon AWS的朋友应该知道SimpleDB。细心的朋友还会发现，自DynamoDB上线以后，在AWS首页的Database列表中，只能看到RDS和DynamoDB，DynamoDB已经替换了SimpleDB原来在这个列表中的位置，似乎SimpleDB已被打入冷宫</p><p>SimpleDB是AWS上的第一个NoSQL数据库服务，AWS团队也在博客上详数SimpleDB几个致命的缺点，正是这些缺点导致AWS开发全新的数据库服务。</p><ul><li>SimpleDB有单表限制。SimpleDB也有类似于Table的东西叫做Domain，每个Domain最多只能保存10GB的数据，而DynamoDB并没有单表存储的任何限制。虽然SimpleDB提供的Schema Free可以提高开发者的效率和灵活性，但10GB这种无法扩展的限制妨碍了很多开发者的使用，使得很多开发者采用了其他NoSQL解决方案。</li><li>性能不稳定。SimpleDB以简单为设计目标，SimpleDB并不需要用户指定Primary Key，也不需要用户创建索引，会默认对所有Attribute创建索引。然而这种简洁性也带来了一些副作用：如果用户插入的一条数据带有非常多的Attribute，SimpleDB就需要去更新所有Attribute的索引，而且不管用户将来是否需要。真是所谓的：用或者不用，索引就在那里。正是由于默认对所有Attribute创建索引，整个Domain才会有10GB这么奇怪的限制，以避免数据量变得比较大时，创建索引的过程会变得极慢无比，根本不能忍受。而DynamoDB因为有了Provisioned Throughput的限制以及采用了固态硬盘，可以保证每个请求的性能是非常稳定而且高效的。</li><li>一致性问题。SimpleDB设计时采用的是最终一致性模型，而DynamoDB读数据时用户可以选择弱一致性或者强一致性，对用户而言可以根据业务特征来进行选择，毕竟强一致性在逻辑上更容易让用户接受。</li></ul><p>在Schema上，两者都不需要固定Schema，但value类型不够丰富，在API设计上DynamoDB继承了SimpleDB的风格，请求的参数个数显得比较臃肿，好在有不同语言的SDK，方便了用户的开发。</p><p>DynamoDB不是SimpleDB 2.0，但DynamoDB也吸收了SimpleDB以及其他NoSQL数据设计思想的精华，相信会有不少用户会采用DynamoDB作为他们的NoSQL解决方案。</p><p><strong>DynamoDB和Cassandra、MongoDB的比较</strong></p><p>前面说过Cassandra受2007年Amazon发表的Dynamo论文影响非常深，在DynamoDB发布的第一天，提供Cassandra商业化支持的DataStax公司的Jonathan Ellis就写了一篇文章，分析了Cassandra和DynamoDB的差异。</p><p>虽然Jonathan Ellis认为DynamoDB不支持Secondary Key Indexes是在开历史的倒车，但如果DynamoDB支持了Secondary Key Indexes，那么它是无法保证每个请求性能的高效性的。这和DynamoDB的设计理念相冲突，于是舍弃了这部分的功能。</p><p>其实从开发的易用角度来讲，DynamoDB没有Cassandra和MongoDB强大，Cassandra有CQL可以做非常丰富的查询，<a href="https://baike.baidu.com/item/MongoDB/60411" target="_blank" rel="noopener">MongoDB</a>的查询功能也非常强大，而且后两者都提供Shell客户端，并有不少第三方开发的工具可以进行管理与使用。在条件更新上，DynamoDB也没有MongoDB使用起来那么方便，并且MongoDB提供了更多的原子性操作。在对value类型的支持上，另两者都不如MongoDB，毕竟MongoDB是文档型的数据库，可以理解为底层存储的是JSON。毕竟，JSON支持的类型以及在JSON上可以做的操作是很丰富的。我一直觉得DynamoDB没有支持类JSON格式是个遗憾。也许可能是DynamoDB团队觉得如果支持类JSON格式的话，在API的设计上会显得更加臃肿和让用户更难理解API如何使用。但个人认为，DynamoDB如果提供相应的SDK其实是可以解决这个问题的，就算MongoDB的开放接口相对DynamoDB更加复杂，开发者都是直接使用驱动（相当于SDK）进行开发，于是在开发应用上MongoDB远胜于DynamoDB。</p><p>但从运维的角度来讲，DynamoDB省去开发者部署/监控/维护数据库环节，给开发者节约了大量时间，强大的扩展能力又减轻了后续运维的压力，这正是DynamoDB最大的价值所在。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;AmazonDynamoDB被设计成用来托管的NoSQL数据库服务、可预期的性能、可实现无缝扩展性和可靠性等核心问题。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
      <category term="Database" scheme="http://yoursite.com/tags/Database/"/>
    
  </entry>
  
  <entry>
    <title>Kafka</title>
    <link href="http://yoursite.com/2020/08/23/Kafka/"/>
    <id>http://yoursite.com/2020/08/23/Kafka/</id>
    <published>2020-08-23T16:45:36.000Z</published>
    <updated>2020-09-11T02:04:34.031Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Kafka</strong>是由<a href="https://zh.wikipedia.org/wiki/Apache软件基金会" target="_blank" rel="noopener">Apache软件基金会</a>开发的一个<a href="https://zh.wikipedia.org/wiki/开源" target="_blank" rel="noopener">开源</a><a href="https://zh.wikipedia.org/wiki/流处理" target="_blank" rel="noopener">流处理</a>平台，由<a href="https://zh.wikipedia.org/wiki/Scala" target="_blank" rel="noopener">Scala</a>和<a href="https://zh.wikipedia.org/wiki/Java" target="_blank" rel="noopener">Java</a>编写。该项目的目标是为处理实时数据提供一个统一、高吞吐、低延迟的平台。其持久化层本质上是一个“按照分布式事务日志架构的大规模发布/订阅消息队列”，这使它作为企业级基础设施来处理流式数据非常有价值。此外，Kafka可以通过Kafka Connect连接到外部系统（用于数据输入/输出），并提供了Kafka Streams——一个<a href="https://zh.wikipedia.org/wiki/Java" target="_blank" rel="noopener">Java</a>流式处理<a href="https://zh.wikipedia.org/wiki/库_(计算机)" target="_blank" rel="noopener">库</a>。</p><p>该设计受<a href="https://zh.wikipedia.org/w/index.php?title=事务日志&action=edit&redlink=1" target="_blank" rel="noopener">事务日志</a>的影响较大。</p><a id="more"></a><h3 id="kafka是什么"><a href="#kafka是什么" class="headerlink" title="kafka是什么"></a>kafka是什么</h3><p>Kafka是由LinkedIn开发的一个分布式基于发布/订阅的消息系统，使用Scala编写，它以可水平扩展和高吞吐率而被广泛使用。</p><h3 id="产生背景"><a href="#产生背景" class="headerlink" title="产生背景"></a>产生背景</h3><p>Kafka是一个消息系统，用作LinkedIn的活动流（Activity Stream）和运营数据处理管道（Pipeline）的基础。活动流数据是几乎所有站点在对其网站使用情况做报表时都要用到的数据中最常规的部分。活动数据包括页面访问量（Page View）、被查看内容方面的信息以及搜索情况等内容。这种数据通常的处理方式是先把各种活动以日志的形式写入某种文件，然后周期性地对这些文件进行统计分析。运营数据指的是服务器的性能数据（CPU、IO使用率、请求时间、服务日志等等数据)。运营数据的统计方法种类繁多。</p><h3 id="基本架构图"><a href="#基本架构图" class="headerlink" title="基本架构图"></a>基本架构图</h3><p><img src="https://res.cloudinary.com/shuaiz/image/upload/v1599789703/kafka_qxjq9d.png" alt="kafka_architecture"></p><h3 id="基本概念解释"><a href="#基本概念解释" class="headerlink" title="基本概念解释"></a>基本概念解释</h3><p><strong>Broker</strong><br>Kafka集群包含一个或多个服务器，这种服务器被称为broker。broker端不维护数据的消费状态，提升了性能。直接使用磁盘进行存储，线性读写，速度快：避免了数据在JVM内存和系统内存之间的复制，减少耗性能的创建对象和垃圾回收。 </p><p><strong>Producer</strong><br>负责发布消息到Kafka broker</p><p><strong>Consumer</strong><br>消息消费者，向Kafka broker读取消息的客户端，consumer从broker拉取(pull)数据并进行处理。</p><p><strong>Topic</strong><br>每条发布到Kafka集群的消息都有一个类别，这个类别被称为Topic。（物理上不同Topic的消息分开存储，逻辑上一个Topic的消息虽然保存于一个或多个broker上但用户只需指定消息的Topic即可生产或消费数据而不必关心数据存于何处）</p><p><strong>Partition</strong><br>Parition是物理上的概念，每个Topic包含一个或多个Partition.</p><p><strong>Consumer Group</strong><br>每个Consumer属于一个特定的Consumer Group（可为每个Consumer指定group name，若不指定group name则属于默认的group）</p><p><strong>Topic &amp; Partition</strong><br>Topic在逻辑上可以被认为是一个queue，每条消费都必须指定它的Topic，可以简单理解为必须指明把这条消息放进哪个queue里。为了使得Kafka的吞吐率可以线性提高，物理上把Topic分成一个或多个Partition，每个Partition在物理上对应一个文件夹，该文件夹下存储这个Partition的所有消息和索引文件。若创建topic1和topic2两个topic，且分别有13个和19个分区，则整个集群上会相应会生成共32个文件夹（本文所用集群共8个节点，此处topic1和topic2 replication-factor均为1）。</p><h3 id="适用场景"><a href="#适用场景" class="headerlink" title="适用场景"></a>适用场景</h3><p>1、Messaging<br>  对于一些常规的消息系统,kafka是个不错的选择;partitons/replication和容错,可以使kafka具有良好的扩展性和性能优势.不过到目前为止,我们应该很清楚认识到,kafka并没有提供JMS中的”事务性””消息传输担保(消息确认机制)””消息分组”等企业级特性;kafka只能使用作为”常规”的消息系统,在一定程度上,尚未确保消息的发送与接收绝对可靠(比如,消息重发,消息发送丢失等)</p><p>2、Website activity tracking<br>  kafka可以作为”网站活性跟踪”的最佳工具;可以将网页/用户操作等信息发送到kafka中.并实时监控,或者离线统计分析等</p><p>3、Metrics<br>  Kafka通常被用于可操作的监控数据。这包括从分布式应用程序来的聚合统计用来生产集中的运营数据提要。</p><p>4、Log Aggregation<br>kafka的特性决定它非常适合作为”日志收集中心”;application可以将操作日志”批量””异步”的发送到kafka集群中,而不是保存在本地或者DB中;kafka可以批量提交消息/压缩消息等,这对producer端而言,几乎感觉不到性能的开支.此时consumer端可以使hadoop等其他系统化的存储和分析系统。</p><h3 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h3><p>原文作者：宝哥-NO1<br>转自链接：<a href="https://blog.csdn.net/Love_JavaProgram/article/details/50475511" target="_blank" rel="noopener">https://blog.csdn.net/Love_JavaProgram/article/details/50475511</a><br>版权声明：著作权归作者所有。商业转载请联系作者获得授权，非商业转载请保留以上作者信息和原文链接。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Kafka&lt;/strong&gt;是由&lt;a href=&quot;https://zh.wikipedia.org/wiki/Apache软件基金会&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Apache软件基金会&lt;/a&gt;开发的一个&lt;a href=&quot;https://zh.wikipedia.org/wiki/开源&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;开源&lt;/a&gt;&lt;a href=&quot;https://zh.wikipedia.org/wiki/流处理&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;流处理&lt;/a&gt;平台，由&lt;a href=&quot;https://zh.wikipedia.org/wiki/Scala&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Scala&lt;/a&gt;和&lt;a href=&quot;https://zh.wikipedia.org/wiki/Java&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Java&lt;/a&gt;编写。该项目的目标是为处理实时数据提供一个统一、高吞吐、低延迟的平台。其持久化层本质上是一个“按照分布式事务日志架构的大规模发布/订阅消息队列”，这使它作为企业级基础设施来处理流式数据非常有价值。此外，Kafka可以通过Kafka Connect连接到外部系统（用于数据输入/输出），并提供了Kafka Streams——一个&lt;a href=&quot;https://zh.wikipedia.org/wiki/Java&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Java&lt;/a&gt;流式处理&lt;a href=&quot;https://zh.wikipedia.org/wiki/库_(计算机)&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;库&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;该设计受&lt;a href=&quot;https://zh.wikipedia.org/w/index.php?title=事务日志&amp;action=edit&amp;redlink=1&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;事务日志&lt;/a&gt;的影响较大。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>RabbitMQ</title>
    <link href="http://yoursite.com/2020/08/23/RabbitMQ/"/>
    <id>http://yoursite.com/2020/08/23/RabbitMQ/</id>
    <published>2020-08-23T16:35:16.000Z</published>
    <updated>2020-08-23T16:35:41.642Z</updated>
    
    <content type="html"><![CDATA[<p>RabbitMQ 是一个由 erlang 开发的 AMQP (Advanced Message Queuing Protocol) 的开源实现。</p><p>AMQP ：高级消息队列协议，是应用层协议的一个开放标准，为面向消息的中间件设计。消息中间件主要用于组件之间的解耦，消息的发送者无需知道消息使用者的存在，反之亦然。 AMQP 的主要特征是面向消息、队列、路由（包括点对点和发布 / 订阅）、可靠性、安全。 RabbitMQ 是一个开源的 AMQP 实现，服务器端用 Erlang 语言编写，支持多种客户端，如：Python、Ruby、.NET、Java、JMS、C、PHP、ActionScript、XMPP、STOMP 等，支持 AJAX。用于在分布式系统中存储转发消息，在易用性、扩展性、高可用性等方面表现不俗。</p><a id="more"></a><h3 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h3><ol><li>异步处理</li><li>应用解耦</li><li>流量削峰</li></ol><h3 id="RabbitMQ特性"><a href="#RabbitMQ特性" class="headerlink" title="RabbitMQ特性"></a>RabbitMQ特性</h3><p>RabbitMQ 最初起源于金融系统，用于在分布式系统中存储转发消息，在易用性、扩展性、高可用性等方面表现不俗。具体特点包括：</p><ul><li><p><strong>可靠性（Reliability）</strong></p><ul><li>RabbitMQ 使用一些机制来保证可靠性，如持久化、传输确认、发布确认。</li></ul></li><li><p><strong>灵活的路由（Flexible Routing）</strong></p><ul><li>在消息进入队列之前，通过 Exchange 来路由消息的。对于典型的路由功能，RabbitMQ 已经提供了一些内置的 Exchange 来实现。针对更复杂的路由功能，可以将多个 Exchange 绑定在一起，也通过插件机制实现自己的 Exchange 。</li></ul></li><li><p><strong>消息集群（Clustering）</strong></p><ul><li>多个 RabbitMQ 服务器可以组成一个集群，形成一个逻辑 Broker 。</li></ul></li><li><p><strong>高可用（Highly Available Queues）</strong></p><ul><li>队列可以在集群中的机器上进行镜像，使得在部分节点出问题的情况下队列仍然可用。</li></ul></li><li><p><strong>多种协议（Multi-protocol）</strong></p><ul><li>RabbitMQ 支持多种消息队列协议，比如 STOMP、MQTT 等等。</li></ul></li><li><p><strong>多语言客户端（Many Clients）</strong></p><ul><li>RabbitMQ 几乎支持所有常用语言，比如 Java、.NET、Ruby 等等。</li></ul></li><li><p><strong>管理界面（Management UI）</strong></p><ul><li>RabbitMQ 提供了一个易用的用户界面，使得用户可以监控和管理消息 Broker 的许多方面。</li></ul></li><li><p><strong>跟踪机制（Tracing）</strong></p><ul><li>如果消息异常，RabbitMQ 提供了消息跟踪机制，使用者可以找出发生了什么。</li></ul></li><li><p><strong>插件机制（Plugin System）</strong></p><ul><li>RabbitMQ 提供了许多插件，来从多方面进行扩展，也可以编写自己的插件。</li></ul></li></ul><h3 id="RabbitMQ基本概念"><a href="#RabbitMQ基本概念" class="headerlink" title="RabbitMQ基本概念"></a>RabbitMQ基本概念</h3><ul><li><p><strong>Message</strong>：消息，消息是不具名的，它由消息头和消息体组成。</p><ul><li>消息体是不透明的，而消息头则由一系列的可选属性组成，这些属性包括routing-key（路由键）、priority（相对于其他消息的优先权）、delivery-mode（指出该消息可能需要持久性存储）等。</li></ul></li><li><p><strong>Publisher</strong>：消息的生产者，也是一个向交换器发布消息的客户端应用程序。</p></li><li><p><strong>Exchange</strong>：交换器，用来接收生产者发送的消息并将这些消息路由给服务器中的队列。</p></li><li><p><strong>Routing Key</strong>：路由关键字, exchange根据这个关键字进行消息投递。</p></li><li><p><strong>Binding</strong></p><ul><li>绑定，用于消息队列和交换器之间的关联。一个绑定就是基于路由键将交换器和消息队列连接起来的路由规则，所以可以将交换器理解成一个由绑定构成的路由表。</li></ul></li><li><p><strong>Queue</strong></p><ul><li>消息队列，用来保存消息直到发送给消费者。它是消息的容器，也是消息的终点。一个消息可投入一个或多个队列。消息一直在队列里面，等待消费者连接到这个队列将其取走。</li></ul></li><li><p><strong>Connection</strong>：网络连接，比如一个TCP连接。</p></li><li><p><strong>Channel</strong>： 信道，多路复用连接中的一条独立的双向数据流通道。</p><ul><li>信道是建立在真实的TCP连接内地虚拟连接，AMQP 命令都是通过信道发出去的，不管是发布消息、订阅队列还是接收消息，这些动作都是通过信道完成。因为对于操作系统来说建立和销毁 TCP 都是非常昂贵的开销，所以引入了信道的概念，以复用一条 TCP 连接。</li></ul></li><li><p><strong>Consumer</strong>： 消息的消费者，表示一个从消息队列中取得消息的客户端应用程序。</p></li><li><p><strong>Virtual Host</strong>：虚拟主机，表示一批交换器、消息队列和相关对象。</p><ul><li>虚拟主机是共享相同的身份认证和加密环境的独立服务器域。每个 vhost 本质上就是一个 mini 版的 RabbitMQ 服务器，拥有自己的队列、交换器、绑定和权限机制。vhost 是 AMQP 概念的基础，必须在连接时指定，RabbitMQ 默认的 vhost 是 / 。</li></ul></li><li><p><strong>Broker</strong>：表示消息队列服务器实体。</p><ul><li>它提供一种传输服务,它的角色就是维护一条从生产者到消费者的路线，保证数据能按照指定的方式进行传输, </li></ul></li></ul><h3 id="Exchange类型"><a href="#Exchange类型" class="headerlink" title="Exchange类型"></a>Exchange类型</h3><p>Exchange 分发消息时根据类型的不同分发策略有区别，目前共四种类型：direct、fanout、topic、headers 。headers 匹配 AMQP 消息的 header 而不是路由键，此外 headers 交换器和 direct 交换器完全一致，但性能差很多，目前几乎用不到了，所以直接看另外三种类型：</p><ul><li><p>direct</p><ul><li>消息中的路由键（routing key）如果和 Binding 中的 binding key 一致， 交换器就将消息发到对应的队列中。路由键与队列名完全匹配，如果一个队列绑定到交换机要求路由键为 “dog”，则只转发 routing key 标记为 “dog” 的消息，不会转发 “dog.puppy”，也不会转发 “dog.guard” 等等。它是完全匹配、单播的模式。</li></ul></li><li><p>fanout</p><ul><li>每个发到 fanout 类型交换器的消息都会分到所有绑定的队列上去。fanout 交换器不处理路由键，只是简单的将队列绑定到交换器上，每个发送到交换器的消息都会被转发到与该交换器绑定的所有队列上。很像子网广播，每台子网内的主机都获得了一份复制的消息。fanout 类型转发消息是最快的。</li></ul></li><li><p>topic</p><ul><li>topic 交换器通过模式匹配分配消息的路由键属性，将路由键和某个模式进行匹配，此时队列需要绑定到一个模式上。它将路由键和绑定键的字符串切分成单词，这些单词之间用点隔开。它同样也会识别两个通配符：符号 “#” 和符号 “”。# 匹配 0 个或多个单词，匹配不多不少一个单词。</li></ul></li></ul><h3 id="ConnectionFactory、Connection、Channel"><a href="#ConnectionFactory、Connection、Channel" class="headerlink" title="ConnectionFactory、Connection、Channel"></a>ConnectionFactory、Connection、Channel</h3><p>ConnectionFactory、Connection、Channel 都是 RabbitMQ 对外提供的 API 中最基本的对象。</p><ul><li>Connection 是 RabbitMQ 的 socket 链接，它封装了 socket 协议相关部分逻辑。</li><li>ConnectionFactory 为 Connection 的制造工厂。</li><li>Channel 是我们与 RabbitMQ 打交道的最重要的一个接口，我们大部分的业务操作是在 Channel 这个接口中完成的，包括定义 Queue、定义 Exchange、绑定 Queue 与 Exchange、发布消息等。</li></ul><h3 id="任务分发机制"><a href="#任务分发机制" class="headerlink" title="任务分发机制"></a>任务分发机制</h3><p>1、<strong>Round-robin dispathching 循环分发</strong><br>RabbbitMQ 的分发机制非常适合扩展，而且它是专门为并发程序设计的，如果现在 load 加重，那么只需要创建更多的 Consumer 来进行任务处理</p><p>2、<strong>Message acknowledgment 消息 确认</strong><br>在实际应用中，可能会发生消费者收到 Queue 中的消息，但没有处理完成就宕机（或出现其他意外）的情况，这种情况下就可能会导致消息丢失。为了避免这种情况发生，我们可以要求消费者在消费完消息后发送一个回执给 RabbitMQ，RabbitMQ 收到消息回执（Message acknowledgment）后才将该消息从 Queue 中移除；如果 RabbitMQ 没有收到回执并检测到消费者的 RabbitMQ 连接断开，则 RabbitMQ 会将该消息发送给其他消费者（如果存在多个消费者）进行处理。这里不存在 timeout 概念，一个消费者处理消息时间再长也不会导致该消息被发送给其他消费者，除非它的 RabbitMQ 连接断开。 这里会产生另外一个问题，如果我们的开发人员在处理完业务逻辑后，忘记发送回执给 RabbitMQ，这将会导致严重的 bug——Queue 中堆积的消息会越来越多；消费者重启后会重复消费这些消息并重复执行业务逻辑…<br>另外 pub message 是没有 ack 的。</p><p>3、<strong>Message durability 消息持久化</strong><br>如果我们希望即使在 RabbitMQ 服务重启的情况下，也不会丢失消息，我们可以将 Queue 与 Message 都设置为可持久化的（durable），这样可以保证绝大部分情况下我们的 RabbitMQ 消息不会丢失。但依然解决不了小概率丢失事件的发生（比如 RabbitMQ 服务器已经接收到生产者的消息，但还没来得及持久化该消息时 RabbitMQ 服务器就断电了），如果我们需要对这种小概率事件也要管理起来，那么我们要用到事务。由于这里仅为 RabbitMQ 的简单介绍，所以这里将不讲解 RabbitMQ 相关的事务。<br>要持久化队列 queue 的持久化需要在声明时指定 durable=True;<br>这里要注意，队列的名字一定要是 Broker 中不存在的，不然不能改变此队列的任何属性.<br>队列和交换机有一个创建时候指定的标志 durable,durable 的唯一含义就是具有这个标志的队列和交换机会在重启之后重新建立，它不表示说在队列中的消息会在重启后恢复</p><p><strong>消息持久化包括 3 部分</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"> - 1.exchange持久化,在声明时指定durable &#x3D;&gt; true</span><br><span class="line">hannel.ExchangeDeclare(ExchangeName,&quot;direct&quot;,durable:true,autoDelete:false,arguments:null);&#x2F;&#x2F;声明消息队列，且为可持久化的</span><br><span class="line"></span><br><span class="line"> - 2.queue持久化,在声明时指定durable &#x3D;&gt; true</span><br><span class="line">channel.QueueDeclare(QueueName,durable:true,exclusive:false,autoDelete:false,arguments:null);&#x2F;&#x2F;声明消息队列，且为可持久化的</span><br><span class="line"></span><br><span class="line"> - 3.消息持久化,在投递时指定delivery_mode &#x3D;&gt; 2(1是非持久化).</span><br><span class="line">channel.basic_publish(exchange&#x3D;&#39;&#39;,</span><br><span class="line">                      routing_key&#x3D;&quot;task_queue&quot;,</span><br><span class="line">                      body&#x3D;message,</span><br><span class="line">                      properties&#x3D;pika.BasicProperties(</span><br><span class="line">                         delivery_mode &#x3D; 2, # make message persistent</span><br><span class="line">                      ))</span><br></pre></td></tr></table></figure><p>如果 exchange 和 queue 都是持久化的，那么它们之间的 binding 也是持久化的，如果 exchange 和 queue 两者之间有一个持久化，一个非持久化，则不允许建立绑定.<br><strong>注意</strong>：一旦创建了队列和交换机，就不能修改其标志了，例如，创建了一个 non-durable 的队列，然后想把它改变成 durable 的，唯一的办法就是删除这个队列然后重现创建。</p><p><u>关于持久化的进一步讨论：</u><br>为了数据不丢失，我们采用了：<br>在数据处理结束后发送ack，这样RabbitMQ Server会认为Message Deliver 成功。<br>持久化queue，可以防止RabbitMQ Server 重启或者crash引起的数据丢失。<br>持久化Message，理由同上。</p><p>但是这样能保证数据100%不丢失吗？答案是<strong>否定</strong>的。问题就在与RabbitMQ需要时间去把这些信息存到磁盘上，这个time window虽然短，但是它的确还是有。在这个时间窗口内如果数据没有保存，数据还会丢失。还有另一个原因就是RabbitMQ并不是为每个Message都做fsync：它可能仅仅是把它保存到Cache里，还没来得及保存到物理磁盘上。因此这个持久化还是有问题。但是对于大多数应用来说，这已经足够了。当然为了保持一致性，你可以把每次的publish放到一个transaction中。这个transaction的实现需要user defined codes。那么商业系统会做什么呢？一种可能的方案是在系统panic时或者异常重启时或者断电时，应该给各个应用留出时间去flash cache，保证每个应用都能exit gracefully。</p><p>4、<strong>Fair dispath 公平分发</strong><br>你可能也注意到了，分发机制不是那么优雅，默认状态下，RabbitMQ 将第 n 个 Message 分发给第 n 个 Consumer。n 是取余后的，它不管 Consumer 是否还有 unacked Message，只是按照这个默认的机制进行分发.<br>那么如果有个 Consumer 工作比较重，那么就会导致有的 Consumer 基本没事可做，有的 Consumer 却毫无休息的机会，那么，Rabbit 是如何处理这种问题呢？</p><p>4.1 Prefetch count<br>前面我们讲到如果有多个消费者同时订阅同一个 Queue 中的消息，Queue 中的消息会被平摊给多个消费者。这时如果每个消息的处理时间不同，就有可能会导致某些消费者一直在忙，而另外一些消费者很快就处理完手头工作并一直空闲的情况。我们可以通过设置 prefetchCount 来限制 Queue 每次发送给每个消费者的消息数，比如我们设置 prefetchCount=1，则 Queue 每次给每个消费者发送一条消息；消费者处理完这条消息后 Queue 会再给该消费者发送一条消息。</p><p>通过 basic.qos 方法设置 prefetch_count=1，这样 RabbitMQ 就会使得每个 Consumer 在同一个时间点最多处理一个 Message，换句话说，在接收到该 Consumer 的 ack 前，它不会将新的 Message 分发给它</p><p>1<br>channel.basic_qos(prefetch_count=1)</p><p>注意，这种方法可能会导致 queue 满。当然，这种情况下你可能需要添加更多的 Consumer，或者创建更多的 virtualHost 来细化你的设计。</p><h3 id="消息序列化"><a href="#消息序列化" class="headerlink" title="消息序列化"></a>消息序列化</h3><p>RabbitMQ 使用 ProtoBuf 序列化消息，它可作为 RabbitMQ 的 Message 的数据格式进行传输，由于是结构化的数据，这样就极大的方便了 Consumer 的数据高效处理，当然也可以使用 XML，与 XML 相比，ProtoBuf 有以下优势:</p><ol><li>简单</li><li>size 小了 3-10 倍</li><li>速度快了 20-100 倍</li><li>易于编程</li><li>减少了语义的歧义.</li></ol><p>ProtoBuf 具有速度和空间的优势，使得它现在应用非常广泛</p><h3 id="RPC"><a href="#RPC" class="headerlink" title="RPC"></a>RPC</h3><p>MQ 本身是基于异步的消息处理，前面的示例中所有的生产者（P）将消息发送到 RabbitMQ 后不会知道消费者（C）处理成功或者失败（甚至连有没有消费者来处理这条消息都不知道）。 但实际的应用场景中，我们很可能需要一些同步处理，需要同步等待服务端将我的消息处理完成后再进行下一步处理。这相当于 RPC（Remote Procedure Call，远程过程调用）。在 RabbitMQ 中也支持 RPC。<br>RabbitMQ 中实现 RPC 的机制是：<br>客户端发送请求（消息）时，在消息的属性（MessageProperties ，在 AMQP 协议中定义了 14 中 properties ，这些属性会随着消息一起发送）中设置两个值 replyTo （一个 Queue 名称，用于告诉服务器处理完成后将通知我的消息发送到这个 Queue 中）和 correlationId （此次请求的标识号，服务器处理完成后需要将此属性返还，客户端将根据这个 id 了解哪条请求被成功执行了或执行失败）<br>服务器端收到消息并处理<br>服务器端处理完消息后，将生成一条应答消息到 replyTo 指定的 Queue ，同时带上 correlationId 属性<br>客户端之前已订阅 replyTo 指定的 Queue ，从中收到服务器的应答消息后，根据其中的 correlationId 属性分析哪条请求被执行了，根据执行结果进行后续业务处理。</p><h3 id="RabbitMQ-选型和对比"><a href="#RabbitMQ-选型和对比" class="headerlink" title="RabbitMQ 选型和对比"></a>RabbitMQ 选型和对比</h3><ol><li><p>从社区活跃度</p><ul><li>按照目前网络上的资料，RabbitMQ 、activeM 、ZeroMQ 三者中，综合来看， RabbitMQ 是首选。</li></ul></li><li><p>持久化消息比较</p><ul><li>ZeroMq 不支持，ActiveMq 和 RabbitMq 都支持。持久化消息主要是指我们机器在不可抗力因素等情况下挂掉了，消息不会丢失的机制。</li></ul></li><li><p>综合技术实现</p><ul><li>可靠性、灵活的路由、集群、事务、高可用的队列、消息排序、问题追踪、可视化管理工具、插件系统等等。<br>  RabbitMq / Kafka 最好，ActiveMq 次之，ZeroMq 最差。当然 ZeroMq 也可以做到，不过自己必须手动写代码实现，代码量不小。尤其是可靠性中的：持久性、投递确认、发布者证实和高可用性。</li></ul></li><li><p>高并发</p><ul><li>毋庸置疑，RabbitMQ 最高，原因是它的实现语言是天生具备高并发高可用的 erlang 语言。</li></ul></li><li><p>比较关注的比较， RabbitMQ 和 Kafka</p><ul><li>RabbitMq 比 Kafka 成熟，在可用性上，稳定性上，可靠性上， RabbitMq 胜于 Kafka （理论上）。</li><li>另外，Kafka 的定位主要在日志等方面， 因为 Kafka 设计的初衷就是处理日志的，可以看做是一个日志（消息）系统一个重要组件，针对性很强，所以 如果业务方面还是建议选择 RabbitMq 。</li><li>还有就是，Kafka 的性能（吞吐量、TPS ）比 RabbitMq 要高出来很多。</li></ul></li></ol><p>选型最后总结：<br>如果我们系统中已经有选择 Kafka ，或者 RabbitMq ，并且完全可以满足现在的业务，建议就不用重复去增加和造轮子。可以在 Kafka 和 RabbitMq 中选择一个适合自己团队和业务的，这个才是最重要的。但是毋庸置疑现阶段，综合考虑没有第三选择。</p><h3 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h3><p>原文作者：arunfung<br>转自链接：<a href="https://learnku.com/articles/27446" target="_blank" rel="noopener">https://learnku.com/articles/27446</a><br>版权声明：著作权归作者所有。商业转载请联系作者获得授权，非商业转载请保留以上作者信息和原文链接。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;RabbitMQ 是一个由 erlang 开发的 AMQP (Advanced Message Queuing Protocol) 的开源实现。&lt;/p&gt;
&lt;p&gt;AMQP ：高级消息队列协议，是应用层协议的一个开放标准，为面向消息的中间件设计。消息中间件主要用于组件之间的解耦，消息的发送者无需知道消息使用者的存在，反之亦然。 AMQP 的主要特征是面向消息、队列、路由（包括点对点和发布 / 订阅）、可靠性、安全。 RabbitMQ 是一个开源的 AMQP 实现，服务器端用 Erlang 语言编写，支持多种客户端，如：Python、Ruby、.NET、Java、JMS、C、PHP、ActionScript、XMPP、STOMP 等，支持 AJAX。用于在分布式系统中存储转发消息，在易用性、扩展性、高可用性等方面表现不俗。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>ZooKeeper</title>
    <link href="http://yoursite.com/2020/08/22/ZooKeeper/"/>
    <id>http://yoursite.com/2020/08/22/ZooKeeper/</id>
    <published>2020-08-23T03:18:56.000Z</published>
    <updated>2020-09-11T01:58:01.955Z</updated>
    
    <content type="html"><![CDATA[<p>提到ZooKeeper，相信大家都不会陌生。Dubbo, Kafka, Hadoop等等项目里都能看到它的影子。但是你真的了解 ZooKeeper 吗？如果面试官让你给他讲讲 ZooKeeper 是个什么东西，你能回答到什么地步呢？<br>这篇文章是概念性的认识，你会得到 ZooKeeper 是什么，ZooKeeper 设计的目标，ZooKeeper 能做什么和ZooKeeper 基本的概念。</p><a id="more"></a><h3 id="一、ZooKeeper是什么"><a href="#一、ZooKeeper是什么" class="headerlink" title="一、ZooKeeper是什么"></a>一、ZooKeeper是什么</h3><p>ZooKeeper 是一个分布式的，开放源码的分布式应用程序协调服务，是Google的Chubby一个开源的实现，是Hadoop和Hbase的重要组件。它是一个为分布式应用提供一致性服务的软件，提供的功能包括：配置维护、域名服务、分布式同步、组服务等。</p><p>官网：<a href="http://zookeeper.apache.org/" target="_blank" rel="noopener">http://zookeeper.apache.org/</a></p><p>源码：<a href="https://github.com/apache/zookeeper" target="_blank" rel="noopener">https://github.com/apache/zookeeper</a></p><h3 id="二、ZooKeeper-的由来"><a href="#二、ZooKeeper-的由来" class="headerlink" title="二、ZooKeeper 的由来"></a>二、ZooKeeper 的由来</h3><p><strong>下面这段内容摘自《从Paxos到Zookeeper 》 ，本文中很多的名词介绍也来自本书。</strong></p><blockquote><p>Zookeeper 最早起源于雅虎研究院的一个研究小组。在当时，研究人员发现，在雅虎内部很多大型系统基本都需要依赖一个类似的系统来进行分布式协调，但是这些系统往往都存在分布式单点问题。所以，雅虎的开发人员就试图开发一个通用的无单点问题的分布式协调框架，以便让开发人员将精力集中在处理业务逻辑上。<br>关于“ZooKeeper”这个项目的名字，其实也有一段趣闻。在立项初期，考虑到之前内部很多项目都是使用动物的名字来命名的（例如著名的Pig项目),雅虎的工程师希望给这个项目也取一个动物的名字。时任研究院的首席科学家 RaghuRamakrishnan 开玩笑地说：“在这样下去，我们这儿就变成动物园了！”此话一出，大家纷纷表示就叫动物园管理员吧一一一因为各个以动物命名的分布式组件放在一起，雅虎的整个分布式系统看上去就像一个大型的动物园了，而 Zookeeper 正好要用来进行分布式环境的协调一一于是，Zookeeper 的名字也就由此诞生了。</p></blockquote><h3 id="三、ZooKeeper的特性"><a href="#三、ZooKeeper的特性" class="headerlink" title="三、ZooKeeper的特性"></a>三、ZooKeeper的特性</h3><ul><li>顺序一致性，从同一个客户端发起的事务请求，最终将会严格地按照其发起顺序被应用到Zookeeper中去。</li><li>原子性，所有事务请求的处理结果在整个集群中所有机器上的应用情况是一致的，即整个集群要么都成功应用了某个事务，要么都没有应用。</li><li>单一视图，无论客户端连接的是哪个 Zookeeper 服务器，其看到的服务端数据模型都是一致的。</li><li>可靠性，一旦服务端成功地应用了一个事务，并完成对客户端的响应，那么该事务所引起的服务端状态变更将会一直被保留，除非有另一个事务对其进行了变更。</li><li>实时性，Zookeeper 保证在一定的时间段内，客户端最终一定能够从服务端上读取到最新的数据状态。</li></ul><h3 id="四、ZooKeeper的设计目标"><a href="#四、ZooKeeper的设计目标" class="headerlink" title="四、ZooKeeper的设计目标"></a>四、ZooKeeper的设计目标</h3><ul><li>简单的数据结构<br>  Zookeeper 使得分布式程序能够通过一个共享的树形结构的名字空间来进行相互协调，即Zookeeper 服务器内存中的数据模型由一系列被称为ZNode的数据节点组成，Zookeeper 将全量的数据存储在内存中，以此来提高服务器吞吐、减少延迟的目的。</li><li>可以构建集群<br>  Zookeeper 集群通常由一组机器构成，组成 Zookeeper 集群的而每台机器都会在内存中维护当前服务器状态，并且每台机器之间都相互通信。</li><li>顺序访问<br>  对于来自客户端的每个更新请求，Zookeeper 都会分配一个全局唯一的递增编号，这个编号反映了所有事务操作的先后顺序。</li><li>高性能<br>  Zookeeper 和Redis一样全量数据存储在内存中，100%读请求压测QPS 12-13W。</li></ul><h3 id="五、关于-ZooKeeper-的一些重要概念"><a href="#五、关于-ZooKeeper-的一些重要概念" class="headerlink" title="五、关于 ZooKeeper 的一些重要概念"></a>五、关于 ZooKeeper 的一些重要概念</h3><h4 id="5-1-Zookeeper-集群"><a href="#5-1-Zookeeper-集群" class="headerlink" title="5.1 Zookeeper 集群"></a>5.1 Zookeeper 集群</h4><p>Zookeeper是一个由多个server组成的集群, 一个leader，多个follower。（这个不同于我们常见的Master/Slave模式）leader为客户端服务器提供读写服务，除了leader外其他的机器只能提供读服务。<br>每个 server 保存一份数据副本全数据一致，分布式读 follower，写由 leader 实施更新请求转发，由 leader 实施更新请求顺序进行，来自同一个 client 的更新请求按其发送顺序依次执行数据更新原子性，一次数据更新要么成功，要么失败。全局唯一数据视图，client 无论连接到哪个 server，数据视图都是一致的实时性，在一定事件范围内，client 能读到最新数据。</p><h4 id="5-2-集群角色"><a href="#5-2-集群角色" class="headerlink" title="5.2 集群角色"></a>5.2 集群角色</h4><p><strong>Leader</strong>：是整个 Zookeeper 集群工作机制中的核心 。Leader 作为整个 ZooKeeper 集群的主节点，负责响应所有对 ZooKeeper 状态变更的请求。<br>主要工作：</p><ul><li>事务请求的唯一调度和处理，保障集群处理事务的顺序性。</li><li>集群内各服务器的调度者。</li></ul><p>Leader 选举是 Zookeeper 最重要的技术之一，也是保障分布式数据一致性的关键所在。我们以三台机器为例，在服务器集群初始化阶段，当有一台服务器Server1启动时候是无法完成选举的，当第二台机器 Server2 启动后两台机器能互相通信，每台机器都试图找到一个leader，于是便进入了 leader 选举流程.</p><ol><li>每个 server 发出一个投票<br> 投票的最基本元素是（SID-服务器id,ZXID-事务id）</li><li>接受来自各个服务器的投票</li><li>处理投票<br> 优先检查 ZXID(数据越新ZXID越大), ZXID比较大的作为leader，ZXID一样的情况下比较SID</li><li>统计投票<br> 这里有个<strong>过半</strong>的概念，大于集群机器数量的一半，即大于或等于（n/2+1）,我们这里的由三台，大于等于2即为达到“过半”的要求。<br> 这里也有引申到为什么 Zookeeper 集群推荐是单数。</li></ol><table><thead><tr><th align="left">集群数量</th><th align="left">至少正常运行数量</th><th align="left">允许挂掉的数量</th></tr></thead><tbody><tr><td align="left">2</td><td align="left">2的半数为1，半数以上最少为2</td><td align="left">0</td></tr><tr><td align="left">3</td><td align="left">3的半数为1.5，半数以上最少为2</td><td align="left">1</td></tr><tr><td align="left">4</td><td align="left">4的半数为2，半数以上最少为3</td><td align="left">1</td></tr><tr><td align="left">5</td><td align="left">5的半数为2.5，半数以上最少为3</td><td align="left">2</td></tr><tr><td align="left">6</td><td align="left">6的半数为3，半数以上最少为4</td><td align="left">2</td></tr></tbody></table><p>通过以上可以发现，3台服务器和4台服务器都最多允许1台服务器挂掉，5台服务器和6台服务器都最多允许2台服务器挂掉,明显4台服务器成本高于3台服务器成本，6台服务器成本高于5服务器成本。这是由于半数以上投票通过决定的。</p><ol><li>改变服务器状态<br> 一旦确定了 leader，服务器就会更改自己的状态，且一半不会再发生变化，比如新机器加入集群、非 leader 挂掉一台。</li></ol><p><strong>Follower</strong> ：是 Zookeeper 集群状态的跟随者。他的逻辑就比较简单。除了响应本服务器上的读请求外，follower 还要处理leader 的提议，并在 leader 提交该提议时在本地也进行提交。另外需要注意的是，leader 和 follower 构成ZooKeeper 集群的法定人数，也就是说，只有他们才参与新 leader的选举、响应 leader 的提议。</p><p><strong>Observer</strong> ：服务器充当一个观察者的角色。如果 ZooKeeper 集群的读取负载很高，或者客户端多到跨机房，可以设置一些 observer 服务器，以提高读取的吞吐量。Observer 和 Follower 比较相似，只有一些小区别：首先 observer 不属于法定人数，即不参加选举也不响应提议，也不参与写操作的“<strong>过半写成功</strong>”策略；其次是 observer 不需要将事务持久化到磁盘，一旦 observer 被重启，需要从 leader 重新同步整个名字空间。</p><h4 id="5-3会话（Session）"><a href="#5-3会话（Session）" class="headerlink" title="5.3会话（Session）"></a>5.3会话（Session）</h4><p>Session 指的是 ZooKeeper 服务器与客户端会话。在 ZooKeeper 中，一个客户端连接是指客户端和服务器之间的一个 TCP 长连接。客户端启动的时候，首先会与服务器建立一个 TCP 连接，从第一次连接建立开始，客户端会话的生命周期也开始了。通过这个连接，客户端能够通过心跳检测与服务器保持有效的会话，也能够向Zookeeper 服务器发送请求并接受响应，同时还能够通过该连接接收来自服务器的Watch事件通知。 Session 的 sessionTimeout 值用来设置一个客户端会话的超时时间。当由于服务器压力太大、网络故障或是客户端主动断开连接等各种原因导致客户端连接断开时，只要在sessionTimeout规定的时间内能够重新连接上集群中任意一台服务器，那么之前创建的会话仍然有效。在为客户端创建会话之前，服务端首先会为每个客户端都分配一个sessionID。由于 sessionID 是 Zookeeper 会话的一个重要标识，许多与会话相关的运行机制都是基于这个 sessionID 的，因此，无论是哪台服务器为客户端分配的 sessionID，都务必保证全局唯一。</p><h4 id="5-3-1-会话（Session）"><a href="#5-3-1-会话（Session）" class="headerlink" title="5.3.1 会话（Session）"></a>5.3.1 会话（Session）</h4><p>在Zookeeper客户端与服务端成功完成连接创建后，就创建了一个会话，Zookeeper会话在整个运行期间的生命周期中，会在不同的会话状态中之间进行切换，这些状态可以分为CONNECTING、CONNECTED、RECONNECTING、RECONNECTED、CLOSE等。</p><p>一旦客户端开始创建Zookeeper对象，那么客户端状态就会变成CONNECTING状态，同时客户端开始尝试连接服务端，连接成功后，客户端状态变为CONNECTED，通常情况下，由于断网或其他原因，客户端与服务端之间会出现断开情况，一旦碰到这种情况，Zookeeper客户端会自动进行重连服务，同时客户端状态再次变成CONNCTING，直到重新连上服务端后，状态又变为CONNECTED，在通常情况下，客户端的状态总是介于CONNECTING 和CONNECTED 之间。但是，如果出现诸如会话超时、权限检查或是客户端主动退出程序等情况，客户端的状态就会直接变更为CLOSE状态。</p><h4 id="5-3-2-会话创建"><a href="#5-3-2-会话创建" class="headerlink" title="5.3.2 会话创建"></a>5.3.2 会话创建</h4><p>Session是Zookeeper中的会话实体，代表了一个客户端会话，其包含了如下四个属性</p><ol><li>sessionID。会话ID，唯一标识一个会话，每次客户端创建新的会话时，Zookeeper都会为其分配一个全局唯一的sessionID。</li><li>TimeOut。会话超时时间，客户端在构造Zookeeper实例时，会配置sessionTimeout参数用于指定会话的超时时间，Zookeeper客户端向服务端发送这个超时时间后，服务端会根据自己的超时时间限制最终确定会话的超时时间。</li><li>TickTime。下次会话超时时间点，为了便于Zookeeper对会话实行”分桶策略”管理，同时为了高效低耗地实现会话的超时检查与清理，Zookeeper会为每个会话标记一个下次会话超时时间点，其值大致等于当前时间加上TimeOut。</li><li>isClosing。标记一个会话是否已经被关闭，当服务端检测到会话已经超时失效时，会将该会话的isClosing标记为”已关闭”，这样就能确保不再处理来自该会话的心情求了。</li></ol><p>Zookeeper为了保证请求会话的全局唯一性，在SessionTracker初始化时，调用initializeNextSession方法生成一个sessionID，之后在Zookeeper运行过程中，会在该sessionID的基础上为每个会话进行分配，初始化算法如下</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">initializeNextSession</span><span class="params">(<span class="keyword">long</span> id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">long</span> nextSid = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// 无符号右移8位使为了避免左移24后，再右移8位出现负数而无法通过高8位确定sid值</span></span><br><span class="line">  nextSid = (System.currentTimeMillis() &lt;&lt; <span class="number">24</span>) &gt;&gt;&gt; <span class="number">8</span>;</span><br><span class="line">  nextSid = nextSid | (id &lt;&lt; <span class="number">56</span>);</span><br><span class="line">  <span class="keyword">return</span> nextSid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="5-3-3-会话管理"><a href="#5-3-3-会话管理" class="headerlink" title="5.3.3 会话管理"></a>5.3.3 会话管理</h4><p>Zookeeper的会话管理主要是通过SessionTracker来负责，其采用了<strong>分桶策略</strong>（将类似的会话放在同一区块中进行管理）进行管理，以便Zookeeper对会话进行不同区块的隔离处理以及同一区块的统一处理。</p><h3 id="5-4-数据节点-Znode"><a href="#5-4-数据节点-Znode" class="headerlink" title="5.4 数据节点 Znode"></a>5.4 数据节点 Znode</h3><p>在Zookeeper中，“节点”分为两类，第一类同样是指构成集群的机器，我们称之为机器节点；第二类则是指数据模型中的数据单元，我们称之为数据节点一一ZNode。<br>Zookeeper将所有数据存储在内存中，数据模型是一棵树（Znode Tree)，由斜杠（/）的进行分割的路径，就是一个Znode，例如/foo/path1。每个上都会保存自己的数据内容，同时还会保存一系列属性信息。</p><h5 id="5-4-1-节点类型"><a href="#5-4-1-节点类型" class="headerlink" title="5.4.1 节点类型"></a>5.4.1 节点类型</h5><p>在Zookeeper中，node可以分为持久节点和临时节点和顺序节点三大类。<br>可以通过组合生成如下四种类型节点</p><ol><li>PERSISTENT<br> 持久节点,节点创建后便一直存在于Zookeeper服务器上，直到有删除操作来主动清除该节点。</li><li>PERSISTENT_SEQUENTIAL<br> 持久顺序节点, 相比持久节点，其新增了顺序特性，每个父节点都会为它的第一级子节点维护一份顺序，用于记录每个子节点创建的先后顺序。在创建节点时，会自动添加一个数字后缀，作为新的节点名，该数字后缀的上限是整形的最大值。</li><li>EPEMERAL<br> 临时节点，临时节点的生命周期与客户端会话绑定，客户端失效，节点会被自动清理。同时，Zookeeper规定不能基于临时节点来创建子节点，即临时节点只能作为叶子节点。</li><li>EPEMERAL_SEQUENTIAL<br> 临时顺序节点,在临时节点的基础添加了顺序特性。</li></ol><h4 id="5-5-版本——保证分布式数据原子性操作"><a href="#5-5-版本——保证分布式数据原子性操作" class="headerlink" title="5.5 版本——保证分布式数据原子性操作"></a>5.5 版本——保证分布式数据原子性操作</h4><p>每个数据节点都具有三种类型的版本信息，对数据节点的任何更新操作都会引起版本号的变化。</p><p>version– 当前数据节点数据内容的版本号<br>cversion– 当前数据子节点的版本号<br>aversion– 当前数据节点ACL变更版本号</p><p>上述各版本号都是表示修改次数，如version为1表示对数据节点的内容变更了一次。即使前后两次变更并没有改变数据内容，version的值仍然会改变。version可以用于写入验证，类似于CAS。</p><h4 id="5-6watcher事件监听器"><a href="#5-6watcher事件监听器" class="headerlink" title="5.6watcher事件监听器"></a>5.6watcher事件监听器</h4><p>ZooKeeper允许用户在指定节点上注册一些Watcher，当数据节点发生变化的时候，ZooKeeper服务器会把这个变化的通知发送给感兴趣的客户端</p><h4 id="5-7-ACL-权限控制——保障数据的安全"><a href="#5-7-ACL-权限控制——保障数据的安全" class="headerlink" title="5.7 ACL 权限控制——保障数据的安全"></a>5.7 ACL 权限控制——保障数据的安全</h4><p>ACL是Access Control Lists 的简写， ZooKeeper采用ACL策略来进行权限控制，有以下权限：<br>CREATE: 创建子节点的权限<br>READ: 获取节点数据和子节点列表的权限<br>WRITE: 更新节点数据的权限<br>DELETE: 删除子节点的权限<br>ADMIN: 设置节点ACL的权限</p><h4 id="5-8-Paxos算法"><a href="#5-8-Paxos算法" class="headerlink" title="5.8 Paxos算法"></a>5.8 Paxos算法</h4><p>Paxos算法是基于消息传递且具有高度容错特性的一致性算法，是目前公认的解决分布式一致性问题最有效的算法之一。（其他算法有二阶段提交、三阶段提交等）<br>篇幅较长 可以参考<a href="https://www.cnblogs.com/linbingdong/p/6253479.html" target="_blank" rel="noopener">https://www.cnblogs.com/linbingdong/p/6253479.html</a></p><h3 id="六、ZooKeeper-可以做什么？"><a href="#六、ZooKeeper-可以做什么？" class="headerlink" title="六、ZooKeeper 可以做什么？"></a>六、ZooKeeper 可以做什么？</h3><ul><li>分布式服务注册与订阅<ul><li>在分布式环境中，为了保证高可用性，通常同一个应用或同一个服务的提供方都会部署多份，达到对等服务。而消费者就须要在这些对等的服务器中选择一个来执行相关的业务逻辑，比较典型的服务注册与订阅，代表：dubbo。</li></ul></li></ul><ul><li><p>分布式配置中心</p><ul><li>发布与订阅模型，即所谓的配置中心，顾名思义就是发布者将数据发布到ZK节点上，供订阅者获取数据，实现配置信息的集中式管理和动态更新。代表：百度的disconf。<br>  github：<a href="https://github.com/knightliao/disconf" target="_blank" rel="noopener">https://github.com/knightliao/disconf</a></li></ul></li><li><p>命名服务</p><ul><li>在分布式系统中，通过使用命名服务，客户端应用能够根据指定名字来获取资源或服务的地址，提供者等信息。被命名的实体通常可以是集群中的机器，提供的服务地址，进程对象等等——这些我们都可以统称他们为名字（Name）。其中较为常见的就是一些分布式服务框架中的服务地址列表。通过调用ZK提供的创建节点的API，能够很容易创建一个全局唯一的path，这个path就可以作为一个名称。</li></ul></li><li><p>分布式锁</p><ul><li>分布式锁，这个主要得益于ZooKeeper为我们保证了数据的强一致性。锁服务可以分为两类，一个是保持独占，另一个是控制时序。</li><li>所谓保持独占，就是所有试图来获取这个锁的客户端，最终只有一个可以成功获得这把锁。通常的做法是把zk上的一个znode看作是一把锁，通过create znode的方式来实现。所有客户端都去创建 /distribute_lock 节点，最终成功创建的那个客户端也即拥有了这把锁。</li><li>控制时序，就是所有视图来获取这个锁的客户端，最终都是会被安排执行，只是有个全局时序了。做法和上面基本类似，只是这里 /distribute_lock 已绊预先存在，客户端在它下面创建临时有序节点（这个可以通过节点的属性控制：CreateMode.EPHEMERAL_SEQUENTIAL来指定）。Zk的父节 (/distribute_lock）维持一份sequence, 保证子节点创建的时序性，从而也形成了每个客户端的全局时序。</li></ul></li><li><p>Master选举</p></li><li><p>负载均衡</p></li></ul><h3 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h3><p>作者：codeyuyu<br>链接：<a href="https://www.imooc.com/article/251135" target="_blank" rel="noopener">https://www.imooc.com/article/251135</a><br>来源：慕课网</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;提到ZooKeeper，相信大家都不会陌生。Dubbo, Kafka, Hadoop等等项目里都能看到它的影子。但是你真的了解 ZooKeeper 吗？如果面试官让你给他讲讲 ZooKeeper 是个什么东西，你能回答到什么地步呢？&lt;br&gt;这篇文章是概念性的认识，你会得到 ZooKeeper 是什么，ZooKeeper 设计的目标，ZooKeeper 能做什么和ZooKeeper 基本的概念。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记8</title>
    <link href="http://yoursite.com/2020/08/21/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B08/"/>
    <id>http://yoursite.com/2020/08/21/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B08/</id>
    <published>2020-08-22T04:36:28.000Z</published>
    <updated>2020-09-11T15:51:38.045Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter6 Partitioning</strong></p><ul><li>The main reason for wanting patition data is <strong>scalability</strong>.</li><li>For <u>very <strong>large dataset</strong></u>, or <u>very <strong>high throughtput</strong></u>, that is not sufficient: we need to break the data into <strong><u>partitions</u>, also known as <u>sharding</u></strong>.<ul><li>What we call a <strong>partition</strong> here is called a <u>shard</u> in <u>MongoDB, ElasticSearch, and SolrClound</u>; it’s known as a <strong>region</strong> in <u>HBase</u>, a <strong>tablet</strong> in <u>Bigtable</u>, a <strong>vnode</strong> in <u>Cassandra</u> and <u>Riak</u>, and a <strong>vBucket</strong> in <u>CouchBase</u>.</li></ul></li><li>Recently discovered by NoSQL databases and Hadoop-based data warehouse.</li></ul><a id="more"></a><h4 id="Partitioning-and-Replication"><a href="#Partitioning-and-Replication" class="headerlink" title="Partitioning and Replication"></a>Partitioning and Replication</h4><ul><li><strong>Partitioning</strong> is usually combined with <strong>replication</strong> so that copies of each partition are stored on multiple nodes.</li><li>A node may store more than one partition.<ul><li>Each partition’s leader is assigned to one node, and its followers are assigned to others nodes.</li><li>Each node may be leader for some partitions and follower for other partitions.</li></ul></li></ul><h4 id="Partitioning-of-Key-Value-Data"><a href="#Partitioning-of-Key-Value-Data" class="headerlink" title="Partitioning of Key-Value Data"></a>Partitioning of Key-Value Data</h4><ul><li>How do you decide which records to store on which nodes?</li><li>If the partitioning is unfair, so that some partitions have more data or queries than others, we call it <strong>skewed</strong>.<ul><li>A partition with disproportionately high load is called a <strong>hot spot</strong>.</li><li>Avoid Hotspot<ul><li>The simplest approach for avoiding hot spots would be assign records to nodes randomly</li><li><strong>disadvantage</strong>: when you’re trying to read a particular item, you have no way of knowing which node it is on, so you have to query all nodes in parellel.</li></ul></li></ul></li><li><strong>Partitioning by Key Range</strong><ul><li>One way of partitioning is to assign a <u>continuous range of keys</u> (from some minimum to some maximum) to each partition, like the volumn of a paper encyclopedia.</li><li>The ranges of keys are not necessarily evenly spaced, because your data may not be evenly distributed.<ul><li>In order to distribute the data evenly, <u>the partition boundaries need to adapt to the data</u>.</li><li>Automatically choose: Bigtable, HBase, RethinkDB, and <strong>MongoDB</strong> (&lt;2.4)</li></ul></li><li>Within each partition, we can keep keys in sorted order. (see “SSTables and LSM-Trees”)</li></ul></li><li><strong>Partioning by Hash of Key</strong>: (aka. <strong>Consistent Hashing</strong>)<ul><li>Because of this risk of skew and hot spots, many distributed datastores <u>use a hash function to determine the partition for a given key</u>.</li><li>A good hash function takes skewed data and makes it <u>uniformly distributed</u>.<ul><li>For partitioning purpose, the hash function need NOT to be cryptographically strong: e.g. <u><strong>Cassandra</strong> and <strong>MongoDB</strong></u> use <strong>MD5</strong>, and <u>Voldemort</u> uses the FowlerNoll-Vo function.</li></ul></li><li>Once you have a suitable hash function for keys, you can <u>assign each partition a range of hashes (rather than a range of keys)</u>, and every key whose hash falls within a partition’s range will be stored in that partition <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598055676/p2-ch6-img1_fgn8fx.png" alt="image-20200821171121981"><ul><li><u>pseudo-randomly</u> (in which case the technique is sometimes known as <strong>consistent hashing</strong>)</li></ul></li><li><strong>Consistent Hashing</strong><ul><li>is a way of evenly distributing load across an internet-wide system of caches such as content delivery network (CDN).</li><li>this particular approach actually doesn’t work very well for databases, so it is rarely used in practice. (C: seems totally not aligned with what some Youtuber was saying…)</li></ul></li><li><u>Downside is we lose a nice property of key-range partitioning</u>: the ability to do efficient range queries. (C: this is very similar that when Partition a MQ, the order of the message will not be aligned)<ul><li><u>In MongoDB</u>, if you have enabled hash-based sharding mode, any <u>range query has to be sent to all partitions</u>.</li><li>Range queries on the primary key are <u>NOT supported</u> by Riak, Couchbase, or Voldemort.</li></ul></li></ul></li><li><u>Cassandra</u> achieves a compromise between the two partitioning strategies.<ul><li>A table in Cassandra can be declared with a compound primary key consisting of several columns.</li><li>Only the first part of that key is hashed to determine the partition, but the other columns are used as a concatenated index for sorting the data in Cassandra’s SSTable.</li><li>The concatenated index approach enables an elegent data model for one-to-many relationships.</li></ul></li><li><strong>Skewed Workloads and Relieving Hot Spots</strong><ul><li>most data system are not able to automatically compensate for such a highly skewed workload, so it’s the responsibility of the application to reduce the skew. (e.g. Celebrity posts)<ul><li>If one key if known to be very hot, a simple technique is to <u><strong>add a random number</strong></u> to the beginning or end of the key.</li><li>However, having split the writes across different keys, any reads now have to do addtional work, as they have to read the data from 100 keys and combine it.</li></ul></li></ul></li></ul><h4 id="Partitioning-and-Secondary-Indexes"><a href="#Partitioning-and-Secondary-Indexes" class="headerlink" title="Partitioning and Secondary Indexes"></a>Partitioning and Secondary Indexes</h4><ul><li>The situation becomes more complicated if <u>secondary indexed</u> are involved.</li><li>A secondary index usually doesn’t identify a record uniquely but rather <u>is a way of searching for occurrences of a particular value</u>.</li><li>Secondary indexes are the bread and butter of relational databases, and they are common in document databases too.</li><li>Many key-value stores (such as HBase and Voldemort) have avoided secondary indexes <ul><li>But some (such as <u>Riak</u>) have</li></ul></li><li>Secondary indexes are the <strong>raison d’etre</strong> (the most important reason or purpose for someone or something’s existence) of search servers such as <u>Solr and Elasticsearch</u>.</li><li><strong>The problem with secondary indexes is that <u>they don’t map neatly partitions</u></strong>.</li><li>There are two main approaches to partitioning a databases with secondary indexes: <strong><u>document-based partitioning</u></strong> <u>and</u> <u><strong>term-based partitoning</strong></u> .</li><li><strong>Partitioning Secondary Indexes <u>by Document</u></strong> (e.g. Used car selling sites)<ul><li>Widely used: <strong><u>MongoDB</u></strong>, Riak, <u><strong>Cassandra</strong></u>, <u>Elasticsearch</u>, SolrCloud, and VoltDB.</li><li>In this indexing approach, each partition is completely seperate: <u>each partition maintains its own secondary indexes, covering only the documents in that partition</u> <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598067772/p2-ch6-img3_rbkciy.png" alt="p2-ch6-img3"></li><li>A document-partitoned index is also known as a <u>local index</u> (as opposed to a <u>global index</u>, described in the next section)<ul><li>You need to send the query to all partitions, and combine all the results you get back</li><li>This approach to querying a partitioned database is sometimes known as <strong>scatter/gather</strong>, and it can make read queries on secondary indexes quite expensive. (prone to tail latency amplification)</li></ul></li></ul></li><li><strong>Partitioning Secondary Indexes by Term</strong><ul><li>We can construct a <strong>global index</strong> that covers data in all partitions.</li><li>A <u>global index</u> must also be partitioned, but it can be partitioned differently from the primary key index.</li><li>We call this kind of index <u>term-partitioned</u>, because the term we’re looking for determines the partition of the index.</li><li><strong>Pros</strong> it can make reads more efficient<ul><li>rather than doing scatter/gather over all partitions, a client only needs to make a request to partition containing the term that it wants.</li></ul></li><li><strong>Cons</strong> it writes are slower and more complicated<ul><li>Because a write to a single document may now affect multiple partitions of the index (every term in the document might be on a different partition, on a different node).</li></ul></li><li>In practice, updates to global secondary indexes are often asynchronous.</li></ul></li></ul><h4 id="Rebalance-Partitioning"><a href="#Rebalance-Partitioning" class="headerlink" title="Rebalance Partitioning"></a>Rebalance Partitioning</h4><ul><li>The process of moving load from one node in the cluster to another is called <strong>rebalancing</strong>.</li><li><strong>Minimum requirements</strong><ul><li>the load (data storage, read and write requests) should be <u>shared fairly</u> between the nodes in the cluster</li><li>While rebalancing is happening, the databases should <u>continue accepting reads and wirtes</u>.</li><li><u>No more data than necessary</u> should be moved between nodes. (fast/minimize load)</li></ul></li><li><strong>Strategies for Rebalancing</strong><ul><li><strong>How Not to do it : hash mod N</strong><ul><li>The problem with the mod N approach is that of the number of nodes N changes, most of the keys will need to be moved from one node to another.</li></ul></li><li><strong>1, Fixed number of partitions</strong>: used in Riak, <u>Elasticsearch</u>, Couchbase, and Voldemort<ul><li>create many more partitions that there are nodes and assign several partitions to each one.<ul><li>If a node is added to the cluster, the new node can steal a few partitions from every existing node until partitions are fairly distributed once again.</li></ul></li><li>the number of partitions configured at the outset is the maximum number of nodes you can have</li></ul></li><li><strong>2, Dynamic partitioning</strong>: key range partitoned databases such as <u>HBase (in HDFS)</u> and <u>RethinkDB</u> create partitons dynamically. <u><strong>MongoDB</strong></u>(&gt;2.4)<ul><li>Each partition is assigned to one node, and each node can handle multiple partitions, like in the case of a fixed number of partitions.</li><li><strong>An advantage</strong> of dynamic partitioning is that the nunber of partitions adapts to the total data volume.</li><li><strong>A caveat</strong> is that an empty database starts off with a single partition, since there is no a prior information about where to draw the partition boundaries. (mitigated by “pre-splitting”)</li></ul></li><li><strong>3, Partitioning proportionally to nodes</strong>: used by <u><strong>Cassandra</strong></u> and Ketama<ul><li>In other words, to <u>have a fixed number of partitions per node</u>. This approach also keeps the size of each partition fairly stable.</li></ul></li></ul></li><li><strong>Operations: Automatic or Manual Rebalancing</strong><ul><li><u>Couchbase, Riak, and Voldemort</u> generate a suggested partition assignment automatically, but require an administrator to commit it before it takes effect.</li><li>Fully automated rebalancing can be convenient, but it is <u>unpredictable</u>. (Consider rebalancing is a large overhaul), especially dangerous when combined with automatic failure detection.</li></ul></li></ul><h4 id="Request-Routing-e-g-ZooKeeper"><a href="#Request-Routing-e-g-ZooKeeper" class="headerlink" title="Request Routing (e.g. ZooKeeper)"></a>Request Routing (e.g. ZooKeeper)</h4><ul><li>when  a client wants to make a request, how does it know which node to connect to?<ul><li>This is an instance of a more general problem called <u><strong>service discovery</strong></u>, which isn’t limited to just databases. (Common problem among “high availability” network applications)</li></ul></li><li>Few different approaches<ul><li>Allow clients to <u>contact any</u> node (e.g., via a round-robin load balancer)</li><li>Send all requests from clients to a <u>routing tier</u> first, which determines the node that should handle each request and forwards it accordingly.</li><li>Requires that clients <u>be aware of the partitioning</u> and the assignment of partitions to nodes.</li></ul></li><li>How does the component making the routing decision (which may be one of the nodes, or the routing tier, or the client) learn about changes in the assignment of partitions to nodes?<ul><li>Hard to handle, but many <u>distributed systems</u> rely on a seperate coordination services such as <strong>Zookeeper</strong> to keep track of this cluster metadata. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1598070041/p2-ch6-img2_rwkqzo.png" alt="p2-ch6-img2"></li><li><strong>Zookeeper</strong> maintains the authorizative mapping of partitions to nodes. Whenever a partition changes ownership, or a node is added or removed, <strong><u>Zookeeper</u> notifies the routing tier</strong> so that it can keep its routing information up to date.</li><li>LinkedIn’s <u>Espresso</u> uses <u>Helix</u> for cluster management (which in turn relies on Zookeeper).</li><li><u>HBase, SolrCloud, and <strong>Kafka</strong></u> also use Zookeeper to track partition assignment <ul><li><strong><u>MongoDB</u></strong> has a simpler architecture </li><li>(C: Latest version of Kafka seems removed Zookeeper)</li></ul></li><li><u><strong>Cassandra</strong> and Riak</u> take a different approach: they use a <u><strong>gossip</strong> protocol</u> among the nodes to disseminate any changes in cluster state.<ul><li>This model puts more complexity in the database nodes but <u>avoids the dependency on an external coordination service</u> such as Zookeeper.</li></ul></li><li><u>Couchbase</u> does not rebalance automatically, which simplifies the design.</li></ul></li><li><strong>Parallel Query Execution</strong><ul><li><strong>massive parallel processing</strong> (MPP) relational database products, often used for analytics, are much more sophisticated in the types of queries they support.</li></ul></li></ul><h4 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h4><ul><li>The goal of partitioning is to <u>spread the data and query load evenly across multiple machines, avoiding hot spots</u> (nodes with disproportionately high load).</li><li>Two main approaches to partitioning <ul><li><strong>Key range partitioning</strong>, where keys are sorted, and a partition owns all the keys from some minimum up to some maximum.<ul><li>partitions are typically rebalanced dynamically by splitting</li><li>Risk of hot spots</li></ul></li><li><strong>Hash partitioning</strong>, where a hash function is applied to each key, and a partition owns a range of hashes.<ul><li>partitioning by hash, it is common to create a fixed number of partition in advance</li><li>Destroy the ordering of keys.</li></ul></li></ul></li><li>A secondary index also needs to be partitioned<ul><li><strong>Document-partitioned indexes (local indexes)</strong>, where the secondary indexes are stored in the same partition as the primary key and value.</li><li><strong>Term-partitioned indexes (global indexes)</strong>, where the secondary indexes are partitioned separately, using the indexed values.</li></ul></li><li>Operations that need to write to several partitions can be difficult to reason about: for example, what happens if the write to one partition succeeds, but another fails?</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter6 Partitioning&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;The main reason for wanting patition data is &lt;strong&gt;scalability&lt;/strong&gt;.&lt;/li&gt;
&lt;li&gt;For &lt;u&gt;very &lt;strong&gt;large dataset&lt;/strong&gt;&lt;/u&gt;, or &lt;u&gt;very &lt;strong&gt;high throughtput&lt;/strong&gt;&lt;/u&gt;, that is not sufficient: we need to break the data into &lt;strong&gt;&lt;u&gt;partitions&lt;/u&gt;, also known as &lt;u&gt;sharding&lt;/u&gt;&lt;/strong&gt;.&lt;ul&gt;
&lt;li&gt;What we call a &lt;strong&gt;partition&lt;/strong&gt; here is called a &lt;u&gt;shard&lt;/u&gt; in &lt;u&gt;MongoDB, ElasticSearch, and SolrClound&lt;/u&gt;; it’s known as a &lt;strong&gt;region&lt;/strong&gt; in &lt;u&gt;HBase&lt;/u&gt;, a &lt;strong&gt;tablet&lt;/strong&gt; in &lt;u&gt;Bigtable&lt;/u&gt;, a &lt;strong&gt;vnode&lt;/strong&gt; in &lt;u&gt;Cassandra&lt;/u&gt; and &lt;u&gt;Riak&lt;/u&gt;, and a &lt;strong&gt;vBucket&lt;/strong&gt; in &lt;u&gt;CouchBase&lt;/u&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;Recently discovered by NoSQL databases and Hadoop-based data warehouse.&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记7</title>
    <link href="http://yoursite.com/2020/08/19/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B07/"/>
    <id>http://yoursite.com/2020/08/19/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B07/</id>
    <published>2020-08-20T05:43:50.000Z</published>
    <updated>2020-09-11T03:35:14.207Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter5 Replication</strong></p><ul><li><strong>Replication</strong> means keeping a <u>copy of the same data on multiple machines</u> that are connected via a network.<ul><li>To keep data geographically close to your users (<strong>reduce latency</strong>)</li><li>To allow the system to continue working even if some if its parts have failed (<strong>increase availability</strong>)</li><li>To scale out the number of machines that can serve read queries (<strong>increase read throughput</strong>)</li></ul></li><li><strong>All of the <u>difficulty</u> in replication lies in <u>handling changes to replicated</u> data</strong>;</li><li>Three popular algorithms for replicating changes between nodes: <strong>single-leader</strong>, <strong>multi-leader</strong>, and <strong>leaderless</strong> replication.</li></ul><a id="more"></a><h3 id="Leaders-and-Followers"><a href="#Leaders-and-Followers" class="headerlink" title="Leaders and Followers"></a>Leaders and Followers</h3><ul><li>Each node that stores a copy of databases is called a <strong>replica</strong>;</li><li>How do we ensure that all the data ends up on all the replicas?</li><li>The most common sloution for this is called <u>leader-based</u> replication (also known as active/passive or master/slave replication)<ul><li>One of the replicas is designated the <strong>leader</strong> (also know as <strong>master</strong> or <strong>primary</strong>).</li><li>The other replicas are known as <strong>followers</strong> (read replicas, <strong>slaves</strong>, secondaries, or hot standbys).</li><li>When a client wants to read from the database, it can query either leader or any of the followers.</li><li>E.g. <ul><li><strong>Relational DB</strong>: <u>PostgreSQL</u> (since version 9.0), MySQL, Oracle Data Guard, and SQL Server’s AlwaysOn Availability Groups.</li><li><strong>Non-Relational DB</strong>: <u>MondoDB</u>, RethinkDB, and Expresso</li><li><strong>Message Broker</strong>: <u>Kafka</u> and RabbitMQ highly available queues</li></ul></li></ul></li><li><strong>Synchronous vs. Asynchronous Replication</strong>: In <u>relational</u> databases, this is often a configurable option; other system are often hardcoded to be either one or the other.<ul><li>Most databases system apply changes to followers in less than a second.</li><li><strong>Synchronous</strong><ul><li><u>Advantage</u>s: The followers is guaranteed to have an up-to-date copy of the data that is consistent with the leader.</li><li><u>Disadvantages</u>: if the synchronous follower doesn’t respond (because it has crashed, or there is a network fault, or for any other reason), the write cannot be processed</li><li><strong>impractical</strong> for <u>all followers to be synonymous</u>: any one node outage would cause the whole system and grind to halt.</li></ul></li><li><strong>Semi-synchronous</strong>: leader and one synchronous follower.</li><li><strong>Often,</strong> <u>leader-based replcation is configured to be completely</u> <strong>asynchronous</strong>.<ul><li>a fully asynchronous configuration has the advantage that the leader can continue processiong writes, even if all of its followers have fallen behind.</li><li>Especially if there are many followers or if they are geographically distributed.</li></ul></li></ul></li><li><strong>Setting Up New Followers</strong><ul><li>How do you ensure that the new followers has an accurate copy of the leader’s data?</li><li>Four Step process: 1, take snapshot; 2, copy snapshot; 3, carry-over new change by using leader’s <u>replication log</u> (e.g. log sequence number, and MySQL calls it the binlog coordinates.); 4, after caught up, keep process data from leader;<ul><li>The practical steps of setting up a follower vary significantly by database.</li></ul></li></ul></li><li><strong>Handling Node Outages</strong>: our goal is to keep the system as a whole running despite individual node failures, and to keep the impact of a node outage as small as possible.<ul><li>How do you  achieve high availability with leader-based replication?</li><li><strong>Follower failure: Catch-up recovery</strong><ul><li>From its log, it knows the latest transaction that was processed before the fault occured.</li></ul></li><li><strong>Leader failure: Failover</strong><ul><li><strong>Failover</strong>: one of the followers needs to be promoted to be the new leader, clients need to be configured to send their writes to the new leader, and the other followers need to start consuming data changes from new leader.</li><li><strong>Automatic failover</strong> process usually consists of the following steps:<ol><li>Determining that the leader has failed. If a node doesn’t respond for some period of time – say 30 seconds – it is assumed to be dead;</li><li>Choosing a new leader, through <u>election process</u> or <u>controller node</u> selection;</li><li>Reconfiguring the system to use the new leader. The system needs to ensure that the old leader becomes a follower and recognizes the new leader.</li></ol></li><li><strong>Failover is fraught with thing that can go wrong</strong><ul><li>If asynchronous replication is used, the new leader may not have received all the writes from the old leader before it failed.</li><li>Discarding writes is especially dangerous if other storage systems outside of the databases need to be coordinated with the database contents.</li><li>In certain fault scenarios, it could happen that two nodes both believe that thay are the leader. – aka <strong><u>split brain</u></strong> which will cause conflict data.</li><li>What is the right timeout before the leader is declared dead?</li></ul></li><li>There are no easy solutions to these problems. For this reason, some operations team prefer to perform <u>failover manually</u>.</li><li>These issues – <u>node failures; unreliable networks, and trade-offs</u> around replica <u>consistency, durability, availability and latency</u> – are in fact fundamental problems in distributed systems.</li></ul></li></ul></li><li><strong>Implementing of Replication Logs</strong><ul><li>How does leader-based replication work under the hood?</li><li><strong>Statement-based replcation(Not recommended)</strong>: (e.g. early version of MySQL)<ul><li>Simplest cases, the leader logs every write request (statement) that it executes and sends that statement log to its follower.</li><li>Potential <u>issues</u> with this approach <ul><li>Any statement that calls a nondeterministic function, such as NOW() or RAND() will get different values;</li><li>If statements use an auto-incrementing column, or if they defend on the existing data in the database;</li><li>Statements that have side effects (e.g. triggers, stored procedures, user-defined functions)</li></ul></li></ul></li><li><strong>Wirte-ahead log (WAL) shipping</strong>: (e.g. PostgreSQL and Oracle)<ul><li>the log is an append-only sequence on bytes containing all writes to the database. Besides writing the log to disk, the leader also sends it accross the network to its followers.</li><li>The main <u>disadvantage</u> is<ul><li>The log describes the data on a very low level: a WAL contains details of which bytes were change in which disk blocks. -&gt; <strong><u>closely coupled to the storage engine</u></strong>.</li></ul></li></ul></li><li><strong>Logical (row-based) log replication</strong><ul><li>An alternative is to use different logs formats for replication and for the storage engine, which allows the replcation to be <strong>decoupled from the storage engine internals</strong>.</li><li>since a <u><strong>logical log</strong> is decoupled from the storage engine</u> internals, it can more easily be kept backward compatible, allowing the leader and the follower to run different versions of the database software, or even different storage engines.</li><li>A logical log format is also easier for external applications to parse. Useful if you want to send the contents of a database to an external system (e.g. <strong>CDC</strong> change data capture)</li></ul></li><li><strong>Trigger-based replication</strong>: (e.g. Goldengate for Oracle)<ul><li>If you want to only replicate a subset of the data, or want to replicate from one kind of database to another, or if you need to conflict resolution logic then you may need to <u>move replication up to the application layer</u>.</li><li>An alternative is to use featrues that are available in many relational databases: <u>trigger and stored procedures</u>.<ul><li>A trigger lets you register custom application code that is automatically executed when a data change (write transaction) occurs in a database system.</li></ul></li><li><u>Trigger-based replication typically has greater overheads than other replcation methods</u>, and is more prone to bugs and limitations than the database’s built-in replication.<ul><li>But, provide greater flexibility.</li></ul></li></ul></li></ul></li></ul><h3 id="Problems-with-replication-Lag"><a href="#Problems-with-replication-Lag" class="headerlink" title="Problems with replication Lag"></a>Problems with replication Lag</h3><ul><li>Leader-based replication requires all writes to go through a single node, but read-only query can go though any replica.</li><li><strong>For read heavy application</strong>: create many followers, and distribute the read requests across those followers. You can increase the capacity for serving read-only requests simply by adding more followers.</li><li><strong>Eventual Consistent</strong>: This inconsistency is just a temporary state – if you stop writing to the database and wait a while, the followers will <strong>eventually</strong> catch up and become consistent with the leader.<ul><li>The term “<strong>eventually</strong>“ is deliberately vague: in general, there is no limit to how far a replica can fall behind.</li></ul></li><li><strong>Reading Your Own Writes</strong><ul><li>A user makes a write, followed by a read from stale replica. To prevent this anomaly, we need <strong><u>read-after-write consistency </u></strong></li><li><u>It makes no promises about the other users</u>: other users’ updates may not be visible until some later time. However, it reassures the user that their own input has been saved correctly.</li><li>How can we implement read-after-write consistency in a system with leader-based replication? e.g.<ul><li>When reading something that the user may have modified, read it from the leader; otherwise, read it from a follower.</li><li>You could track the time of the last update and, for one minute after the last update, make all reads from the leader.</li><li>The client can remember the timestamp of its most recent write</li><li>Any request that needs to be served by the leader must be routed to the datacenter that containes the leader.</li></ul></li><li><strong>cross-device read-after-write consistency</strong>： if the user enters some information on one device and then views it on another device, they should see the information they just entered.<ul><li>If your approach requires reading from the leader, you may first need to route request from all of a user’s devices to the same datacenter</li></ul></li></ul></li><li><strong>Monotonic Reads</strong><ul><li><u>when reading from asynchronous followers is that it’s possible for a user to see things moving backwards in time.</u> This can happen if a user makes several reads from different replicas.</li><li><strong>Monotonic reads</strong>: it’s a lesser guarantee than <u>strong consistency</u>, but a stronger guarantee than <u>eventual consistency</u>.<ul><li>One way of achieving monotonic reads is to <u>make sure that each user always <strong>makes their reads from the same replica</strong></u>.</li></ul></li></ul></li><li><strong>Consistent Prefix Reads</strong>:<ul><li><u>violation of casuality</u>. E.g. If some partition are replicated slower than others, an observer may see the answer before they see the question.</li><li><strong>consistent prefix reads</strong>: This guarantee says that <u>if a sequence of writes happens in a certain order, then anyone reading those writes will see them appear in the same order.</u><ul><li>This is a particular problem in partitioned (sharded) databases.</li></ul></li></ul></li><li><strong>Solutions for Replication Lag</strong><ul><li>Stronger guarantees;</li><li><strong>Transactions:</strong> they are a way for a databases to provide stronger guarantees so that the application can be simpler.</li><li><strong>Single-node transactions</strong> have exisited for a long time, but not for distributed systems.</li></ul></li></ul><h3 id="Muti-Leader-Replication"><a href="#Muti-Leader-Replication" class="headerlink" title="Muti-Leader Replication"></a>Muti-Leader Replication</h3><ul><li>Leader-based repliaction has one <u>major downside: there is only one leader, and all writes must go through it.</u> -&gt; single point failure.</li><li><strong>Multi-leader configuration</strong> (also known as <u>master-master or active/active replication</u>): A natural extension of the leader-based replication model is to allow more than one node to accepts writes.<ul><li>Replication the same: each node that processes a write must forward that data change to all the other nodes.</li></ul></li><li><strong>Use Cases for Multi-Leader Replication</strong><ul><li>It <u>rarely makes sense</u> to use a multi-leader setup <u>within a single datacenter</u>, because the <u>benefits rarely outweight the added complexity</u>.</li><li>Some databases support multi-leader cenfigurations by default, but it is also often implemented with external tools. (e.g. Tungsten Replicator for MySQL, BDR for PostgreSQL, and GoldenGate for Oracle) </li><li><strong>Multi-datacenter operation</strong><ul><li>In a multi-leader configuration, you can have <strong><u>a leader in each datacenter</u></strong>. Each datacenter’s leader replicates its changes to the leaders in other datacenters.</li></ul></li><li><strong>Advantages</strong>: compare with Single-Leader design<ul><li>Performance: reduce latency;</li><li>Tolerance of datacenter outages;</li><li>Tolerance of network problems;<ul><li>A multi-leader configuration with asynchronous replication can ususally tolerate network problems better: a temporary network interruption does not prevent writes being processed.</li></ul></li></ul></li><li><strong>Downsides</strong><ul><li>The same data may be concurrently modified in two different datacenter, and those <strong><u>write conflicts</u></strong> must be resolved.</li><li>As multi-leader replication is a somewhat retrofitted feature in many databses, there are often subtle configuration pitfalls and surprising interactions with other database features.</li></ul></li><li><strong>Clients with offline operation</strong>: E.g. calendar, <u>mobile devices</u> etc. (CouchDB)<ul><li>In this case, every devices has a local database that acts as a leader (it accepts write requests), and there is an asynchronous multi-leader replication process (sync) between the replicas of your calender on all of your devices.</li><li>Multi-leader replication is a tricky thing to get right.</li></ul></li><li><strong>Collaborative editing</strong><ul><li>Real-time collaborative editing applications allow several people to edit a document simultaneously. E.g. Etherpad, Google Docs.</li><li>For faster collaboration, you may want to make the unit of change very small (e.g., a single keystroke) and avoid locking</li></ul></li></ul></li><li><strong>Handling Write Conflicts</strong><ul><li>The <strong>biggest problem</strong> with multi-leader replication is that <u>write conflics</u> can occur, which means that conflict resolution is required.</li><li><strong>Synchronous versus asynchronous <u>conflict detection</strong></u><ul><li>the conflict is only detected <u>asynchronously</u> at some later point in time</li><li>if you want <u>synchronous</u> conflict detection, you might as well just use singe-leader-replication.</li></ul></li><li><strong>Conflic avoidance</strong><ul><li>The simplest strategy for dealing with conflicts is to <strong><u>avoid</u></strong> them if the application can ensure that all writes for a particular record <u>go though the same leader</u>, then conflicts cannot occur.</li></ul></li><li><strong>Converging toward a consistent state</strong>:<ul><li>The database must resolve the conflict in a <strong>convergent</strong> way, which means that <u>all replicas must arrive at the same final value when all chanegs have been replicated</u>.</li><li><strong>Ways of achieving convergent conflict resolution</strong><ul><li><u>Give <strong>each write a unique ID</strong></u> (e.g., a <u>timestamp</u>, a long random number, a <u>UUID</u>, or a <u>hash</u> of the key and value), pick the write with the highest ID as the winner, and throw away the other writes. (<strong>LWW– Last Write Wins</strong>)<ul><li>This approach is popular, it is dangerously prone to data loss.</li></ul></li><li>Give  <strong>each replica a unique ID</strong> (also potential data loss);</li><li>Somehow <strong>merge</strong> the values together;</li><li><strong>Record</strong> the conflict in an explicit data structure that preserves all information, and writes application code that <strong>resolves</strong> the conflict as some later time (e.g. prompt user)</li></ul></li><li><strong>Custom conflict resolution logic</strong><ul><li>Most multi-leader replication tools let you write conflict resolution logic using application code.<ul><li><u>On write</u>: As soon as the databases system detects a conflict in the log of replicated changes, it calls the conflict handler. (e.g. Bucardo)</li><li><u>On Read</u>: When a conflict is detected, all the conflicting writes are stored. The next time the data is read these multiple versions of the data are returned to the application. (e.g. CouchDB)</li></ul></li></ul></li></ul></li><li><strong>Automatic Conflict Resolution</strong><ul><li><strong>Conflict-free replicated data</strong> types (CRDTs, two-way merge) (e.g. Riak2.0)</li><li><strong>Mergable persistent data</strong> structures (similar to git, perform threes-way merge)</li><li><u>Operational transformation</u>: it was designed particularly for concurrent editing of an ordered list of iterms, such as the list of characters that consititute a text document. (Etherpad, Google Docs)</li></ul></li></ul></li><li><strong>Multi-Leader Replication Topologies</strong><ul><li>A replication topology describes the communication paths along which writes are propagetes from one node to another. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1597853443/part2-ch5-img1_t9ci3q.png" alt="p2-ch5-img1"></li><li>The <u>most general topology is all-to-all</u> in which every leader sends its writes to every other leader.</li><li>In <u>circular</u> and <u>star</u> topologies, a write may need to pass through several nodes before it reaches all replicas.<ul><li>Problem: if just one node fails, it can interrupt the flow of replication messages between other nodes, causing them to be unable to communicate until the node is fixed.</li></ul></li><li><u>Fault tolerance</u> of a more densely connected topology (such as all-to-all) is better because it allows message to travel along different paths, avoiding a single point failure.<ul><li>Problem:  Some network links may be faster than others (e.g., due to network congestion), with the result that some replication messages may “overtake” others. <img src="https://res.cloudinary.com/shuaiz/image/upload/v1597853882/part2-ch5-img2_pa6r2a.png" alt="part2-ch5-img2"></li><li>In the above procedure, Leader2 to will receive the value = 2 then receive the value = 1.</li></ul></li></ul></li></ul><h3 id="Leaderless-Replication"><a href="#Leaderless-Replication" class="headerlink" title="Leaderless Replication"></a>Leaderless Replication</h3><ul><li>Amazon used it for its in-house <strong>Dynamo system</strong>. Vi <u>Riak, Cassandra</u>, and <u>Voldmort</u> are open source datastore with leaderless replication models inspired by Dynamo, so this kind of database is also known as <u>Dynamo-style</u>.(C: DynamoDB, single-leader?)</li><li>In some leaderless implementations, the <u>client directly sends</u> its writes to several replicas, while in others, a <strong><u>coordinator</u></strong> node does this on behalf of the client.</li><li><strong>Writing to the Database When a Node is Down</strong><ul><li>In a leaderless configuration, <u>failover does not exist</u>.</li><li>A <strong><u>quorum write, quorum read and read repair</u></strong> after a node outage.</li><li>When a client reads from the database, it doesn’t just send its request to one replica: <strong>read requests are also sent to several nodes in parallel</strong>.</li><li><strong>Read repair and anti-entrophy</strong><ul><li>The replication scheme should ensure that eventually all the data is copied to every replica.</li><li><strong>Read repair</strong>: When a client makes a read from several nodes in parallel, it can detect any stale response. (when read is heavy)</li><li><strong>Anti-entropy process</strong>: In addition, some datastores have a background process that constantly looks for differences in the data between replicas and copies any missing data from one replica to another. (Maybe delay)</li></ul></li><li><strong>Quorums for reading and writing</strong><ul><li>As long as <strong><u>w + r &gt; n</u></strong>, we expects to get an up-to-date value when reading, because at least one of the r nodes we’re reading from must be up to date.</li><li><strong>Reads and wirtes that obey there <u>r</u> and <u>w</u> values are called quorum reads and writes</strong><ul><li>You can think of <strong>r</strong> and <strong>w</strong> as the <u>minimum number of votes</u> required for the read or write to be valid.</li></ul></li><li>In Dynamo-style databases. the parameters n, w, and r are typically configurable. A common choices is to make <strong>n</strong> an odd number (typically 3 or 5) and to set w = r = (n + 1) / 2 (rounded up).</li><li>The quorum condition, w + r &gt; n, allows the system to tolerate unavailable nodes as follows:<ul><li>If w &lt; n, we can still process writes if a node is unavailable.</li><li>if r &lt; n, we can still process reads if a node is unavailable.</li><li>With n = 3, w = 2, r = 2 we can tolerate one unavailable node.</li><li>With n = 5, w = 3, r = 3 we can tolerate two unavailable nodes.</li><li>The parameters w and r determine how many nodes we wait for </li></ul></li><li>If fewer than the required w or r nodes are available, writes or reads return an error.</li></ul></li></ul></li><li><strong>Limitations of Quorum Consistency</strong><ul><li>If you have <u>n</u> <strong>replicas</strong>, and you choose w and r such that w + r &gt; n, you can generally expect every read to return the most recent value written for a key.<ul><li>because the set of nodes of which you’ve written and the set of nodes from which you’ve read must overlap.</li></ul></li><li>Often, r and w are chosen to be a majority (more than n / 2) of nodes, because  that ensures w + r &gt; n while still tolerating up to n / 2 node failures.</li><li>With a smaller w and r<ul><li>more like to read stale values</li><li>But allows <u>lower latency and higher availability</u>: if there is a network interruption and many replicas become unreachable, there’s a higher chance that you can continue processing reads and writes.</li></ul></li><li>Even with w + r &gt; n, there are many edges cases that could cause problems<ul><li>Thus, although quorum appear to guarantee that a read returns the latest written value, in practice it is not so simple.</li></ul></li><li>Dynamo-style databases are generally optimized for use cases that can tolerate <strong>eventual consistency</strong><ul><li>Stronger guarantees generally require <u>transactions</u> or <u>consensus</u>.</li></ul></li><li><strong>Monitoring staleness</strong><ul><li>For leader-based replication, the database typically expose metrics for the replication lag, which you can feed into a monitoring system.</li><li>In system with <u>leaderless replication</u>, there is no fixed order in which writes are applied, which <u>makes monitoring more difficult</u>.</li><li>Eventual consistency is a deliberately vauge guarantee, but for operability it’s important to be able to quantify “eventual”.</li></ul></li></ul></li><li><strong>Sloppy Quorums and Hinted Handoff</strong><ul><li>Databases with appropriately configuered quorums can tolerate the failure of individual node without the need of failover.</li><li>Leaderless replication applealing for use cases that require <u>high availability and low latency</u>, and that can tolerate occasional stale reads.</li><li><strong>Sloppy quorum</strong>: writes and reads still require w and r successful response, but those may include nodes that are not among the designated n “home” nodes for a value. (e.g. Riak enabled default, Cassandra, Voldemort disabled)<ul><li>Particularly <u>useful for increasing write availability</u>: as long as any w nodes are available, the database can accept writes.</li><li>isn’t a quorum at all in the traditional sense. It’s only an assurance of durability.</li></ul></li><li><strong>Hinted handoff</strong>: Once the network interruption is fixed, any wirtes that one node temporarily accepted on behalf of another node are sent to the appropriate “home” nodes.</li><li><strong>Multi-datacenter operation</strong><ul><li><u><strong>Cassandra</strong> ane Voldemort</u> implement their multi-datacenter surpport within the normal leaderless model: the number of replicas n includes nodes in all datacenters, and in the configuration you can specify how many of the n replicas you want to have in each datacenter.</li><li><strong><u>Riak</u></strong> keeps all communication between clients and databases node local to one datacenter, so n describes the number of replicas within one datacenter.</li><li>Cross-datacenter replication between databases clusters happens <u>asynchronously</u> in the background, in a style that is similar to multi-leader replication.</li></ul></li><li><strong>Detecting Concurrent Writes</strong><ul><li><strong>Dynamo-style databases</strong> allow several clients to concurrently write to the same key, which means that <u>conflicts will occur even if strict quorums are used</u>.</li><li>In order to become eventually consistent, the replicas should converge toward the same values.</li><li><strong>Last writes wins (discarding concurrent writes)</strong><ul><li>Declare that each replica need only store the most “recent” value and allow “older” values to be overwritten and discarded.</li><li>writes are concurrent, so their order is undefind.</li><li>Even though the writes don’t have a natural ordering, we can force an arbitrary order on them.</li><li>LWW achieves the goal of eventual convergence, but at the cost of durability<ul><li>If losing data is not acceptable, LWW is a poor choice for confict resolution.</li></ul></li></ul></li><li><strong>The “happens-before” replationship and concurrency</strong><ul><li>An opration A happens before another operation B if B knows about A, or depends on A, or builds upon A in some way.</li><li><strong>Concurrency, Time and Relativity</strong><ul><li><u>For defining <strong>concurrency</strong>, exact time doesn’t matter</u>: <strong>we simply call two operations concurrent if they are both unaware of each other, regardless of the physical time at which they occured.</strong></li></ul></li></ul></li><li><strong>Capturing the happens-before relationship</strong><ul><li>Server can determine whether two operatoins are concurrent by looking at the <strong>version numbers</strong> – it does not need to interpret the values itself (so the value could be any data structure).</li><li>When a write includes the version number from prior read, that tells us which preivous state the writes is based on.</li></ul></li><li><strong>Merging concurrently written values</strong><ul><li>if several operations happen concurrently, the clients have to clean up afterward by merging the concurrently written values. (Aka siblings in Riak)<ul><li>Merging sibling values is essentially the same problem as conflict resolution in multi-leader replication.</li></ul></li><li>a reasonable approach to merging siblings is to just take the union.</li></ul></li><li><strong>Version vectors</strong> (C: allow data to diverge and converge, like git)<ul><li><u>We need to use a version number per replica as well as per key</u>.</li><li>Each replica increments its own version number when processing a write, and also keeps track of the version numbers it has seen from each of the other replicas.</li><li><strong>Version vector</strong>: <u>the collection of version numbers from all the replicas</u><ul><li>most interesting is probably the dotted version vector, which is used in Raik 2.0</li></ul></li><li>The version vector allows the databases to distinguish between overwrites and concurrent writes.</li><li>The version vector structure ensures that it is safe to read from one replica and subsequently write back to another replica.</li><li><strong>Version vectors and vector clocks</strong><ul><li>A version vector is sometimes also called a vector clock, even though they are not quite the same.</li><li>when comparing the state of replicas, version vectors are the right data structure to use.</li></ul></li></ul></li></ul></li></ul></li></ul><h3 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h3><ul><li><strong>Replication</strong> can serve several purposes <ul><li><strong>High availability</strong>: Keeping the system running, even when one machine (or several machines, or an entire datacenter) goes down</li><li><strong>Disconnected operation</strong>: Allowing an application to continue working when there is a network interruption.</li><li><strong>Latency</strong>: Placing data geographically close to users, so that users can onteract with it faster.</li><li><strong>Scalability</strong>: being able to higer volume of reads that a single machine could handle, by performing reads on replicas.</li></ul></li><li>Despite being a simple goal, keeping a copy of the same data on several machines’ replication turns out to be a remarkably tricky problem.</li><li>Three main approaches to replication<ul><li><strong>Single-leader</strong> replication <ul><li>Clients send all writes to a single node (the leader), which sends a stream of data change events to the other replicas(followers). Reads can be perform on any replica, but reads from follwers might be stale.</li></ul></li><li><strong>Multi-leader</strong> replication<ul><li>Clients send each write to several leader nodes, ant of which can accept writes. The leaders send stream of data change events to each other and to any follower nodes.</li></ul></li><li><strong>Leaderless</strong> replication <ul><li>Clients send each writes to several node, and read from several nodes in parallel on order to detect and correct node with stale data.</li></ul></li></ul></li><li>Each appoach has advantages and disadvantages<ul><li>Single-leader<ul><li>is popular because it is fairly easy to understand and there is no conflict resolution to worry about </li></ul></li><li>Multi-leader &amp; Leaderless<ul><li>Can be more robust in there presence of faulty nodes, network interruptions and latency spikes – at the cost of being harder to reason about and providing only <u>very weak consistency</u> guarantees.</li></ul></li></ul></li><li>Replication can be <strong><u>synchronous</u></strong> or <strong><u>asynchronous</u></strong>, which has a profound effect on the system behavior when there is a fault.<ul><li>It’s important to figure out what happens when replication lag increases and servers fail.</li></ul></li><li><u>Consistency models</u> which are helpful for deciding how an application should behave under replication lag<ul><li>Read-after-wirte consistency</li><li>Monotonic reads</li><li>Consistent prefix reads</li></ul></li><li><strong>Concurrency</strong> issues that are inherent in multi-leader and leaderless replication approaches: because they allow multiple writes to happen concurrently, conflicts may occur.</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter5 Replication&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Replication&lt;/strong&gt; means keeping a &lt;u&gt;copy of the same data on multiple machines&lt;/u&gt; that are connected via a network.&lt;ul&gt;
&lt;li&gt;To keep data geographically close to your users (&lt;strong&gt;reduce latency&lt;/strong&gt;)&lt;/li&gt;
&lt;li&gt;To allow the system to continue working even if some if its parts have failed (&lt;strong&gt;increase availability&lt;/strong&gt;)&lt;/li&gt;
&lt;li&gt;To scale out the number of machines that can serve read queries (&lt;strong&gt;increase read throughput&lt;/strong&gt;)&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;All of the &lt;u&gt;difficulty&lt;/u&gt; in replication lies in &lt;u&gt;handling changes to replicated&lt;/u&gt; data&lt;/strong&gt;;&lt;/li&gt;
&lt;li&gt;Three popular algorithms for replicating changes between nodes: &lt;strong&gt;single-leader&lt;/strong&gt;, &lt;strong&gt;multi-leader&lt;/strong&gt;, and &lt;strong&gt;leaderless&lt;/strong&gt; replication.&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing Data Intensive Application学习笔记6</title>
    <link href="http://yoursite.com/2020/08/17/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B06/"/>
    <id>http://yoursite.com/2020/08/17/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B06/</id>
    <published>2020-08-18T04:07:52.000Z</published>
    <updated>2020-08-18T04:09:00.304Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Part2 Intro - Distributed Data</strong></p><p>在part1里，讨论的是单一机器上的情况，在part2讨论了如果多台机器参与数据的存储和检索，会发生什么。</p><a id="more"></a><p>出于多种原因，会将数据库分布在多台机器：</p><ul><li>可扩展性： 当数据量、读取/写入负载超出单机处理能力，可以将负载分散到多台机器。</li><li>高可用性/容错：如果应用需要在单机出现故障的情况下继续工作，则可以使用多台机器，提供一些redundancy</li><li>延迟：如果用户遍布全球，在世界各地部署服务器以使用户从最近的数据中心获取数据，避免远距离传输</li></ul><h3 id="更高的负荷"><a href="#更高的负荷" class="headerlink" title="更高的负荷"></a>更高的负荷</h3><p><strong>共享内存架构</strong>：最简单的方法就是购买更强大的及其，称为垂直扩展（vertical scaling）或向上扩展（scale up），一个超级电脑拥有许多处理器许多内存和硬盘在一个OS下连接，这种<strong>共享内存架构（shared-memory architecture）</strong>中所有组件都可以看作一个单独机器。<br>问题：<strong>成本增长速度快于线性增长</strong></p><p><strong>共享磁盘架构</strong>：<strong>共享磁盘架构（shared-disk architecture）</strong>使用多台具有独立处理器和内存的机器，但是将数据存储在机器之间共享的磁盘阵列上，这些磁盘通过快速网络连接。</p><p><strong>无共享架构</strong>：<strong>无共享架构（shared-nothingarchitecture）</strong>也叫做<strong>水平扩展（horizontal scale）或向外扩展（scale out）</strong>， 在这种架构中，运行数据库软件的每个机器/虚拟机都称为<strong><u>节点</u></strong>， 每个节点使用各自的处理器、内存和硬盘，节点之间的协调是通过软件层面使用传统网络实现。</p><h3 id="复制vs-分区"><a href="#复制vs-分区" class="headerlink" title="复制vs.分区"></a>复制vs.分区</h3><p>数据分布在多个节点有两种常见方式：<br><strong><u>复制（replication）</u></strong></p><ul><li>在几个不同的节点上保存数据的相同副本（可能放在不同的位置）</li><li>复制提供了<strong><u>冗余（redundant）</u></strong>：如果一些节点不可用，剩余节点仍可以提供数据服务；复制还可以帮助改善性能，<a href="">第五章</a>会讲到</li></ul><p><strong><u>分区（partitioning）</u></strong><br>将一个大型数据库拆分成较小的子集称为分区，从而不同分区可以指派给不同的节点（<strong><u>node</u></strong>，也可称为<strong><u>分片[shard]</u></strong>)，<a href="">第六章</a>会讨论到。</p><p><a href="">第七章</a>会讨论<strong><u>事务（transaction）</u></strong>， 可以了解到数据系统中可能出现的问题以及我们可以做什么。</p><p><a href="">第八章</a>和<a href="">第九章</a>将讨论分布式系统的根本局限性。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Part2 Intro - Distributed Data&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在part1里，讨论的是单一机器上的情况，在part2讨论了如果多台机器参与数据的存储和检索，会发生什么。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>A Brief Introduction of Cassandra</title>
    <link href="http://yoursite.com/2020/08/16/A-Brief-Introduction-of-Cassandra/"/>
    <id>http://yoursite.com/2020/08/16/A-Brief-Introduction-of-Cassandra/</id>
    <published>2020-08-16T23:38:09.000Z</published>
    <updated>2020-08-17T02:48:05.602Z</updated>
    
    <content type="html"><![CDATA[<p>Apache Cassandra 是一个开源的、分布式、去中心化、弹性可扩展、高可用性、容错、一致性可调、面向行的数据库，它基于 Amazon Dynamo 的分布式设计和 Google Bigtable 的数据模型。它最初由 Facebook 创建，用于储存收件箱等简单格式数据，于2008年开源。此后，由于 Cassandra 良好的可扩展性，被 Digg、Twitter 等知名 Web 2.0 网站所采纳，成为了一种流行的分布式结构化数据存储方案。截止目前，Apache Cassandra 最新版本为 3.7。</p><p>Apache Cassandra 官方网站为 <a href="https://link.jianshu.com?t=http://cassandra.apache.org/" target="_blank" rel="noopener">http://cassandra.apache.org/</a>。</p><a id="more"></a><h3 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h3><img src="https://res.cloudinary.com/shuaiz/image/upload/v1597620965/Cassandra_qsjmh1.png" alt="Cassandra" style="zoom:33%;" /><p>Cassandra 是 Facebook 于2008年7月在 Google Code 上开源的项目，最早是由 Amazon 前雇员和一位 Microsoft 的工程师写成的。这个系统受到 Amazon Dynamo 的巨大影响（有关 Amazon Dynamo 的介绍，可以查阅论文《Dynamo: Amazon’s Highly Available Key-value Store》，网址为<a href="https://link.jianshu.com?t=http://www.allthingsdistributed.com/files/amazon-dynamo-sosp2007.pdf" target="_blank" rel="noopener">http://www.allthingsdistributed.com/files/amazon-dynamo-sosp2007.pdf</a>）。Cassandra 实现了 Dynamo 风格的副本复制模型和没有单点失效的架构，增加了更加强大的 column family 数据模型。</p><p>之后，在2009年1月，Cassandra 被接纳为 Apache 基金会的孵化器项目，并于2010年3月毕业后成为了顶级项目。此时，很多知名公司包括  Facebook、Rackspace、Digg、Twitter 等都成为了 Cassandra 的用户。</p><p>有关 Cassandra 论文，最早可见由 Facebook 的 Lakshman 和 Malik 发表的《A Decentralized Structured Storage System》（在线阅读地址为 <a href="https://link.jianshu.com?t=http://www.cs.cornell.edu/projects/ladis2009/papers/lakshman-ladis2009.pdf" target="_blank" rel="noopener">http://www.cs.cornell.edu/projects/ladis2009/papers/lakshman-ladis2009.pdf</a>）。在 Cassandra 2.0 发布以后，Jonathan Ellis 对该论文相应的做了修改，发表于 <a href="https://link.jianshu.com?t=http://docs.datastax.com/en/articles/cassandra/cassandrathenandnow.html" target="_blank" rel="noopener">http://docs.datastax.com/en/articles/cassandra/cassandrathenandnow.html</a>。</p><p>伴随着业界对于 Cassandra 商业化、产品化的需求，在2010年4月 Apache Cassandra 项目主席 Jonathan Ellis 以及其同事 Matt Pfeil 成立了一家服务公司，称为 <a href="https://link.jianshu.com?t=http://www.datastax.com/" target="_blank" rel="noopener">DataStax</a>（原先的名字为 Riptano）。DataStax 雇佣了多名 Cassandra 贡献者，成为了领导和支持 Cassandra 项目的主力。</p><p>2015年11月，Cassandra 发布了 3.0 版本，其底层的存储引擎已被重写，支持全局索引，支持 Java 8，并将基于 Thrift 命令行界面给移除了。</p><p>Apache Cassandra 具有如下特性：</p><h4 id="1-分布式与去中心化"><a href="#1-分布式与去中心化" class="headerlink" title="1. 分布式与去中心化"></a>1. 分布式与去中心化</h4><p>Cassandra 是分布式的，这意味着它可以运行在多台机器上，并呈现给用户一个一致的整体。</p><p>对于很多存储系统（如 MySQL、Bigtable、HBase 等），一旦你开始扩展它，就需要把某些节点设为主节点，其他则作为从节点。但 Cassandra 是去中心的，也就是说每个节点都是一样的，没有节点会承担特殊的管理任务。与主从模式（master-slave）相反，Cassandra 的协议是 P2P 的，并使用 gossip 来维护存活或死亡节点的列表。</p><p>去中心化这一事实意味着 Cassandra 不会存在单点失效。Cassandra 集群中的所有节点的功能都完全一样， 所以不存在一个特殊的主机作为主节点来承担协调任务。有时这被叫做“server symmetry（服务器对称）”。</p><p>综上所述，因为 Cassandra 是分布式、去中心化的，它不会有单点失效的问题，所以支持高可用性。</p><h4 id="2-弹性可扩展"><a href="#2-弹性可扩展" class="headerlink" title="2. 弹性可扩展"></a>2. 弹性可扩展</h4><p>可扩展性是指系统架构可以让系统提供更多的服务而不降低使用性能的特性。最简单的达到可扩展性的手段，就是给现有的机器增加硬件的容量、内存进行垂直扩展。而水平扩展则需要增加更多机器，每台机器提供全部或部分数据，这样所有主机都不必负担全部业务请求。但软件自己需要有内部机制来保证集群中节点间的数据同步。</p><p>弹性可扩展是指水平扩展的特性，意即你的集群可以不间断的情况下，方便扩展或缩减服务的规模。这样，你就不需要重新启动进程，不必修改应用的查询，也无需自己手工重新均衡数据分布。在 Cassandra 里，你只要加入新的计算机，Cassandra 就会自动地发现它并让它开始工作。</p><h4 id="3-高可用与容错"><a href="#3-高可用与容错" class="headerlink" title="3. 高可用与容错"></a>3. 高可用与容错</h4><p>系统的可用性是由满足请求的能力来量度的。但计算机可能会有各种各样的故障，从硬件器件故障到网络中断都有可能。所以它们一般都有硬件冗余，并在发生故障事件的情况下会自动响应并进行热切换。从软件的层次来说，设置多个数据中心，就是“软件冗余”，它能保障在灾难发生时，故障中断的功能在剩余系统上能够进行恢复。</p><p>Cassandra 就是高可用的。你可以在不中断系统的情况下替换故障节点，还可以把数据分布到多个数据中心里，从而提供更好的本地访问性能，并且在某一数据中心发生火灾、洪水等不可抗灾难的时候防止系统彻底瘫痪。</p><h4 id="4-Tuneable-Consistency（可调一致性）"><a href="#4-Tuneable-Consistency（可调一致性）" class="headerlink" title="4. Tuneable Consistency（可调一致性）"></a>4. Tuneable Consistency（可调一致性）</h4><p>一致性的基本含义是读操作一定会返回最新写入的结果。扩展数据存储系统就意味着我们不得不在数据一致性、节点可用性和分区容错性之间做某些折衷（即 CAP 理论）。Cassandra 常被称作是“最终一致性”的，简单地说，Cassandra 牺牲了一点一致性来换取了完全的可用性。但是 Cassandra 实际更应该表述为“可调一致性”，它允许你来方便地选定需要的一致性水平与可用性水平，在二者间找到平衡点。</p><p>因为客户端可以控制在更新到达多少个副本之前，必须阻塞系统。这是通过设置 replication factor（副本因子）来调节与之相对的一致性级别。<br> 通过 replication factor，你可以决定准备牺牲多少性能来换取一致性。 replication factor 是你要求更新在集群中传播到的节点数（注意，更新包括所有增加、删除和更新操作）。</p><p>客户端每次操作还必须设置一个 consistency level（一致性级别）参数，这个参数决定了多少个副本写入成功才可以认定写操作是成功的，或者读取过程中读到多少个副本正确就可以认定是读成功的。这里，Cassandra 把决定一致性程度的权利留给了客户自己。</p><p>所以，如果需要的话，你可以设定 consistency level 和 replication factor 相等，从而达到一个较高的一致性水平，不过这样就必须付出同步阻塞操作的代价，只有所有节点都被更新完成才能成功返回一次更新。而实际上，Cassandra 一般都不会这么来用，原因显而易见（这样就丧失了可用性目标，影响性能，而且这不是你选择 Cassandra 的初衷）。而如果一个客户端设置 consistency level 低于  replication factor 的话，即使有节点宕机了，仍然可以写成功。</p><h4 id="5-Row-Oriented（面向行）"><a href="#5-Row-Oriented（面向行）" class="headerlink" title="5. Row-Oriented（面向行）"></a>5. Row-Oriented（面向行）</h4><p>Cassandra 不是真正意义上的“Column-Oriented（面向列）”的数据库而是“Row-Oriented（面向行）”， 它的数据结构不是关系型的，而是一个多维稀疏哈希表。“稀疏”意味着任何一行都可能会有一列或者几列，但每行都不一定（像关系模型那样）和其他行有一样的列。每行都有一个唯一的键值，用于进行数据访问。所以，更确切地说，应该把 Cassandra 看做是一个有索引的、Row-Oriented 的存储系统。</p><p>Cassandra 的数据存储结构基本可以看做是一个多维哈希表。这意味着你不必事先精确地决定你的具体数据结构或是你的记录应该包含哪些具体字段。这特别适合处于草创阶段，还在不断增加或修改服务特性的应用。而且也特别适合应用在敏捷开发项目中，不必进行长达数月的预先分析。对于使用 Cassandra 的应用，如果业务发生变化了，只需要在运行中增加或删除某些字段就行了，不会造成服务中断。</p><p>当然， 这不是说你不需要考虑数据。相反，Cassandra 需要你换个角度看数据。在 RDBMS 里， 你得首先设计一个完整的数据模型， 然后考虑查询方式， 而在 Cassandra 里，你可以首先思考如何查询数据，然后提供这些数据就可以了。</p><h4 id="6-Flexible-Schema（灵活的模式）"><a href="#6-Flexible-Schema（灵活的模式）" class="headerlink" title="6. Flexible Schema（灵活的模式）"></a>6. Flexible Schema（灵活的模式）</h4><p>早期版本的 Cassandra 是忠实于 Bigtable 的设计，而采用的是“schema-free（无模式）”的数据模型，新的 column 可以被动态定义。schema-free 模式的数据库在处理大数据时有非常强的扩展和高性能的优势。但这种模式的主要缺点是在确定数据的含义和数据的格式时存在难点，而这限制了执行复杂查询的能力。</p><p>Cassandra Query Language（CQL）正是为了解决上面提到的问题。CQL 它提供了一种通过类似于 Structured Query Language（SQL）的语法来定义的模式。起初，CQL 是基于  Apache Thrift 项目的 schema-free 接口来提供额外的 Cassandra 接口的。在这个过渡阶段，模式是可选的，可以通过 CQL 来定义，也可以通过 Thrift API 来实现在添加新的 column 时动态扩展。</p><p>自 Cassandra 3.0 以来，将不推荐采用通过基于 Thrift API 来实现动态创建 column 的方式。Cassandra 的底层存储已重新实现，并与 CQL 更紧密地结合起来。Cassandra 并不限制动态扩展模式，但它的工作方式已经显著的不同了。CQL 的集合，如 list、set，特别是 map 提供在无结构化的格式里面添加内容的能力，从而能扩展现有的模式。CQL 还提供了改变列的类型的能力，以支持 JSON 格式的文本的存储。</p><p>因此， Cassandra 是支持“灵活的模式”。</p><h4 id="7-高性能"><a href="#7-高性能" class="headerlink" title="7. 高性能"></a>7. 高性能</h4><p>Cassandra 在设计之初就特别考虑了要充分利用多处理器和多核计算机的性能，并考虑在分布于多个数据中心的大量这类服务器上运行。它可以一致而且无缝地扩展到数百台机器。Cassandra 已经显示出了高负载下的良好表现，在一个非常普通的工作站上，Cassandra 也可以提供非常高的写吞吐量。而如果你增加更多的服务器，你还可以继续保持 Cassandra 所有的特性而无需牺牲性能。</p><h3 id="Cassandra-应用场景"><a href="#Cassandra-应用场景" class="headerlink" title="Cassandra 应用场景"></a>Cassandra 应用场景</h3><p>尽管 Cassandra 设计精巧、功能出色，但也并非能胜任所有工作。所以，这里我们来介绍一下 Cassandra 最擅长的领域。</p><h4 id="1-大规模部署"><a href="#1-大规模部署" class="headerlink" title="1. 大规模部署"></a>1. 大规模部署</h4><p>Cassandra 的很多精巧设计都专注于高可用、可调一致性、P2P 协议、无缝扩展等，这些都是 Cassandra 的卖点。这些特性在单节点工作时都是没有意义的，更无法展现它的全部能力。所以，你需要做一些评估，考虑你期望的流量、吞吐需求以及 SLA 等。如果你认为有几种关系型数据库可以很好地应付你的流量，提供不错的性能，那可能选关系型数据库更好。简单地说，这是因为 RDBMS 更易于在单机上运行，对你来说也更熟悉。</p><p>但是，如果你认为需要至少几个节点才能支撑你的业务，那 Cassandra 就是个不错的选择。如果你的应用可能需要数十个节点，那 Cassandra 可能就是个很棒的选择了。</p><h4 id="2-写密集、统计和分析型工作"><a href="#2-写密集、统计和分析型工作" class="headerlink" title="2. 写密集、统计和分析型工作"></a>2. 写密集、统计和分析型工作</h4><p>Cassandra 是为优异的写吞吐量而特别优化的。许多早期使用 Cassandra 的产品都用于存储用户状态更新、社交网络、建议/评价以及应用统计等。这些应用大都是写多于读的，并且更新可能随时发生并伴有突发的峰值。事实上，支撑应用负载需要很高的多客户线程并发写性能，这正是 Cassandra 的主要特性。</p><p>Cassandra 已经被用于开发了多种不同的应用，包括窗口化的时间序列数据库，用于文档搜索的反向索引，以及分布式任务优先级队列。</p><h4 id="3-地区分布"><a href="#3-地区分布" class="headerlink" title="3. 地区分布"></a>3. 地区分布</h4><p>Cassandra 直接支持多地分布，可以很容易配置成将数据分布到多个数据中心的存储方式。如果你有一个全球部署的应用，那么让数据贴近用户会获得不错的性能收益，Cassandra 正适合这种应用场合。</p><h4 id="4-变化的应用"><a href="#4-变化的应用" class="headerlink" title="4. 变化的应用"></a>4. 变化的应用</h4><p>如果你正在“初创阶段”，业务会不断改进，Cassandra 这种灵活的模式的数据模型可能更适合你。这让你的数据库能更快地跟上业务改进的步伐。</p><h3 id="Cassandra-架构、数据模型"><a href="#Cassandra-架构、数据模型" class="headerlink" title="Cassandra 架构、数据模型"></a>Cassandra 架构、数据模型</h3><p>Cassandra 是设计来处理跨多个节点的大数据工作负载，且不会产生单点故障问题。它的架构是基于对系统和硬件故障也时有发生的理解，即计算机的故障不可避免。Cassandra 通过在集群中的所有节点之间分布均匀的节点，采用 P2P 的方式来定位分布式系统涉及的故障问题。每个节点经常交流有关其自身的状态信息，其他节点利用 P2 gossip 通信协议与集群进行交互。Commit 日志在每个节点上会依次写入活动信息，以确保数据的持久性，然后数据被索引并写入内存结构 memtable 中。memtable 类似于一个回写高速缓存。每次这个内存结构满时，数据就被写入磁盘中的 SSTable 数据文件中。所有的写操作都会自动分区，并在整个集群中进行复制。Cassandra 定期合并使用一种称为 compaction 过程来合并 SSTable，合并过程中会丢弃标记为 tombstone 的数据。为了确保所有的数据在整个集群保持一致，不同的修复机制将被使用。</p><p>Cassandra 是一个分区 row 存储数据库，其中 row 被组织成一个必需有主键的 table。 Cassandra 的架构允许任何已授权的用户可以使用 CQL 语言来连接到任何数据中心的任何节点。为了方便使用，CQL 使用了类似的 SQL 的语法来操作表数据。开发人员可以通过 <a href="https://link.jianshu.com?t=http://docs.datastax.com/en/cql/3.3/cql/cql_reference/cqlshCommandsTOC.html" target="_blank" rel="noopener">cqlsh</a>、<a href="https://link.jianshu.com?t=http://docs.datastax.com/en/developer/devcenter/doc" target="_blank" rel="noopener">DevCenter</a> 或者应用语言的<a href="https://link.jianshu.com?t=http://docs.datastax.com/en/developer/driver-matrix/doc/common/driverMatrix.html#driver-cmpt-matrix" target="_blank" rel="noopener">驱动程序</a>来访问 CQL。通常情况下，集群都每个应用程序都会有一个 keyspace （密钥空间），由许多不同的 table 组成。</p><p>客户端读取或写入请求可以被发送到集群中的任何节点。当客户端通过一个请求连接到一个节点时，则该节点充当协调器用于该客户端特定的操作。协调器充当客户端应用程序和拥有被请求数据的节点之间的代理。协调器来决定在哪些节点能收到请求，这往往是基于集群的配置要求。</p><p>Cassandra 包含如下核心构件：</p><ul><li><strong>Node（节点）</strong>：存储数据的地方。它是 Cassandra 的基础设施组件。</li><li><strong>Data center（数据中心）</strong>：相关节点的集合。数据中心可以是物理数据中心或虚拟数据中心。不同的工作负载应使用单独的数据中心，无论是物理的还是虚拟。复制是由数据中心的设置。使用单独的数据中心可以防止由其他工作负荷受到影响卡桑德拉交易，并保持彼此接近为较低的延迟请求。根据不同的复制因子，可将数据写入到多个数据中心。数据中心必须永远跨越物理位置。</li><li><strong>Cluster（集群）</strong>：一个集群包含一个或多个数据中心。它可以跨越物理位置。</li><li><strong>Commit log（Commit 日志）</strong>：<br>   所有数据首先被写入 Commit 日志以确保数据的持久性。当其所有的数据已冲刷到 SSTable 时，它可以存档、删除或回收。</li><li><strong>SSTable</strong>：<br>   SSTable（sorted string table，经排序的字符串表）是 Cassandra 定期写 memtable 的不变的数据文件。SSTable 仅追加并在磁盘上依次存储并保持 Cassandra table。</li><li><strong>CQL Table</strong>：<br>   通过  table row 获取到的有序的 column 集合。table 由 column 和一个主键组成。</li></ul><h3 id="致谢"><a href="#致谢" class="headerlink" title="致谢"></a>致谢</h3><p>本文转载自简书作者@梁睿坤的文章<a href="https://www.jianshu.com/p/f1485d5151ba" target="_blank" rel="noopener">认识Cassandra</a>的前半部分。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Apache Cassandra 是一个开源的、分布式、去中心化、弹性可扩展、高可用性、容错、一致性可调、面向行的数据库，它基于 Amazon Dynamo 的分布式设计和 Google Bigtable 的数据模型。它最初由 Facebook 创建，用于储存收件箱等简单格式数据，于2008年开源。此后，由于 Cassandra 良好的可扩展性，被 Digg、Twitter 等知名 Web 2.0 网站所采纳，成为了一种流行的分布式结构化数据存储方案。截止目前，Apache Cassandra 最新版本为 3.7。&lt;/p&gt;
&lt;p&gt;Apache Cassandra 官方网站为 &lt;a href=&quot;https://link.jianshu.com?t=http://cassandra.apache.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;http://cassandra.apache.org/&lt;/a&gt;。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
      <category term="Database" scheme="http://yoursite.com/tags/Database/"/>
    
  </entry>
  
  <entry>
    <title>Introduction to Bloom Filter</title>
    <link href="http://yoursite.com/2020/08/16/Introduction-to-Bloom-Filter/"/>
    <id>http://yoursite.com/2020/08/16/Introduction-to-Bloom-Filter/</id>
    <published>2020-08-16T23:22:31.000Z</published>
    <updated>2020-08-16T23:23:22.335Z</updated>
    
    <content type="html"><![CDATA[<h3 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h3><p>如果想判断一个元素是不是在一个集合里，一般想到的是将集合中所有元素保存起来，然后通过比较确定。<a href="https://zh.wikipedia.org/wiki/链表" target="_blank" rel="noopener">链表</a>、<a href="https://zh.wikipedia.org/wiki/树_(数据结构)" target="_blank" rel="noopener">树</a>、<a href="https://zh.wikipedia.org/wiki/散列表" target="_blank" rel="noopener">散列表</a>（又叫哈希表，Hash table）等等数据结构都是这种思路。但是随着集合中元素的增加，我们需要的存储空间越来越大。同时检索速度也越来越慢，上述三种结构的检索时间复杂度分别为<img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/db50673c67c6e72b7342ca1383def18cdead21b6" alt="{\displaystyle O(n),O(\log n),O(1)}">。</p><p>布隆过滤器的原理是，当一个元素被加入集合时，通过K个<a href="https://zh.wikipedia.org/wiki/散列函数" target="_blank" rel="noopener">散列函数</a>将这个元素映射成一个位<a href="https://zh.wikipedia.org/wiki/数组" target="_blank" rel="noopener">数组</a>中的K个点，把它们置为1。检索时，我们只要看看这些点是不是都是1就（<u><strong>大约</strong></u>）知道集合中有没有它了：如果这些点有任何一个0，则被检元素一定不在；如果都是1，则被检元素很可能在。这就是布隆过滤器的基本思想。</p><a id="more"></a><h3 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h3><p>相比于其它的数据结构，布隆过滤器在空间和时间方面都有巨大的优势。布隆过滤器存储空间和插入/查询时间都是常数<img src="https://wikimedia.org/api/rest_v1/media/math/render/svg/f5ec39041121b14e8c2b1a986c9b04547b223e3c" alt="O(k)">。另外，散列函数相互之间没有关系，方便由硬件并行实现。布隆过滤器不需要存储元素本身，在某些对保密要求非常严格的场合有优势。</p><p>布隆过滤器可以表示全集，其它任何数据结构都不能；</p><h3 id="缺点"><a href="#缺点" class="headerlink" title="缺点"></a>缺点</h3><p>但是布隆过滤器的缺点和优点一样明显。误算率是其中之一。随着存入的元素数量增加，误算率随之增加。但是如果元素数量太少，则使用散列表足矣。</p><p>另外，一般情况下不能从布隆过滤器中删除元素。我们很容易想到把位数组变成整数数组，每插入一个元素相应的计数器加1, 这样删除元素时将计数器减掉就可以了。然而要保证安全地删除元素并非如此简单。首先我们必须保证删除的元素的确在布隆过滤器里面。这一点单凭这个过滤器是无法保证的。另外计数器回绕也会造成问题。</p><p>在降低误算率方面，有不少工作，使得出现了很多布隆过滤器的变种。</p><h3 id="Java代码实现"><a href="#Java代码实现" class="headerlink" title="Java代码实现"></a>Java代码实现</h3><p>1 m位的BitSet， 初始化为0； 2 k个不同的hash function；3 计算当前String的k个hash function的值； 4 对于BitSet置为1； 5 可能有误差；</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> Others;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.BitSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BloomFilter</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_SIZE = <span class="number">1</span> &lt;&lt; <span class="number">25</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> BitSet bits = <span class="keyword">new</span> BitSet(DEFAULT_SIZE);</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span>[] seeds = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">7</span>, <span class="number">11</span>, <span class="number">13</span>, <span class="number">37</span>, <span class="number">61</span>&#125;;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> HashFunction [] functions = <span class="keyword">new</span> HashFunction[seeds.length];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">HashFunction</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">int</span> capacity;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">int</span> seed;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="title">HashFunction</span><span class="params">(<span class="keyword">int</span> capacity, <span class="keyword">int</span> seed)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.capacity = capacity;</span><br><span class="line">            <span class="keyword">this</span>.seed = seed;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hash</span><span class="params">(String value)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">int</span> res = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; value.length(); i++) &#123;</span><br><span class="line">                res = seed * res + value.charAt(i);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> (capacity - <span class="number">1</span>) &amp; res;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">BloomFilter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; seeds.length; i++) &#123;</span><br><span class="line">                functions[i] = <span class="keyword">new</span> HashFunction(DEFAULT_SIZE, seeds[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(String value)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (HashFunction function : functions) &#123;</span><br><span class="line">                bits.set(function.hash(value), <span class="keyword">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">contains</span><span class="params">(String value)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (value == <span class="keyword">null</span>) <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">boolean</span> res = <span class="keyword">true</span>;</span><br><span class="line">            <span class="keyword">for</span> (HashFunction function : functions) &#123;</span><br><span class="line">                res = res &amp; bits.get(function.hash(value));</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> res;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="结束"><a href="#结束" class="headerlink" title="结束"></a>结束</h3><p>BitSet不是太懂，需要进一步学习。</p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;基本概念&quot;&gt;&lt;a href=&quot;#基本概念&quot; class=&quot;headerlink&quot; title=&quot;基本概念&quot;&gt;&lt;/a&gt;基本概念&lt;/h3&gt;&lt;p&gt;如果想判断一个元素是不是在一个集合里，一般想到的是将集合中所有元素保存起来，然后通过比较确定。&lt;a href=&quot;https://zh.wikipedia.org/wiki/链表&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;链表&lt;/a&gt;、&lt;a href=&quot;https://zh.wikipedia.org/wiki/树_(数据结构)&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;树&lt;/a&gt;、&lt;a href=&quot;https://zh.wikipedia.org/wiki/散列表&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;散列表&lt;/a&gt;（又叫哈希表，Hash table）等等数据结构都是这种思路。但是随着集合中元素的增加，我们需要的存储空间越来越大。同时检索速度也越来越慢，上述三种结构的检索时间复杂度分别为&lt;img src=&quot;https://wikimedia.org/api/rest_v1/media/math/render/svg/db50673c67c6e72b7342ca1383def18cdead21b6&quot; alt=&quot;{\displaystyle O(n),O(\log n),O(1)}&quot;&gt;。&lt;/p&gt;
&lt;p&gt;布隆过滤器的原理是，当一个元素被加入集合时，通过K个&lt;a href=&quot;https://zh.wikipedia.org/wiki/散列函数&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;散列函数&lt;/a&gt;将这个元素映射成一个位&lt;a href=&quot;https://zh.wikipedia.org/wiki/数组&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;数组&lt;/a&gt;中的K个点，把它们置为1。检索时，我们只要看看这些点是不是都是1就（&lt;u&gt;&lt;strong&gt;大约&lt;/strong&gt;&lt;/u&gt;）知道集合中有没有它了：如果这些点有任何一个0，则被检元素一定不在；如果都是1，则被检元素很可能在。这就是布隆过滤器的基本思想。&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
  <entry>
    <title>Designing-Data-Intensive-Application学习笔记5</title>
    <link href="http://yoursite.com/2020/08/16/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B05/"/>
    <id>http://yoursite.com/2020/08/16/Designing-Data-Intensive-Application%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B05/</id>
    <published>2020-08-16T22:53:01.000Z</published>
    <updated>2020-09-10T21:28:15.217Z</updated>
    
    <content type="html"><![CDATA[<p><strong>Chapter4 Encoding and Evolution</strong></p><a id="more"></a><ul><li>Applications inevitably change over time.</li><li><u>schema-on-read</u>(“<strong>schemaless</strong>“) databases don’t enforces schema, so the databases can contain a mixture of older and newer data formats written at different times.</li><li>When data changes usually you will need code change, but in large application, code changes often connot happen instantaneously.<ul><li>With <u>server-side</u> applications you may want to perform a <strong>rolling upgrade</strong> (also known as a <strong>staged rollout</strong>) </li><li>WIth <u>client-side</u> applications you’re at the mercy of the user, who may not install the update for some time.</li></ul></li><li>We need to maintain compatibility in both directions<ul><li><strong>Backward compatbility</strong>: Newer code can read data that was rewritten by older code. (relative easy, since you already knew the existing old data schema)</li><li><strong>Forward compatbility</strong>: Older code can read data taht was written by newer code. (raletive harder, it requires older code to ignore additions made by a newer version of the code)</li></ul></li></ul><h3 id="Formats-for-Encoding-Data"><a href="#Formats-for-Encoding-Data" class="headerlink" title="Formats for Encoding Data"></a>Formats for Encoding Data</h3><ul><li>The <u>transaction from the in-memory representation to a byte sequence</u> is called <strong>encoding</strong> (also known as <strong>serialization</strong> or <strong>marshalling</strong>), and the reverse is called <strong>decoding</strong> (<strong>parsing, deserialization, unmarshalling</strong>).</li><li><strong>Language-Specific Formats</strong>: (bad idea, usually don’t recommended)<ul><li>Java has <strong>java.is.Serialization</strong>, Ruby has <strong>Marshal</strong>, Python has <strong>pickle</strong> and so on. Many third-party libraries also exist, such as <strong>Kryo</strong> for Java.</li><li>Problems<ul><li>The encoding is often tied to a particular programming language, and reading the data in another language is very difficult.</li><li>In order to restore data in same object types, the decoding process needs to be able to instantiate arbitrary classes. (Potentially security risk)</li><li><strong>Versioning</strong> data is often an afterthought in these libraries.</li><li><strong>Efficiency</strong> (CPU time taken to encode or decode, and the size of the encoded structure) is also often an afterthought.</li></ul></li></ul></li><li><strong>JSON, XML, and Binary Variants</strong><ul><li>Problems<ul><li>There is a lot of ambiguity around encoding of numbers. This is a problem when dealing with large numbers (e.g. 2^35);</li><li>JSON and XML have good support for Unicode character string(i.e., human-readable text), but they don’t support binary strings;</li><li>There is optional schema support for both XML and JSON (but complicated to learn and use);</li><li>CSV does not have any schema, so it is up to the application to define the meaning of each row and column.</li></ul></li><li>Despite these flaws, <u>JSON, XML and CSV</u> are good enough for many purposes.</li><li><u><strong>The difficulty of getting different organizations to agree on anything outweighs most other concerns</strong></u>;</li></ul></li><li><strong>Binary encoding</strong>: for data that is used only internally within your organization, there is less pressure to use a <u>lowest-common-denominator</u> encoding format.<ul><li><u>JSON is verbose but it use lots of space</u>, hence it led to the development of a profusion of binary encodings for JSON (MessagePack, BSON, BJSON, UBJSON, BISON and Smile, to name a few) and for XML (WBXML and Fast Infoset, for example) [but they don’t save too much space neither]</li></ul></li><li><strong>Thrift and Protocol Buffers</strong>: <u>Apache Thrift [by Facebook]</u> and <u>Protocol Buffers (protobuf) [by Google]</u> are binary encoding libraries that are based on the same principle.<ul><li>both Thrift and Protocol Buffers<ul><li>Requires a schema for any data that is encoded</li><li>each come with a code generation tool</li></ul></li><li><strong>Thrift</strong> has two different binary encoding formats <ul><li>BinaryProtocal</li><li>CompactProtocal (and DenseProtocal only support C++)</li></ul></li><li><strong>Protocol</strong> only has binary encoding<ul><li>Does not have a list or array data type, but instead has a <u>repeated</u> marker for fields (which is a third option alongside <u>required</u> and <u>optional</u>).</li></ul></li><li><strong>Field tags and schema evolution</strong><ul><li>to maintain backward compatibility, every filed you add after the initial deployment of the schema <u>must be optional or have a default value</u>.</li><li>you can only remove a field that is optional (a required filed can never be removed), and you can never use the same tag number again.</li></ul></li><li><strong>Data Types and schema evolution</strong></li></ul></li><li><strong>Appache Avro</strong>: (subproject of Hadoop) is another binary encoding format that is interestingly different from Protocol Buffers and Thrift.<ul><li><strong>The writer’s schema and the reader’s schema</strong><ul><li>The key idea with Avro is that <u>the writer’s schema and the reader’s scheam don’t have to be the same</u> – they only need to be compatible.</li></ul></li><li><strong>Schema evolution rules</strong><ul><li>To maintain compatibility, you may only add or remove a field that has a default value.</li></ul></li><li>In Avro<ul><li>if you want to allow a field to be null, you have to use a union type. For example, union {null, long, string} field.</li><li>Dosen’t have <u>optional</u> and <u>required</u> markers.</li><li>Change field name: the reader’s schema can contain aliased for field names, so it can match and old write’s schema filed names against the aliases.</li></ul></li><li>A database of schma version is a useful thing to have in any cases, since it acts as documentation and gives you a chance to check schema compatibility.</li><li><strong>Dynamically generated schemas</strong></li><li>Code generation and dynamically typed languages<ul><li>After a schema has been defiend, you can generate code that implements this schema in a programming language of your choice.</li></ul></li></ul></li><li><strong>The Merits of Schemas</strong><ul><li>schema evolution allows the same kind of flexibility as schemaless/schema-on-read JSON databases provided, while also providing better guarantees your data and better tooling.</li></ul></li></ul><h3 id="Modes-of-Dataflow"><a href="#Modes-of-Dataflow" class="headerlink" title="Modes of Dataflow"></a>Modes of Dataflow</h3><ul><li>forwards and backward compatibility, which are important for <strong>evolvability</strong> (upgrade system independently, and not having to change everything at once)</li><li><strong>Via Databases</strong><ul><li><strong>Different values written at different times:</strong> A database generally allows any value to be updated at anytime. (aka <strong><u>data outlives code</u></strong>) using <u>Schema evolution</u> rules.</li><li>Archival storage: use latest schema<ul><li>Encode the data in an analytical-friendly column-oriented format such as <u>Parquet</u></li></ul></li></ul></li><li><strong>Via Sychronized Service call: REST</strong> or <strong>RPC</strong><ul><li><strong>REST</strong> seems to be the predominant style for <strong>public</strong> APIs.</li><li><strong>RPC</strong> frameworks are on requests between services owned by the same organization, typically within the same datacenter.</li><li>The most common arrangement is to have two roles: <strong>client</strong> and <strong>servers</strong>.<ul><li>The API exposed by the server is known as a <strong><u>service</u></strong>.</li></ul></li><li>Moreover, a server can itself be a client to another service (e.g. web app server acts as a client to a database)<ul><li><strong><u>service-oriented architecture (SOA)</u></strong>, more recently refined and rebranded as <strong><u>microservices architecture</u></strong>;</li><li>A key design goal of a <strong><u>service-oriented/microservices</u></strong> architecture is <strong>to make the application easier to change and maintain by making services independently deployable and evovable</strong>.</li></ul></li><li><strong>Web services: REST</strong> and <strong>SOAP</strong><ul><li><strong>REST</strong> is not a protocal, but rather a <strong><u>design philosophy</u></strong> that builds upon the principles of HTTP. An API designed according to the principles of REST is called <u>RESTful</u>.<ul><li>A defination format such as <strong><u>OpenAPT</u></strong>, also known as <u><strong>Swagger</strong></u>, can be used to describe RESTful APIs and produce documentation.</li></ul></li><li><strong>SOAP</strong> is a XML-based protocal for making network API requests.<ul><li>The API of a SOAP web service is described using an XML-based language called the <strong><u>Web Service Description Language</u></strong>, or <strong>WSDL</strong>. WSDL enables code generation so that a client can access a remote service using local classes and mothod calls;(good for static typed language like Java)</li></ul></li></ul></li><li><strong>RPC</strong><ul><li>The  RPC model tries to make a request to remote network service look the same as calling a function or method in your programming language, within the same process (this abstraction is called <strong><u>location transparency</u></strong>).</li><li>Althought RPC seems convenient at first, the approach is <strong><u>fundamentally flawed</u></strong>. <u>A netwrok request is very different from a local function call</u>.<ul><li>A network request is unpredictable;</li><li>A network response is unpredictable;</li><li><u>Idempotence is required for re-try</u>;</li><li>A network request is much slower than a function call, and its latency is also wildy variable.</li></ul></li><li><strong>Current directions for RPC</strong>: <u>Thrift</u> and <u>Avro</u> come with RPC support included, <u>gRPC</u> is an RPC implementation using Protocol Buffers, <u>Finagel</u> also uses Thrift, and <u>Rest.li</u> uses JSON over HTTP.<ul><li>Custom RPC protocols with a binary encoding format can achieve better performance than something generic like HSON over REST;</li><li>But REST it is good for experimentation and debugging;</li></ul></li><li>Data encoding and evolution for RPC</li></ul></li></ul></li><li><strong>Via Asynchronous message passing</strong>: <u>asynchronous message-passing systems</u>, which are somewhere between RPC and databases<ul><li><strong>Advantages</strong><ul><li>Act as buffer;</li><li>Auto-redeliver message;</li><li>Allow message fan-out;</li><li>Decouple the sender and recipient;</li></ul></li><li><strong>Disadvantage</strong><ul><li><u>message-passing communication is usually one-way</u>: a sender normally doesn’t expect receive a reply to its messages.</li><li><u>communication pattern is asynchronous</u>: the sender doesn’t wait for the message to be delivered, but simply sends it and then forgets about it.</li></ul></li><li><strong>Message brokers</strong>:open source implementation such as <strong><u>RabbitMQ</u></strong>, <u>ActiveMQ, HornetQ, NATS, and Apache <strong>Kafka</strong></u> have become popular.<ul><li>one process sends a message to a named <strong>queue</strong> or <strong>topic</strong>, and the <strong>broker</strong> ensures that the message is delivered to one or more <strong>cunsumers</strong> of or <strong>subscribers</strong> to that <strong>queue</strong> or <strong>topic</strong>.</li><li>There can be many <strong>producers</strong> and many <strong>consumers</strong> on the same topic.</li><li>A topic porvides only one-way data flow. However, <u>a consumer may itself publish messages to another topic</u> (so you can chain them together), or to reply queue that is consumed by the sender of the original message (allowing a request/response data flow, similar to RPC)</li><li><strong>Distributed actor frameworks</strong>: The <u>actor model</u> is a programming model for concurrency in a single process. (e.g. Aka, Orlean, Erlang OTP)<ul><li>A distributed actor framework essentially integrates a message broker and the actor programming model into a single framework.</li></ul></li></ul></li></ul></li></ul><h3 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h3><ul><li>many services need to support rolling upgrades -&gt; evolvability<ul><li>released without downtime</li><li>make deployments less risky</li></ul></li><li>all data flowing around the system is encoded in a way that provides backward compatibility (new code can read old data) and forward compatibility (old code can read new data)<ul><li>data encoding formats and it pros and cons;</li></ul></li><li>several modes of dataflow, illustrating different scenarios in which data encoding are important.<ul><li><strong>Database</strong>, where the process writing to the database encodes the data and the process reading from the database decodes it.</li><li><strong>RPC and REST APIs</strong>, where the client encodes a request, the server decodes the request and encodes a response, and the client finally decode the response.</li><li><strong>Asynchronous message passing</strong> (using message broker or actors), where modes communicate by sending each other message that are encoded by the sender and decoded by the recipient.</li></ul></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;strong&gt;Chapter4 Encoding and Evolution&lt;/strong&gt;&lt;/p&gt;
    
    </summary>
    
    
    
      <category term="System Design" scheme="http://yoursite.com/tags/System-Design/"/>
    
  </entry>
  
</feed>
